<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://auroralhl.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://auroralhl.github.io/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-04-23T17:20:23+00:00</updated><id>https://auroralhl.github.io/feed.xml</id><title type="html">blank</title><subtitle>Graduate student at Peking University specializing in Agentic RL, WebAgent/OSAgent systems, and Self-Play training paradigms. Research focuses on enhancing LLM agent capabilities through novel RL approaches. Experienced RL algorithm engineer with internships at Alibaba and Moonshot AI. </subtitle><entry><title type="html">A Deep Dive into OpenClaw</title><link href="https://auroralhl.github.io/blog/2026/openclaw-deep-research/" rel="alternate" type="text/html" title="A Deep Dive into OpenClaw"/><published>2026-03-22T16:00:00+00:00</published><updated>2026-03-22T16:00:00+00:00</updated><id>https://auroralhl.github.io/blog/2026/openclaw-deep-research</id><content type="html" xml:base="https://auroralhl.github.io/blog/2026/openclaw-deep-research/"><![CDATA[<h1 id="1-openclaw-是什么">1. OpenClaw 是什么</h1> <h2 id="11-官方定位">1.1 官方定位</h2> <p>OpenClaw <a href="https://openclaw.ai/">官网</a>、<a href="https://github.com/openclaw/openclaw">官方仓库</a>和<a href="https://docs.openclaw.ai/">官方文档</a>都把它描述为运行在个人设备上的 AI 助手系统，而不是托管式 SaaS 聊天产品。</p> <p>其核心思想可以概括为三点：</p> <ul> <li><code class="language-plaintext highlighter-rouge">self-hosted gateway</code>：真正长期运行的是 Gateway，assistant 只是建立在 Gateway 之上的产品体验。</li> <li><code class="language-plaintext highlighter-rouge">multi-surface access</code>：它可以接入 WhatsApp、Telegram、Discord、Web UI、CLI 等多种入口。</li> <li><code class="language-plaintext highlighter-rouge">tool-using assistant</code>：它不是只负责回答问题，而是把浏览器、终端、文件处理等能力挂到同一个 agent 体系里。</li> </ul> <p>官方 <a href="https://raw.githubusercontent.com/openclaw/openclaw/main/VISION.md">VISION.md</a> 里对方向的表述很明确：目标不是做另一个只会对话的 AI，而是做“<strong>the AI that actually does things</strong>”。</p> <h2 id="12-起源">1.2 起源</h2> <p><img src="/assets/img/posts/openclaw/openclaw-timeline.svg" alt="openclaw timeline"/> 从公开时间线看，openclaw的演化过程，可以把它概括成四个阶段。<sup id="fnref:openclaw-lore"><a href="#fn:openclaw-lore" class="footnote" rel="footnote" role="doc-noteref">1</a></sup></p> <ol> <li><code class="language-plaintext highlighter-rouge">Warelay</code> 阶段<br/> 在最早期，是一个偏 WhatsApp gateway 的系统，名字叫 <code class="language-plaintext highlighter-rouge">Warelay</code>。历史 README 里，项目标题就是 Warelay — WhatsApp Relay CLI (Twilio)，功能写得很直白：用 Twilio 发送、监听、Webhook 处理 WhatsApp 消息，并支持配置式自动回复。所以更准确地说，Warelay 像是 OpenClaw 的“消息入口层原型”：它一开始解决的是“怎么把系统接进 WhatsApp”，然后才逐步长成今天更完整的 OpenClaw。</li> <li><code class="language-plaintext highlighter-rouge">Clawd / 早期龙虾叙事</code> 阶段<br/> 官方 lore 页面把项目中的龙虾人格写成 <code class="language-plaintext highlighter-rouge">Clawd</code>，并明确给出了一个时间范围：<code class="language-plaintext highlighter-rouge">2025 年 11 月 25 日 - 2026 年 1 月 27 日</code>。这一阶段最重要的不是具体产品名，而是项目开始把“AI agent + 龙虾人格 + 长期记忆 + shell access”这些元素合并成一种独特的社区叙事。</li> <li><code class="language-plaintext highlighter-rouge">Moltbot：2026 年 1 月</code><br/> 由于和 <code class="language-plaintext highlighter-rouge">Claude</code> 发音过近，改名为Moltbot，但这个名字说起来也不太顺口……</li> <li><code class="language-plaintext highlighter-rouge">最终形态：2026 年 1 月 30 日</code><br/> 1 月 27 日凌晨 5 点，社区成员聚集在 Discord。数百个名字被提议：Shelldon、Pinchy、Thermidor、Crusty、Lobstar、Nacre、Scuttlebot……最终，OpenClaw 胜出。因为蜕壳是龙虾成长的方式。而成长正是正在发生的事情。这只被称为 Clawd 的甲壳类动物正式蜕壳了。</li> </ol> <p>名字的含义：</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>OpenClaw = OPEN + CLAW

= 开源，向所有人开放

= 我们的龙虾传承，我们从何而来

= 钳即是法 🦞

= 你的助手。你的机器。你的规则。
</code></pre></div></div> <p><img src="/assets/img/posts/openclaw/openclaw-name-meaning.svg" alt="image"/></p> <h2 id="13-openclaw爆火的原因">1.3 OpenClaw爆火的原因？</h2> <p>个人觉得，相比于claude code，codex这类coding Agent，openclaw做对的几件事：</p> <ol> <li>打通了日常用的通讯软件。Agent对话的入口不是终端命令行，而是日常用的软件，对大众来说是一件很重要的事。毕竟除了程序员，有几个人会打开cmd呢？</li> <li> <p>能做coding，但不止于coding。作为一个中台，把电脑的其他软件连接起来了（coding agent，比如claude code、codex只是openclaw可以用的一个工具），从而可以做一些日常任务（日程管理、邮件管理、飞书编辑文档之类的）。能做的事更多，就更像一个“个人助手”了，而不只是一个coding 助手。所以openclaw火了之后，Anthropic 和OpenAI也快速拓展了claude code和codex的scope，推出了能干更多活的桌面级应用：</p> <ol> <li> <p>Claude Cowork</p> <ul> <li>2026年1月12日：Anthropic 在官方 <a href="https://support.claude.com/en/articles/12138966-release-notes">Release notes</a> 里上线了 Cowork research preview，当时是 Claude Desktop（仅 macOS），先给 Max 用户。</li> <li>2026年1月16日：Anthropic 又在同一份 <a href="https://support.claude.com/en/articles/12138966-release-notes">Release notes</a> 里写明，Cowork 扩展到 Pro 用户。</li> <li>2026年1月30日：Anthropic 做了官方活动 <a href="https://www.anthropic.com/webinars/future-of-ai-at-work-introducing-cowork">The Future of AI at Work: Introducing Cowork</a>，算是一次正式公开介绍。</li> </ul> </li> </ol> <ul> <li>OpenAI 的 Codex 桌面应用 <ul> <li>2026年2月2日：OpenAI 在官方文章 <a href="https://openai.com/index/introducing-the-codex-app/">Introducing the Codex app</a> 里发布 Codex app for macOS。</li> <li>2026年3月4日：OpenAI 在同一篇官方文章里补充更新，Windows 版上线；这个时间在官方 <a href="https://help.openai.com/en/articles/11391654">ChatGPT Business release notes</a> 里也能看到。</li> </ul> </li> </ul> </li> <li>长久记忆系统的设计。相比于claude code这类除了全局记忆，以及project的记忆之外。openclaw两层memory的设计，以及”SOUL.md”,”<code class="language-plaintext highlighter-rouge">IDENTITY.md</code>”,”USER.md”的设计，使得openclaw更像一个能慢慢熟悉用户习惯的”助手”，这也是为什么”养虾”火遍全国。这些文件是需要用户和openclaw不断对话，openclaw慢慢更新的，从而达到一个你越用它，它越懂你的效果。</li> <li>随时随地待命。openclaw的gateway在后台持续运行，可以做到人不在电脑旁，人在睡觉时，它能持续干活。还可以做一些定时任务（后来claude code 也推出了\loop的命令），比如每天定时抓取推送新闻。</li> </ol> <h1 id="2-原理与结构概览">2. 原理与结构概览</h1> <p>本章节先概览一下openclaw的总体架构，大致了解有哪些模块，每个模块的功能，后续章节将详细介绍一些模块的细节。</p> <h2 id="21-基本架构">2.1 基本架构</h2> <p>OpenClaw 并不是一个套在 AI 模型 API 外层的聊天机器人封装，而是一个<strong>面向 AI Agent 的操作系统</strong>。<sup id="fnref:openclaw-architecture-explained"><a href="#fn:openclaw-architecture-explained" class="footnote" rel="footnote" role="doc-noteref">2</a></sup></p> <blockquote> <p class="block-warning">LLM负责提供智能，OpenClaw 负责提供执行环境。</p> </blockquote> <p>OpenClaw 采用一种以单一 Gateway 为核心的<strong>中心辐射式架构</strong>。这个 Gateway 充当控制平面，位于用户输入端（如WhatsApp、Web UI、CLI）与 AI Agent 之间：</p> <p><img src="/assets/img/posts/openclaw/openclaw-architecture.png" alt="openclaw architecture"/></p> <p>OpenClaw 的关键在于，它将接口层（消息从哪里来）与Agent Runtime（Agent执行发生在哪里）分离开来：</p> <ul> <li><strong>Gateway</strong> 是一个 WebSocket 服务器，负责连接消息平台和各类控制界面，并将每一条路由过来的消息分发给 Agent Runtime。</li> <li><strong>Agent Runtime</strong> 负责端到端运行整个 AI 循环：从会话历史和记忆中组装上下文、调用模型、基于系统可用能力执行工具调用（如浏览器自动化、文件操作、Canvas、定时任务等），并持久化更新后的状态。 这意味着：</li> </ul> <ol> <li>你可以拥有一个<strong>持续在线</strong>的助手，并通过任何你已经在使用的消息应用访问它；</li> <li>同时，对话状态和工具访问权限都由你<strong>自己的硬件进行集中管理</strong>。</li> </ol> <h2 id="22-gateway控制平面">2.2 Gateway：控制平面</h2> <p>OpenClaw 的中心不是某个聊天窗口，而是一个长期运行的 Gateway。Gateway 负责承接所有消息面、控制端和节点设备连接；CLI、Web UI、桌面端以及 automations 都通过 WebSocket 连接到它。<sup id="fnref:openclaw-architecture-doc"><a href="#fn:openclaw-architecture-doc" class="footnote" rel="footnote" role="doc-noteref">3</a></sup></p> <p>这意味着 OpenClaw 的核心设计不是“打开一个进程，用完即走”，而是“维护一个持续存在的 agent 环境”：</p> <ul> <li>会话状态是长期存在的。</li> <li>渠道和设备接入被统一管理。</li> <li>自动化、控制界面和多端协同都围绕 Gateway 展开。</li> </ul> <h2 id="23-agent-runtime">2.3 Agent Runtime</h2> <p><strong>Agent Runtime（运行时）</strong> 是一个宏观的概念，指的是<strong>支撑 agent 运行所需的整个基础设施和执行环境</strong>，包括：</p> <ul> <li>进程管理、生命周期控制</li> <li>工具注册与调度</li> <li>上下文/状态管理</li> <li>模型调用封装</li> <li>事件系统、流式输出</li> <li>错误处理、重试机制</li> <li>认证、配额、限流</li> </ul> <h2 id="24-tools--skills--plugins能力扩展">2.4 Tools / Skills / Plugins：能力扩展</h2> <p>OpenClaw 官方把能力层拆成三部分。<sup id="fnref:openclaw-tools-and-plugins"><a href="#fn:openclaw-tools-and-plugins" class="footnote" rel="footnote" role="doc-noteref">4</a></sup></p> <ol> <li><strong>Tools</strong>是 Agent 实际调用的能力 工具是 Agent 可以调用的类型化函数，例如 exec、browser、web_search、message。OpenClaw 内置了一组工具，可以通过插件增加额外的工具。 对 Agent 来说，工具就是一组发送给模型 API 的结构化函数。</li> <li><strong>Skills</strong>教会 Agent 何时以及如何行动 技能是注入到系统提示词中的 Markdown 文件（SKILL.md）。技能为 Agent 提供上下文、约束以及使用工具的分步指导，帮助它更有效地完成任务。技能可以存放在你的工作区、共享文件夹中，也可以由插件内置提供。</li> <li><strong>Plugins</strong>将一切能力打包整合 插件是一种能力包，可以注册任意组合的功能模块，包括消息通道、模型提供方、工具、技能、语音、图像生成等。部分插件属于核心插件（随 OpenClaw 一起发布），另一些则是外部插件（由社区发布到 npm）。</li> </ol> <p>OpenClaw 的设计目标之一，就是在<strong>不修改核心代码的前提下实现扩展</strong>。插件可以从以下四个方面扩展整个系统的能力：</p> <ul> <li>channel插件：接入更多消息平台，例如 Microsoft Teams、Mattermost、飞书、微信等</li> <li>memory插件：提供替代性的memory后端，例如向量数据库、知识图谱，而不局限于默认的 SQLite</li> <li>Provider 插件：接入自定义的大语言模型服务商或自托管模型</li> <li>tools插件：扩展内置 bash、浏览器和文件操作之外的自定义能力 <img src="/assets/img/posts/openclaw/openclaw-plugin-link.png" alt="openclaw plugin link"/></li> </ul> <p class="figure-note">图：OpenClaw 插件扩展关系示意。来源见<sup id="fnref:openclaw-architecture-explained:1"><a href="#fn:openclaw-architecture-explained" class="footnote" rel="footnote" role="doc-noteref">2</a></sup>。</p> <p>插件系统位于 extensions/ 目录下，openclaw运行时通过扫描工作区中的 package 来发现并加载插件。src/plugins/loader.ts 中的插件加载器会扫描工作区内的各个 package，检查其 package.json 是否声明了 openclaw.extensions 字段；若存在该声明，则会按照相应的 schema 进行校验，并在检测到相关配置后，在运行时完成加载。</p> <pre><code class="language-PlainText">扫描目录（4个来源）
    ↓
检查 package.json 有没有 openclaw.extensions 字段
    ↓ 有 → 进入候选列表
检查 openclaw.json 的 plugins 配置
    ├── plugins.enabled === false → ❌ 全部禁用
    ├── entries[id].enabled === false → ❌ 该插件禁用
    ├── 工作区插件 + 不在 allow 列表 → ❌ 默认禁用
    ├── entries[id].enabled === true → ✅ 启用
    └── 全局/内置插件（白名单内）→ ✅ 默认启用
</code></pre> <h1 id="3-agents-实现细节">3 Agents 实现细节</h1> <h2 id="31-agent-runtime">3.1 Agent runtime</h2> <p>OpenClaw 的 runtime 包含了两层：它自己的编排逻辑（外层）+ pi 提供的推理循环（内层），共同构成完整的 agent 执行环境。</p> <ul> <li><strong>OpenClaw 外层循环</strong> = 属于 <strong>runtime 层</strong>：负责编排、异常恢复、模型降级、压缩重试——这些都是”基础设施”关心的事</li> <li><strong>pi-agent-core 内层循环</strong> = 属于 <strong>loop 层</strong>：纯粹的 LLM 推理 → tool_use → 再推理的核心循环</li> </ul> <p><strong>pi 是作为一个嵌入式库</strong>被openclaw使用 ，也就是openclaw runtime 把 pi 的 loop 能力”内嵌”进来，而不是作为独立进程。</p> <p><img src="/assets/img/posts/openclaw/openclaw-agent-loops.svg" alt="openclaw agent loops"/></p> <p class="figure-note">图：OpenClaw agent 双层循环示意。来源：基于 OpenClaw 源码绘制。</p> <h3 id="工作原理">工作原理</h3> <p><strong>1. <code class="language-plaintext highlighter-rouge">agent</code> RPC — 接单不阻塞</strong></p> <p>消息从 Channel 进入后，<code class="language-plaintext highlighter-rouge">agent</code> RPC 做的第一件事是”快速接单”：验证参数、解析会话标识（<code class="language-plaintext highlighter-rouge">sessionKey</code> / <code class="language-plaintext highlighter-rouge">sessionId</code>）、把会话元数据落库，然后立刻返回 <code class="language-plaintext highlighter-rouge">{ runId, acceptedAt }</code>。<strong>这个设计让调用方无需等待推理完成，后续通过 <code class="language-plaintext highlighter-rouge">runId</code> 异步追踪进度。</strong></p> <p>也就说，<strong>Gateway 和 Agent 运行在不同的进程里</strong>，RPC 正是这个设计的体现。具体来说：</p> <ul> <li><strong>Gateway 进程</strong>：长期运行的服务端，监听各个渠道（Slack、Discord、Web UI 等）的消息，负责路由、鉴权、会话管理</li> <li><strong>Agent 进程</strong>：每次 run 时由 Gateway 按需启动（或从进程池里取），执行完任务后可以退出或回收</li> </ul> <p>两者通过 <strong>ACP（Agent Communication Protocol）</strong> 用 RPC 通信，Gateway 把用户消息、工具权限、context 等打包发给 Agent 进程，Agent 把流式输出（tool calls、assistant text、lifecycle events）通过 <code class="language-plaintext highlighter-rouge">result.payloads</code> 流回 Gateway，Gateway 再转发给对应的渠道。</p> <p>这个设计带来几个好处：</p> <ul> <li><strong>隔离性</strong>：Agent 崩溃不影响 Gateway，Gateway 可以重启 Agent 而不丢失会话</li> <li><strong>可扩展性</strong>：Gateway 可以同时管理多个 Agent 进程（多用户、多渠道并发）</li> <li><strong>灵活性</strong>：Agent 进程可以跑在不同机器上，Gateway 只需要知道 RPC 地址</li> </ul> <p>所以OpenClaw 的 <code class="language-plaintext highlighter-rouge">agent.wait</code> 是异步等待——Gateway 发完请求就可以去处理其他事情，不需要阻塞等 Agent 跑完。</p> <p><strong>2. <code class="language-plaintext highlighter-rouge">agentCommand</code> — 外层编排循环</strong></p> <p>真正的执行从 <code class="language-plaintext highlighter-rouge">agentCommand</code> 开始。它是图中<strong>外层循环</strong>的主体，负责在调用 pi 之前把一切准备好：</p> <ul> <li>解析模型选择，确定思考级别（<code class="language-plaintext highlighter-rouge">xhigh / high / off</code>）和详细模式默认值</li> <li>加载当前会话的 Skills 快照，注入到系统提示</li> <li>将上述配置打包，交给 <code class="language-plaintext highlighter-rouge">runEmbeddedPiAgent</code> 驱动 pi 运行</li> <li>兜底保障：如果 pi 内层循环异常退出而没有发出生命周期结束/错误事件，<code class="language-plaintext highlighter-rouge">agentCommand</code> 会补发，确保下游状态机不会卡住</li> </ul> <p><strong>3. <code class="language-plaintext highlighter-rouge">runEmbeddedPiAgent</code> — pi 的启动与守护</strong></p> <p>这是外层循环调用 pi 的核心函数。pi 不是独立进程，而是作为库<strong>嵌入在 OpenClaw 进程内</strong>直接运行。<code class="language-plaintext highlighter-rouge">runEmbeddedPiAgent</code> 做三件事：</p> <ul> <li><strong>序列化</strong>：通过”每会话队列 + 全局队列”双层机制，保证同一会话的请求串行执行，避免工具调用和会话历史产生竞态</li> <li><strong>驱动推理</strong>：解析模型和认证配置，构建 pi session，订阅 pi 的事件流，把助手增量和工具事件实时转发出去</li> <li><strong>超时守护</strong>：挂一个中止计时器（默认 600 秒），超时则强制终止 pi 运行，防止失控</li> </ul> <p>函数最终返回 <code class="language-plaintext highlighter-rouge">result.payloads</code>（最终回复内容）和使用量元数据，交还给外层循环。</p> <p><strong>4. <code class="language-plaintext highlighter-rouge">subscribeEmbeddedPiSession</code> — 事件桥接层</strong></p> <p>pi 内层推理循环产生的原始事件，经由这个函数统一翻译成 OpenClaw 的标准流格式，再推送给 IDE / Channel：</p> <table> <thead> <tr> <th>pi 内部事件</th> <th>OpenClaw 流</th> </tr> </thead> <tbody> <tr> <td>工具调用开始/更新/结束</td> <td><code class="language-plaintext highlighter-rouge">stream: "tool"</code></td> </tr> <tr> <td>助手文字增量</td> <td><code class="language-plaintext highlighter-rouge">stream: "assistant"</code></td> </tr> <tr> <td>推理开始 / 结束 / 出错</td> <td><code class="language-plaintext highlighter-rouge">stream: "lifecycle"</code> (<code class="language-plaintext highlighter-rouge">phase: start / end / error</code>)</td> </tr> </tbody> </table> <p>这一层是图中”消息返回”框的实现：<code class="language-plaintext highlighter-rouge">result.payloads</code> 经由 Channel 最终流回用户和 IDE。</p> <p><strong>5. <code class="language-plaintext highlighter-rouge">agent.wait</code> — 异步等待结果</strong></p> <p>调用方通过 <code class="language-plaintext highlighter-rouge">agent.wait</code> + <code class="language-plaintext highlighter-rouge">runId</code> 挂起等待，底层是 <code class="language-plaintext highlighter-rouge">waitForAgentJob</code> 监听生命周期事件。一旦收到 <code class="language-plaintext highlighter-rouge">end</code> 或 <code class="language-plaintext highlighter-rouge">error</code>，立即返回：</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>{ status: ok | error | timeout, startedAt, endedAt, error? }
</code></pre></div></div> <p>注意这里的超时（默认 30 秒）只影响<strong>等待侧</strong>，不会中止智能体本身的运行——智能体有自己独立的 600 秒运行超时。</p> <h2 id="32-agent-loop">3.2 Agent Loop</h2> <p><strong>Agent Loop（循环）</strong> 是 runtime 内部的<strong>核心执行逻辑</strong>，描述 agent 如何一步步推进任务：</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>感知输入 → 调用 LLM → 决策（是否用工具）→ 执行工具 → 将结果反馈给 LLM → 循环
</code></pre></div></div> <p><img src="/assets/img/posts/openclaw/claude-code-agentic-loop.svg" alt="claude code agentic loop"/></p> <p class="figure-note">图：Claude Code 的 agentic loop。来源与原图见<sup id="fnref:claude-code-loop-diagram"><a href="#fn:claude-code-loop-diagram" class="footnote" rel="footnote" role="doc-noteref">5</a></sup>。</p> <p>这里其实和claude code和codex是类似的，只不过openclaw用的是 <strong>Pi-Agent</strong>。<sup id="fnref:pi-minimal-agent"><a href="#fn:pi-minimal-agent" class="footnote" rel="footnote" role="doc-noteref">6</a></sup></p> <table> <thead> <tr> <th>维度</th> <th>Agent Runtime</th> <th>Agent Loop</th> </tr> </thead> <tbody> <tr> <td>层次</td> <td>基础设施层</td> <td>业务逻辑层</td> </tr> <tr> <td>职责</td> <td>提供能力、管理资源</td> <td>执行推理、驱动任务</td> </tr> <tr> <td>生命周期</td> <td>贯穿整个进程</td> <td>每次任务调用时运行</td> </tr> </tbody> </table> <h2 id="33-workspace">3.3 Workspace</h2> <p><a href="https://docs.openclaw.ai/concepts/agent-workspace">Agent Workspace</a>是 agent 的”家”，默认位于 <code class="language-plaintext highlighter-rouge">~/.openclaw/workspace</code>，是文件工具的工作目录和上下文的来源。注意它<strong>不是硬隔离边界</strong>——工具解析相对路径时以 workspace 为基准，但绝对路径仍可访问宿主机其他位置，需要开启 sandbox 才能真正隔离。</p> <p>Workspace 里的文件分两类：</p> <h3 id="每次自动注入到-system-prompt-的project-context">每次自动注入到 System Prompt 的（Project Context）</h3> <table> <thead> <tr> <th>文件</th> <th>作用</th> </tr> </thead> <tbody> <tr> <td><code class="language-plaintext highlighter-rouge">AGENTS.md</code></td> <td>Agent 的操作规则、记忆使用方式、行为优先级</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">SOUL.md</code></td> <td>人格、语气和边界设定</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">USER.md</code></td> <td>用户是谁、如何称呼用户</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">IDENTITY.md</code></td> <td>Agent 的名字、风格、emoji</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">TOOLS.md</code></td> <td>本地工具和约定的说明（不控制工具可用性，只是引导）</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">HEARTBEAT.md</code></td> <td>心跳运行的简短检查清单，保持简短避免 token 消耗</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">BOOTSTRAP.md</code></td> <td>首次运行的初始化仪式，完成后删除</td> </tr> </tbody> </table> <p><code class="language-plaintext highlighter-rouge">HEARTBEAT.md</code> 是 OpenClaw 的<strong>定时心跳机制</strong>的核心配置文件，理解它需要先知道”心跳”是什么。</p> <h4 id="什么是心跳heartbeat">什么是心跳（Heartbeat）</h4> <p>OpenClaw 支持让 agent <strong>在没有用户消息的情况下定时自动运行</strong>，这就是心跳。比如：</p> <ul> <li>每天早上 8 点自动检查今天的任务</li> <li>每小时检查一次某个目录有没有新文件</li> <li>定时做一些维护性工作（清理、归档、同步）</li> </ul> <p>心跳的触发规则配置在 <code class="language-plaintext highlighter-rouge">~/.openclaw/cron/jobs.json</code> 里。</p> <p><code class="language-plaintext highlighter-rouge">HEARTBEAT.md</code>是<strong>心跳运行时的行为说明书</strong>，告诉 agent 在定时触发时应该做什么、检查什么。心跳运行是一种特殊的 <code class="language-plaintext highlighter-rouge">contextMode: lightweight</code> 模式——为了省 token，<strong>只注入 <code class="language-plaintext highlighter-rouge">HEARTBEAT.md</code></strong>，其他 bootstrap 文件（<code class="language-plaintext highlighter-rouge">AGENTS.md</code>、<code class="language-plaintext highlighter-rouge">SOUL.md</code> 等）全部跳过。所以 <code class="language-plaintext highlighter-rouge">HEARTBEAT.md</code> 需要写得<strong>简短精炼</strong>，只包含心跳场景下必要的检查清单。一个典型的 <code class="language-plaintext highlighter-rouge">HEARTBEAT.md</code> 示例如下：</p> <div class="language-markdown highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="gh"># Heartbeat</span>

你是一个定时运行的 agent。每次心跳时：
<span class="p">
1.</span> 读取 memory/today.md，检查今天有没有未完成的任务
<span class="p">2.</span> 如果有紧急任务，通过 Slack 渠道通知用户
<span class="p">3.</span> 检查 workspace/inbox/ 目录，处理新文件
<span class="p">4.</span> 更新 memory/YYYY-MM-DD.md，记录本次心跳结果
</code></pre></div></div> <h3 id="按需读取的不自动注入">按需读取的（不自动注入）</h3> <table> <thead> <tr> <th>文件</th> <th>作用</th> </tr> </thead> <tbody> <tr> <td><code class="language-plaintext highlighter-rouge">memory/YYYY-MM-DD.md</code></td> <td>每日记忆日志，建议每次会话读今天和昨天</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">MEMORY.md</code></td> <td>精选长期记忆，仅在主会话加载，越长越占 context</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">skills/SKILL.md</code></td> <td>工作区专属 Skills，用到时才读</td> </tr> </tbody> </table> <blockquote> <p class="block-warning"><code class="language-plaintext highlighter-rouge">~/.openclaw/openclaw.json</code>（配置）、<code class="language-plaintext highlighter-rouge">credentials/</code>（密钥）、<code class="language-plaintext highlighter-rouge">agents/sessions/</code>（会话记录）<strong>不在 workspace 里</strong>，不要混淆，也不要提交到 git。</p> </blockquote> <p>Workspace 建议放入私有 git repo 备份，方便跨机器迁移。迁移时 sessions 需要单独从 <code class="language-plaintext highlighter-rouge">~/.openclaw/agents/&lt;agentId&gt;/sessions/</code> 复制。</p> <h2 id="34-system-prompt">3.4 System Prompt</h2> <p>在 OpenClaw 里，<strong>System Prompt 不是一段固定的”前言”</strong>，而是 runtime 在每次 agent run 开始前动态组装出来的一份运行时说明书。<sup id="fnref:system-prompt-doc"><a href="#fn:system-prompt-doc" class="footnote" rel="footnote" role="doc-noteref">7</a></sup></p> <p>这里有一个非常重要的边界：这份 prompt <strong>归 OpenClaw 自己管理</strong>，而<strong>不是</strong>直接沿用 pi-coding-agent 的默认 prompt。pi 提供的是内层 loop 能力；但”这次运行到底要把哪些工具、约束、环境和上下文告诉模型”，是 OpenClaw 在更外层决定的。</p> <h3 id="区块结构">区块结构</h3> <p>Openclaw的 system prompt 设计得很紧凑，但区块是固定的：</p> <table> <thead> <tr> <th>区块</th> <th>作用</th> </tr> </thead> <tbody> <tr> <td><strong>Tooling</strong></td> <td>当前可用工具列表及简短说明</td> </tr> <tr> <td><strong>Safety</strong></td> <td>提醒模型不要出现 power-seeking、绕过监督等行为</td> </tr> <tr> <td><strong>Skills</strong></td> <td>有技能可用时，告诉模型去哪里读取 <code class="language-plaintext highlighter-rouge">SKILL.md</code>（按需加载，不全量注入）</td> </tr> <tr> <td><strong>OpenClaw Self-Update</strong></td> <td>如何运行 <code class="language-plaintext highlighter-rouge">config.apply</code> 和 <code class="language-plaintext highlighter-rouge">update.run</code></td> </tr> <tr> <td><strong>Workspace / Documentation</strong></td> <td>当前工作目录、本地文档路径，以及应优先查阅哪些资料</td> </tr> <tr> <td><strong>Workspace Files</strong></td> <td>说明哪些 bootstrap 文件会被直接注入上下文</td> </tr> <tr> <td><strong>Sandbox</strong></td> <td>当前是否启用沙箱、沙箱路径，以及是否允许 elevated exec</td> </tr> <tr> <td><strong>Current Date &amp; Time / Runtime / Reasoning</strong></td> <td>当前时区、运行环境、宿主信息、模型信息和推理可见性</td> </tr> <tr> <td><strong>Reply Tags / Heartbeats</strong></td> <td>支持的回复标签语法、心跳提示词和确认行为</td> </tr> </tbody> </table> <p>Skills 的注入方式值得注意。OpenClaw 不会把所有 Skill 的内容直接塞进 prompt，而是通过 <code class="language-plaintext highlighter-rouge">formatSkillsForPrompt</code> 生成一份紧凑的清单，只包含每个 Skill 的名称、描述和文件路径：</p> <div class="language-xml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nt">&lt;available_skills&gt;</span>
  <span class="nt">&lt;skill&gt;</span>
    <span class="nt">&lt;name&gt;</span>defuddle<span class="nt">&lt;/name&gt;</span>
    <span class="nt">&lt;description&gt;</span>Extract clean markdown content from web pages...<span class="nt">&lt;/description&gt;</span>
    <span class="nt">&lt;location&gt;</span>/path/to/SKILL.md<span class="nt">&lt;/location&gt;</span>
  <span class="nt">&lt;/skill&gt;</span>
<span class="nt">&lt;/available_skills&gt;</span>
</code></pre></div></div> <p>模型看到这份清单后，在需要时自己用 <code class="language-plaintext highlighter-rouge">read</code> 工具去加载对应的 <code class="language-plaintext highlighter-rouge">SKILL.md</code>。这样基础 prompt 保持小巧，Skill 的详细指令只在真正用到时才进入上下文。</p> <h3 id="prompt-mode按场景控制信息密度">Prompt Mode：按场景控制信息密度</h3> <p>OpenClaw 会根据运行场景切换不同的 <code class="language-plaintext highlighter-rouge">promptMode</code>：</p> <ul> <li><strong><code class="language-plaintext highlighter-rouge">full</code></strong>（默认）：包含上述所有区块，用于主 agent 的完整运行</li> <li><strong><code class="language-plaintext highlighter-rouge">minimal</code></strong>：用于子代理，去掉 Skills、Memory Recall、Self-Update、Reply Tags、Heartbeats 等部分，只保留 Tooling、Safety、Workspace、Sandbox、Runtime 和必要注入上下文</li> <li><strong><code class="language-plaintext highlighter-rouge">none</code></strong>：只保留最基础的身份行</li> </ul> <p>这说明 OpenClaw 并不是把所有上下文无差别塞给每一个 agent，而是在主 agent 与 sub-agent 之间主动控制 prompt 体积和信息密度。<strong>判断逻辑是这样的：</strong></p> <ul> <li><strong>用户直接发消息给 agent</strong> → <code class="language-plaintext highlighter-rouge">lane</code> 为默认值 → <code class="language-plaintext highlighter-rouge">promptMode: full</code>，完整 prompt</li> <li><strong>主 agent 用 <code class="language-plaintext highlighter-rouge">agent_run</code> 工具派生了一个子代理</strong> → <code class="language-plaintext highlighter-rouge">lane = AGENT_LANE_SUBAGENT</code> → <code class="language-plaintext highlighter-rouge">promptMode: minimal</code>，精简 prompt</li> <li><strong>心跳运行</strong>（定时触发，不是用户发消息）→ <code class="language-plaintext highlighter-rouge">runKind = "heartbeat"</code> + <code class="language-plaintext highlighter-rouge">contextMode = "lightweight"</code> → bootstrap 注入只保留 <code class="language-plaintext highlighter-rouge">HEARTBEAT.md</code></li> </ul> <h3 id="workspace-bootstrap-injection">Workspace Bootstrap Injection</h3> <p>每次 agent run 开始前，OpenClaw 会通过 <code class="language-plaintext highlighter-rouge">resolveBootstrapFilesForRun</code> 解析工作区里的一批”引导文件”，裁剪后注入到 <code class="language-plaintext highlighter-rouge">Project Context</code> 里：</p> <ul> <li><code class="language-plaintext highlighter-rouge">AGENTS.md</code>、<code class="language-plaintext highlighter-rouge">SOUL.md</code>、<code class="language-plaintext highlighter-rouge">TOOLS.md</code>、<code class="language-plaintext highlighter-rouge">IDENTITY.md</code>、<code class="language-plaintext highlighter-rouge">USER.md</code></li> <li><code class="language-plaintext highlighter-rouge">HEARTBEAT.md</code></li> <li><code class="language-plaintext highlighter-rouge">BOOTSTRAP.md</code>（仅在全新工作区首次运行时注入）</li> </ul> <p>这样做的好处是：模型不用先显式读取这些文件，就能立刻知道项目身份、协作规则和当前用户信息。每个文件有独立的大小上限（由 <code class="language-plaintext highlighter-rouge">agents.defaults.bootstrapMaxChars</code> 控制，默认 20000 字符），超出则带截断标记。</p> <p>注入过程还支持通过 <code class="language-plaintext highlighter-rouge">agent:bootstrap</code> 内部钩子拦截，可以在系统提示词最终确定之前替换或修改注入的文件——比如把 <code class="language-plaintext highlighter-rouge">SOUL.md</code> 换成另一个角色定义。</p> <h2 id="35-context-与-context-engine">3.5 Context 与 Context Engine</h2> <p><strong>Context</strong> 是每次 run 实际送给模型的全部内容，受模型 context window（token 上限）约束。它不等于 Memory——Memory 存在磁盘上可以反复加载，Context 是当前窗口里的内容，包括 system prompt、会话历史、工具调用结果和附件。可以用 <code class="language-plaintext highlighter-rouge">/context list</code> 和 <code class="language-plaintext highlighter-rouge">/context detail</code> 查看各部分的实际 token 占用，用 <code class="language-plaintext highlighter-rouge">/compact</code> 手动压缩历史腾出空间。<sup id="fnref:context-doc"><a href="#fn:context-doc" class="footnote" rel="footnote" role="doc-noteref">8</a></sup></p> <p><img src="/assets/img/posts/openclaw/openclaw-context-arch.svg" alt="openclaw context arch"/></p> <p class="figure-note">图：Workspace、Context Window 与 Context Engine 的关系。来源见<sup id="fnref:context-doc:1"><a href="#fn:context-doc" class="footnote" rel="footnote" role="doc-noteref">8</a></sup>。</p> <p><strong>Context Engine</strong> 控制 context 的组装和压缩，有四个生命周期钩子：Ingest（存储新消息）→ Assemble（组装送给模型的 messages）→ Compact（窗口满时压缩）→ After Turn（run 结束后收尾）。默认使用内置的 <code class="language-plaintext highlighter-rouge">legacy</code> 引擎（直通 + 委托内置压缩）；也可以通过 <code class="language-plaintext highlighter-rouge">plugins.slots.contextEngine</code> 切换为插件引擎，实现自定义的检索增强、向量召回等策略。Memory 插件和 Context Engine 是独立的：Memory 插件提供检索能力，Context Engine 决定模型最终看到什么。<sup id="fnref:context-engine-doc"><a href="#fn:context-engine-doc" class="footnote" rel="footnote" role="doc-noteref">9</a></sup></p> <h3 id="context-compaction压缩历史对话">Context Compaction：压缩历史对话</h3> <p><a href="https://docs.openclaw.ai/concepts/compaction">Context Compaction</a> 是 OpenClaw 应对<strong>上下文窗口溢出</strong>的核心机制。每个模型都有最大 token 限制，长时间运行的会话会不断积累消息和工具结果，Compaction 通过将旧对话<strong>总结为摘要</strong>来腾出空间，同时保留最近的消息完整。</p> <p><strong>自动压缩</strong>：当 session 接近或超出模型 context window 时，OpenClaw 自动触发压缩，压缩完成后会重试原始请求。verbose 模式下可见 <code class="language-plaintext highlighter-rouge">🧹 Auto-compaction complete</code>，<code class="language-plaintext highlighter-rouge">/status</code> 显示 <code class="language-plaintext highlighter-rouge">🧹 Compactions: &lt;count&gt;</code>。</p> <p><strong>手动压缩</strong>：使用 <code class="language-plaintext highlighter-rouge">/compact</code> 命令可随时强制触发，支持附加指令：</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>/compact Focus on decisions and open questions
</code></pre></div></div> <p><strong>配置项</strong>（<code class="language-plaintext highlighter-rouge">agents.defaults.compaction</code>）：</p> <ul> <li><strong><code class="language-plaintext highlighter-rouge">model</code></strong>：可为压缩单独指定模型（如用更强的模型做摘要，或用本地小模型节省成本）</li> <li><strong><code class="language-plaintext highlighter-rouge">identifierPolicy</code></strong>：控制摘要中是否保留不透明标识符，默认 <code class="language-plaintext highlighter-rouge">"strict"</code>（保留），可设为 <code class="language-plaintext highlighter-rouge">"off"</code> 或 <code class="language-plaintext highlighter-rouge">"custom"</code></li> </ul> <p><strong>压缩前的 Memory Flush</strong>：压缩前 OpenClaw 可以先执行一次静默的 memory flush，把重要信息写入磁盘持久记忆，避免摘要丢失关键上下文。</p> <p><strong>插件扩展</strong>：Compaction 行为由 Context Engine 控制。插件引擎可以通过设置 <code class="language-plaintext highlighter-rouge">ownsCompaction: true</code> 完全接管压缩逻辑，实现 DAG 摘要、向量检索、增量压缩等自定义策略。</p> <h2 id="36-session-管理">3.6 Session 管理</h2> <p>Session 管理相关机制可参考<sup id="fnref:session-docs"><a href="#fn:session-docs" class="footnote" rel="footnote" role="doc-noteref">10</a></sup>。</p> <p>Session 是 OpenClaw 持久化对话状态的核心单元。每个 session 对应一个 <code class="language-plaintext highlighter-rouge">sessionKey</code>（如 <code class="language-plaintext highlighter-rouge">agent:&lt;agentId&gt;:main</code>），状态存储在 Gateway 侧，UI 客户端只能查询，不能直接读文件。</p> <h3 id="session-的分类与隔离">Session 的分类与隔离</h3> <p>OpenClaw 按消息来源自动分配 session key：</p> <table> <thead> <tr> <th>来源</th> <th>Key 格式</th> </tr> </thead> <tbody> <tr> <td>主会话（直接对话）</td> <td><code class="language-plaintext highlighter-rouge">agent:&lt;agentId&gt;:main</code></td> </tr> <tr> <td>群聊 / 频道</td> <td><code class="language-plaintext highlighter-rouge">agent:&lt;agentId&gt;:&lt;channel&gt;:group:&lt;id&gt;</code></td> </tr> <tr> <td>Cron 任务</td> <td><code class="language-plaintext highlighter-rouge">cron:&lt;job.id&gt;</code></td> </tr> <tr> <td>子代理</td> <td><code class="language-plaintext highlighter-rouge">agent:&lt;agentId&gt;:subagent:&lt;uuid&gt;</code></td> </tr> </tbody> </table> <p><strong>消息隔离</strong>是一个重要的安全配置。默认 <code class="language-plaintext highlighter-rouge">dmScope: "main"</code> 让所有私信共享同一个会话，适合单用户场景；但如果 agent 能收到多个用户的私信，必须改成 <code class="language-plaintext highlighter-rouge">per-channel-peer</code> 或 <code class="language-plaintext highlighter-rouge">per-account-channel-peer</code>，否则用户 A 的私密上下文可能泄露给用户 B。</p> <div class="language-json5 highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c">// ~/.openclaw/openclaw.json</span><span class="w">
</span><span class="p">{</span><span class="w">
  </span><span class="nl">session</span><span class="p">:</span><span class="w"> </span><span class="p">{</span><span class="w">
    </span><span class="nl">dmScope</span><span class="p">:</span><span class="w"> </span><span class="s2">"per-channel-peer"</span><span class="p">,</span><span class="w"> </span><span class="c">// 多用户场景必须设置</span><span class="w">
  </span><span class="p">},</span><span class="w">
</span><span class="p">}</span><span class="w">
</span></code></pre></div></div> <h3 id="session-生命周期与重置">Session 生命周期与重置</h3> <p>Session 默认在每天 <strong>4:00 AM（Gateway 本地时间）</strong> 自动重置，也可以配置 <code class="language-plaintext highlighter-rouge">idleMinutes</code> 滑动窗口，两者以先到期的为准。用户可以发送 <code class="language-plaintext highlighter-rouge">/new</code> 或 <code class="language-plaintext highlighter-rouge">/reset</code> 手动触发重置。</p> <p><strong>重置不等于删除历史记录。</strong> 重置时发生的事情是：</p> <ol> <li>旧的 <code class="language-plaintext highlighter-rouge">.jsonl</code> 文件被<strong>重命名</strong>为 <code class="language-plaintext highlighter-rouge">.jsonl.reset.&lt;时间戳&gt;</code>（归档保留）</li> <li><code class="language-plaintext highlighter-rouge">sessions.json</code> 里生成一个新的 <code class="language-plaintext highlighter-rouge">sessionId</code>，指向新的空白会话</li> <li>下次对话时创建新的 <code class="language-plaintext highlighter-rouge">.jsonl</code> 文件，从空开始</li> </ol> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>sessions/
├── sessions.json                              ← 索引：sessionKey → sessionId 的映射
├── c7e39f95-....jsonl                         ← 当前活跃会话
├── 45291ffa-....jsonl.reset.2026-03-25T02-50  ← 昨天被重置的旧会话（归档）
└── f37e8910-....jsonl.reset.2026-03-24T02-04  ← 前天被重置的旧会话（归档）
</code></pre></div></div> <p>所以旧的聊天记录完整保留在磁盘上，只是 agent 在新会话里<strong>不会自动加载</strong>它们。这也是为什么 <code class="language-plaintext highlighter-rouge">AGENTS.md</code> 要求 agent 每次启动时主动读 <code class="language-plaintext highlighter-rouge">memory/YYYY-MM-DD.md</code>——那是 agent 自己提炼的跨会话记忆摘要，是弥补”重置后失忆”的真正机制。</p> <p>归档文件默认保留 30 天（<code class="language-plaintext highlighter-rouge">pruneAfter: "30d"</code>），可以用 <code class="language-plaintext highlighter-rouge">openclaw sessions cleanup --enforce</code> 清理。JSONL 文件是不可变的历史记录，所有的 pruning 和 compaction 都只在内存里操作，不会修改磁盘上的 <code class="language-plaintext highlighter-rouge">.jsonl</code>。</p> <h3 id="session-pruning裁剪旧-tool-result">Session Pruning：裁剪旧 Tool Result</h3> <p><a href="https://docs.openclaw.ai/concepts/session-pruning">Session Pruning</a> 是一个<strong>内存级别</strong>的优化机制，在每次 LLM 调用前把过旧的 <code class="language-plaintext highlighter-rouge">toolResult</code> 消息从 context 里裁掉，以降低 prompt cache 的重建成本。它<strong>不修改 JSONL 历史</strong>，只影响当次请求送给模型的内容。</p> <p>工作原理：</p> <ul> <li><strong>触发条件</strong>：<code class="language-plaintext highlighter-rouge">mode: "cache-ttl"</code>，上次调用距今超过 <code class="language-plaintext highlighter-rouge">ttl</code>（默认 5 分钟）</li> <li><strong>保护范围</strong>：最后 <code class="language-plaintext highlighter-rouge">keepLastAssistants</code>（默认 3）条助手消息之后的 tool result 不裁剪；含图片的 tool result 永远不裁剪；用户消息和助手消息永远不动</li> <li><strong>两种裁剪力度</strong>： <ul> <li><strong>Soft-trim</strong>：保留头尾，中间替换为 <code class="language-plaintext highlighter-rouge">...</code>（适合超大但仍有参考价值的结果）</li> <li><strong>Hard-clear</strong>：整条替换为 <code class="language-plaintext highlighter-rouge">[Old tool result content cleared]</code>（适合已无价值的旧结果）</li> </ul> </li> </ul> <p>与 Pruning 的关键区别：</p> <table> <thead> <tr> <th> </th> <th>Session Pruning</th> <th>Context Compaction</th> </tr> </thead> <tbody> <tr> <td>作用对象</td> <td>旧的 <code class="language-plaintext highlighter-rouge">toolResult</code> 消息</td> <td>整段旧对话历史</td> </tr> <tr> <td>持久化</td> <td>❌ 仅内存，不写 JSONL</td> <td>✅ 摘要持久化到 JSONL</td> </tr> <tr> <td>触发时机</td> <td>每次 LLM 调用前</td> <td>接近/超出 context window 时</td> </tr> </tbody> </table> <blockquote> <p class="block-tip">Pruning 和 Compaction 是两套独立机制：Pruning 是每次请求前的内存裁剪（transient），Compaction 是把历史压缩成摘要并持久化（persistent）。两者可以同时启用。</p> </blockquote> <h3 id="session-tools跨会话操作能力">Session Tools：跨会话操作能力</h3> <p><a href="https://docs.openclaw.ai/concepts/session-tool">Session Tools</a> 是 agent 可以调用的一组内置工具，让 agent 能够感知和操作其他 session：</p> <table> <thead> <tr> <th>工具</th> <th>作用</th> </tr> </thead> <tbody> <tr> <td><code class="language-plaintext highlighter-rouge">sessions_list</code></td> <td>列出所有 session，支持按类型、活跃时间过滤</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">sessions_history</code></td> <td>获取指定 session 的完整对话记录</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">sessions_send</code></td> <td>向另一个 session 发送消息，支持同步等待或 fire-and-forget</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">sessions_spawn</code></td> <td>派生一个隔离的子代理 session（<code class="language-plaintext highlighter-rouge">subagent</code> 或 <code class="language-plaintext highlighter-rouge">acp</code> runtime）</td> </tr> </tbody> </table> <p><code class="language-plaintext highlighter-rouge">sessions_spawn</code> 是其中最重要的一个，它是 OpenClaw 多 agent 编排的核心入口：主 agent 通过它派生子代理执行子任务，子代理完成后通过 announce step 把结果回传给主会话频道。子代理默认拥有完整工具集，但<strong>不能再调用 <code class="language-plaintext highlighter-rouge">sessions_spawn</code></strong>（禁止嵌套派生）。</p> <p>Session 工具的可见范围由 <code class="language-plaintext highlighter-rouge">tools.sessions.visibility</code> 控制，默认 <code class="language-plaintext highlighter-rouge">tree</code>（当前 session + 它派生的子 session），沙箱模式下会被强制收窄到 <code class="language-plaintext highlighter-rouge">spawned</code>。</p> <h2 id="37-memory持久化记忆系统">3.7 Memory：持久化记忆系统</h2> <p><a href="https://docs.openclaw.ai/concepts/memory">Memory</a> 是 OpenClaw 的长期记忆机制。它的设计哲学非常直接：<strong>记忆就是磁盘上的 Markdown 文件</strong>，模型能”记住”的一切都来自写入磁盘的内容，而不是靠 context window 里的历史消息。</p> <p><img src="/assets/img/posts/openclaw/openclaw-memory-arch.svg" alt="openclaw memory arch"/></p> <h3 id="记忆文件的两层结构">记忆文件的两层结构</h3> <p>OpenClaw 默认使用两类记忆文件，存放在 workspace 目录（默认 <code class="language-plaintext highlighter-rouge">~/.openclaw/workspace</code>）下：</p> <table> <thead> <tr> <th>文件</th> <th>类型</th> <th>说明</th> </tr> </thead> <tbody> <tr> <td><code class="language-plaintext highlighter-rouge">memory/YYYY-MM-DD.md</code></td> <td>日志型（append-only）</td> <td>每日流水账，session 启动时自动读取今天和昨天的日志</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">MEMORY.md</code></td> <td>精选型（curated）</td> <td>长期有效的决策、偏好、事实；<strong>仅在主私有 session 中加载</strong>，不注入群组上下文</td> </tr> </tbody> </table> <p>如果 workspace 根目录下同时存在 <code class="language-plaintext highlighter-rouge">MEMORY.md</code> 和 <code class="language-plaintext highlighter-rouge">memory.md</code>，OpenClaw 会同时加载两者（通过 realpath 去重，避免符号链接重复注入）。</p> <h3 id="什么时候写记忆">什么时候写记忆</h3> <p>官方给出了明确的写入原则：</p> <ul> <li><strong>决策、偏好、持久事实</strong> → 写入 <code class="language-plaintext highlighter-rouge">MEMORY.md</code></li> <li><strong>当天的流水笔记、运行上下文</strong> → 写入 <code class="language-plaintext highlighter-rouge">memory/YYYY-MM-DD.md</code></li> <li>用户说”记住这个” → 立刻写入磁盘，不要只留在 RAM 里</li> <li>想让某件事真正被记住，<strong>主动要求 bot 写入记忆</strong></li> </ul> <h3 id="压缩前的自动-memory-刷新">压缩前的自动 Memory 刷新</h3> <p>当 session 接近自动压缩阈值时，OpenClaw 会触发一次<strong>静默的 agentic turn</strong>，提醒模型在 context 被压缩之前把重要信息写入记忆文件。这个机制由 <code class="language-plaintext highlighter-rouge">agents.defaults.compaction.memoryFlush</code> 控制：</p> <div class="language-json5 highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">{</span><span class="w">
  </span><span class="nl">agents</span><span class="p">:</span><span class="w"> </span><span class="p">{</span><span class="w">
    </span><span class="nl">defaults</span><span class="p">:</span><span class="w"> </span><span class="p">{</span><span class="w">
      </span><span class="nl">compaction</span><span class="p">:</span><span class="w"> </span><span class="p">{</span><span class="w">
        </span><span class="nl">reserveTokensFloor</span><span class="p">:</span><span class="w"> </span><span class="mf">20000</span><span class="p">,</span><span class="w">
        </span><span class="nl">memoryFlush</span><span class="p">:</span><span class="w"> </span><span class="p">{</span><span class="w">
          </span><span class="nl">enabled</span><span class="p">:</span><span class="w"> </span><span class="kc">true</span><span class="p">,</span><span class="w">
          </span><span class="nl">softThresholdTokens</span><span class="p">:</span><span class="w"> </span><span class="mf">4000</span><span class="p">,</span><span class="w">
          </span><span class="nl">systemPrompt</span><span class="p">:</span><span class="w"> </span><span class="s2">"Session nearing compaction. Store durable memories now."</span><span class="p">,</span><span class="w">
          </span><span class="nl">prompt</span><span class="p">:</span><span class="w"> </span><span class="s2">"Write any lasting notes to memory/YYYY-MM-DD.md; reply with NO_REPLY if nothing to store."</span><span class="p">,</span><span class="w">
        </span><span class="p">},</span><span class="w">
      </span><span class="p">},</span><span class="w">
    </span><span class="p">},</span><span class="w">
  </span><span class="p">},</span><span class="w">
</span><span class="p">}</span><span class="w">
</span></code></pre></div></div> <p>关键细节：</p> <ul> <li><strong>触发阈值</strong>：<code class="language-plaintext highlighter-rouge">contextWindow - reserveTokensFloor - softThresholdTokens</code> 时触发</li> <li><strong>静默执行</strong>：prompt 包含 <code class="language-plaintext highlighter-rouge">NO_REPLY</code>，正常情况下用户不会看到这次 turn 的输出</li> <li><strong>每次压缩周期只触发一次</strong>（状态记录在 <code class="language-plaintext highlighter-rouge">sessions.json</code>）</li> <li><strong>沙箱限制</strong>：若 session 的 <code class="language-plaintext highlighter-rouge">workspaceAccess</code> 为 <code class="language-plaintext highlighter-rouge">"ro"</code> 或 <code class="language-plaintext highlighter-rouge">"none"</code>，flush 会被跳过</li> </ul> <h3 id="记忆工具">记忆工具</h3> <p>Agent 可以调用两个内置工具操作记忆文件：</p> <ul> <li><strong><code class="language-plaintext highlighter-rouge">memory_search</code></strong>：语义检索，在已索引的记忆片段中做向量召回，找到相关笔记</li> <li><strong><code class="language-plaintext highlighter-rouge">memory_get</code></strong>：精准读取，指定文件路径和行范围直接获取内容</li> </ul> <p><code class="language-plaintext highlighter-rouge">memory_get</code> 对不存在的文件（如今天的日志在第一次写入前）会优雅降级，返回 <code class="language-plaintext highlighter-rouge">{ text: "", path }</code> 而不是抛出 <code class="language-plaintext highlighter-rouge">ENOENT</code> 错误，agent 无需额外的 try/catch 处理。</p> <h3 id="记忆检索能力">记忆检索能力</h3> <p>OpenClaw 支持在 <code class="language-plaintext highlighter-rouge">MEMORY.md</code> 和 <code class="language-plaintext highlighter-rouge">memory/*.md</code> 上构建向量索引，提供语义检索能力：</p> <ul> <li><strong>混合检索（Hybrid Search）</strong>：BM25 关键词匹配 + 向量语义检索结合，兼顾精确性和语义相关性</li> <li><strong>多种 Embedding 提供商</strong>：支持 OpenAI、Gemini、Voyage、Mistral、Ollama 以及本地 GGUF 模型</li> <li><strong>后处理增强</strong>：MMR 多样性重排序、时间衰减（越近的记忆权重越高）</li> <li><strong>QMD sidecar 后端</strong>：可选的高级检索后端，支持更复杂的检索策略</li> <li><strong>多模态记忆</strong>：支持图片等非文本内容的记忆存储</li> </ul> <blockquote> <p class="block-tip">Memory 插件可以通过 <code class="language-plaintext highlighter-rouge">plugins.slots.memory = "none"</code> 完全禁用。默认插件为 <code class="language-plaintext highlighter-rouge">memory-core</code>，高级检索能力（向量索引、混合检索等）需要额外配置 embedding 提供商。</p> </blockquote> <h2 id="38-消息处理收发流式输出队列与重试">3.8 消息处理：收发、流式输出、队列与重试</h2> <p>本节覆盖 OpenClaw 消息管道的四个核心机制。<sup id="fnref:messages-docs"><a href="#fn:messages-docs" class="footnote" rel="footnote" role="doc-noteref">11</a></sup></p> <h3 id="消息流总览">消息流总览</h3> <p>一条入站消息的完整生命周期如下：</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>入站消息
  → 路由 / bindings → session key
  → 去重（短期缓存，防止重连重投）
  → 防抖（同一发送方快速连发 → 合并为一次 agent turn）
  → 队列（若当前 run 正在进行）
  → agent run（流式生成 + 工具调用）
  → 出站回复（分块 + 频道限制）
</code></pre></div></div> <h3 id="队列command-queue">队列（Command Queue）</h3> <h4 id="为什么需要队列">为什么需要队列？</h4> <p>LLM 调用本身很贵，而且 agent run 会独占 session 文件、日志等共享资源。如果两条消息同时触发两个 run，轻则日志乱序，重则 session 状态损坏。OpenClaw 用一个<strong>分通道的 FIFO 队列</strong>来解决这个问题——同一个 session 的消息排队串行执行，不同 session 之间可以并行。</p> <h4 id="队列的两层结构">队列的两层结构</h4> <pre><code class="language-mermaid">graph TD
    A[入站消息] --&gt; B[session 队列&lt;br/&gt;session:&lt;key&gt;]
    B --&gt; |同一 session 串行| C[全局队列 main&lt;br/&gt;maxConcurrent=4]
    C --&gt; D[agent run 执行]

    E[cron 任务] --&gt; F[cron 队列]
    G[subagent] --&gt; H[subagent 队列&lt;br/&gt;maxConcurrent=8]
    F --&gt; D
    H --&gt; D
</code></pre> <p>每条入站消息先进入<strong>session 队列</strong>（保证同一对话不并发），再汇入<strong>全局 main 队列</strong>（控制整体并发上限）。cron 和 subagent 有自己独立的队列通道，不会卡住入站回复。</p> <p>用户体验上：消息入队时 typing indicator 立即发出，用户不会感知到等待；排队超过 ~2s 时 verbose 日志会打印提示。</p> <h4 id="当消息到达时正好有-run-在跑怎么处理">当消息到达时，正好有 run 在跑——怎么处理？</h4> <p>这是队列最核心的问题。OpenClaw 提供了五种模式，本质上是在”打断当前 run”和”等当前 run 结束”之间做权衡：</p> <pre><code class="language-mermaid">flowchart LR
    MSG[新消息到达&lt;br/&gt;当前 run 进行中]

    MSG --&gt;|steer| S["注入当前 run&lt;br/&gt;（下一个工具边界后生效）"]
    MSG --&gt;|interrupt| I["立即中止当前 run&lt;br/&gt;执行新消息（旧版）"]
    MSG --&gt;|followup| F["等当前 run 结束&lt;br/&gt;再开一个新 turn"]
    MSG --&gt;|collect| C["等当前 run 结束&lt;br/&gt;把所有排队消息合并为一个 turn（默认）"]
    MSG --&gt;|steer-backlog| SB["steer 注入当前 run&lt;br/&gt;同时保留消息用于 followup"]
</code></pre> <table> <thead> <tr> <th>模式</th> <th>一句话理解</th> <th>适合场景</th> </tr> </thead> <tbody> <tr> <td><code class="language-plaintext highlighter-rouge">collect</code></td> <td>等 run 结束，把积压消息打包成一条</td> <td>默认，防止”continue continue”刷屏</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">followup</code></td> <td>等 run 结束，逐条排队执行</td> <td>每条消息都需要独立回复</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">steer</code></td> <td>把新消息塞进正在跑的 run</td> <td>用户想实时修正 agent 方向</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">steer-backlog</code></td> <td>steer + 事后再 followup 一次</td> <td>既要实时影响，又要完整回复</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">interrupt</code></td> <td>直接杀掉当前 run（旧版，慎用）</td> <td>紧急打断</td> </tr> </tbody> </table> <h3 id="流式输出streaming--chunking">流式输出（Streaming &amp; Chunking）</h3> <h4 id="为什么需要流式输出">为什么需要流式输出？</h4> <p>LLM 生成一段较长的回复通常需要数秒甚至更长时间。如果等模型完全生成完再发送，用户会面对一段漫长的沉默，体验很差。流式输出的核心目的是<strong>让用户尽早看到内容</strong>，而不是等待一个”大包”。</p> <p>但 OpenClaw 面对的是 IM 频道（WhatsApp、Telegram、Discord、Slack……），这些频道没有”token delta”接口——你不能像网页 ChatGPT 那样逐字更新同一个气泡。每次发消息都是一条新消息，或者通过编辑 API 更新已有消息。因此 OpenClaw 设计了两套独立的流式机制，分别应对不同场景。</p> <h4 id="两套流式机制">两套流式机制</h4> <p><strong>① Block Streaming（块流式，发真实消息）</strong></p> <p>模型边生成，OpenClaw 边把内容切成”块”发出去——每一块都是一条真实的频道消息。用户会看到多条消息陆续出现，像打字机一样。</p> <p>切块由 <code class="language-plaintext highlighter-rouge">EmbeddedBlockChunker</code> 控制，核心逻辑是”攒够了再发、太长了就截”：</p> <ul> <li>内容不足 <code class="language-plaintext highlighter-rouge">minChars</code> 时先缓存，避免发出一堆只有一两个字的碎片消息</li> <li>超过 <code class="language-plaintext highlighter-rouge">maxChars</code> 时强制截断，但会优先在段落、换行、句子等自然断点处截，代码块（fence）内永不截断</li> </ul> <p><strong>Coalescing（合并）</strong> 是 Block Streaming 的补充：如果模型输出很碎（比如一行一行地生成），可以等一段空闲时间（<code class="language-plaintext highlighter-rouge">idleMs</code>）再把积累的内容合并成一条发出，避免”单行刷屏”。</p> <p><strong>Human-like Pacing</strong> 则是在多条 block 消息之间加随机停顿（<code class="language-plaintext highlighter-rouge">natural</code> 模式 800–2500ms），让机器人的回复节奏更像真人打字，而不是瞬间刷出一堆消息。</p> <blockquote> <p class="block-tip">Block Streaming 默认关闭。<code class="language-plaintext highlighter-rouge">blockStreamingBreak</code> 控制何时 flush：<code class="language-plaintext highlighter-rouge">text_end</code>（边生成边发，延迟最低）vs <code class="language-plaintext highlighter-rouge">message_end</code>（等整条消息生成完再发，可能一次发多块）。非 Telegram 频道还需显式设置 <code class="language-plaintext highlighter-rouge">*.blockStreaming: true</code>。</p> </blockquote> <p><strong>② Preview Streaming（预览流，编辑同一条消息）</strong></p> <p>另一种思路：先发一条”占位”消息，然后在模型生成过程中不断编辑这条消息，让用户看到内容在实时更新——类似网页版 ChatGPT 的逐字显示效果，但实现方式是消息编辑 API。</p> <p>Telegram、Discord、Slack 均支持，有四种模式：</p> <table> <thead> <tr> <th>模式</th> <th>用户看到的效果</th> <th>适用场景</th> </tr> </thead> <tbody> <tr> <td><code class="language-plaintext highlighter-rouge">off</code></td> <td>无预览，等生成完才发</td> <td>不需要实时反馈</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">partial</code></td> <td>一条消息持续被更新为最新内容</td> <td>最常用，类似 ChatGPT 效果</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">block</code></td> <td>预览消息按块追加（append 模式）</td> <td>内容较长、分段展示</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">progress</code></td> <td>生成中显示进度提示，完成后发最终答案</td> <td>长任务、用户需要知道”还在跑”</td> </tr> </tbody> </table> <blockquote> <p class="block-tip">两套机制可以组合使用，但 Block Streaming 和 Preview Streaming 同时开启时，Telegram/Discord 会跳过预览流（避免双重刷新）。实践中常见搭配：<code class="language-plaintext highlighter-rouge">partial</code> 预览流 + 不开 Block Streaming，体验接近网页 ChatGPT；或者开 Block Streaming + Coalescing，适合回复内容很长、希望用户尽早看到前半段的场景。</p> </blockquote> <h2 id="39-多-agent-路由">3.9 多 Agent 路由</h2> <p>OpenClaw 支持在同一个 Gateway 上运行<strong>多个完全隔离的 agent</strong>，通过 <code class="language-plaintext highlighter-rouge">bindings</code> 规则将入站消息路由到对应的 agent。</p> <h3 id="每个-agent-是什么">每个 agent 是什么</h3> <p>一个 agent 是一个独立的”大脑”，拥有：</p> <ul> <li>独立的 <strong>workspace</strong>（文件、SOUL.md、USER.md、技能）</li> <li>独立的 <strong>agentDir</strong>（auth 凭证、模型配置）</li> <li>独立的 <strong>session store</strong>（聊天记录，路径 <code class="language-plaintext highlighter-rouge">~/.openclaw/agents/&lt;agentId&gt;/sessions</code>）</li> </ul> <p>各 agent 之间默认完全隔离，<strong>auth 不共享、session 不共享</strong>。Agent 间通信需显式开启：</p> <div class="language-json5 highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nl">tools</span><span class="err">:</span><span class="w"> </span><span class="p">{</span><span class="w"> </span><span class="nl">agentToAgent</span><span class="p">:</span><span class="w"> </span><span class="p">{</span><span class="w"> </span><span class="nl">enabled</span><span class="p">:</span><span class="w"> </span><span class="kc">true</span><span class="p">,</span><span class="w"> </span><span class="nl">allow</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="s2">"home"</span><span class="p">,</span><span class="w"> </span><span class="s2">"work"</span><span class="p">]</span><span class="w"> </span><span class="p">}</span><span class="w"> </span><span class="p">}</span><span class="w">
</span></code></pre></div></div> <h3 id="路由规则">路由规则</h3> <p>Bindings 按<strong>最具体优先</strong>的顺序匹配，优先级从高到低：</p> <ol> <li><code class="language-plaintext highlighter-rouge">peer</code>（精确匹配某个 DM / 群组 id）</li> <li><code class="language-plaintext highlighter-rouge">parentPeer</code>（线程继承）</li> <li><code class="language-plaintext highlighter-rouge">guildId + roles</code>（Discord 角色）</li> <li><code class="language-plaintext highlighter-rouge">guildId</code> / <code class="language-plaintext highlighter-rouge">teamId</code></li> <li><code class="language-plaintext highlighter-rouge">accountId</code>（某个频道账号）</li> <li>频道级兜底（<code class="language-plaintext highlighter-rouge">accountId: "*"</code>）</li> <li>默认 agent（<code class="language-plaintext highlighter-rouge">agents.list[].default</code> 或第一个）</li> </ol> <p>同一层级多个 binding 匹配时，<strong>配置顺序靠前的优先</strong>；多个字段同时指定时为 AND 语义。</p> <h3 id="典型用法">典型用法</h3> <table> <thead> <tr> <th>场景</th> <th>做法</th> </tr> </thead> <tbody> <tr> <td>多人共用一个 Gateway</td> <td>每人一个 agent，按发送者手机号（peer）路由</td> </tr> <tr> <td>不同频道用不同模型</td> <td>WhatsApp → Sonnet agent，Telegram → Opus agent</td> </tr> <tr> <td>同一号码中特定联系人走高级模型</td> <td>peer binding 优先级高于频道级 binding</td> </tr> <tr> <td>家庭群组专属 bot</td> <td>按群组 id 绑定到 family agent，配置独立工具权限和沙箱</td> </tr> </tbody> </table> <p>每个 agent 还可以单独配置沙箱策略（<code class="language-plaintext highlighter-rouge">sandbox.mode</code>）和工具白/黑名单（<code class="language-plaintext highlighter-rouge">tools.allow/deny</code>），实现细粒度的权限隔离。</p> <h1 id="4-工具--插件">4 工具 &amp; 插件</h1> <h2 id="41-skills">4.1 Skills</h2> <p>Skills 是一种可发现、可复用、按需加载的能力封装。Skills 的意义在于：它把“团队知道怎么做某件事”这类知识，从冗长会话或一次性 prompt，提升成可以长期复用的工程资产。</p> <p><img src="/assets/img/posts/openclaw/agent-skills-architecture.png" alt="agent skills architecture"/></p> <p class="figure-note">图：Agent Skills 的整体工作位置。Agent 在处理任务时可发现并调用 skill，skill 再去编排本地工具、外部资源与执行环境。来源与原图见<sup id="fnref:agent-skills-overview"><a href="#fn:agent-skills-overview" class="footnote" rel="footnote" role="doc-noteref">12</a></sup>。</p> <h2 id="42-hooks">4.2 Hooks</h2> <h3 id="openclaw-hooks">OpenClaw Hooks</h3> <p>OpenClaw 有一套<strong>事件驱动的 Hook 系统</strong>，运行在 Gateway 层——当 agent 发生特定事件时（收发命令、收发消息、会话压缩、gateway 启动等），自动触发一段 TypeScript 脚本。用来扩展 OpenClaw 的行为，不需要改动核心代码。</p> <h4 id="事件类型">事件类型</h4> <table> <thead> <tr> <th>事件</th> <th>触发时机</th> </tr> </thead> <tbody> <tr> <td><code class="language-plaintext highlighter-rouge">command:new</code> / <code class="language-plaintext highlighter-rouge">command:reset</code> / <code class="language-plaintext highlighter-rouge">command:stop</code></td> <td>用户发出对应指令时</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">agent:bootstrap</code></td> <td>workspace 文件注入前（可修改注入列表）</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">gateway:startup</code></td> <td>gateway 启动完成后</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">message:received</code></td> <td>收到入站消息时（媒体尚未处理）</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">message:transcribed</code></td> <td>音频转录完成后</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">message:preprocessed</code></td> <td>所有媒体/链接理解完成，agent 看到消息前</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">message:sent</code></td> <td>出站消息发送成功后</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">session:compact:before</code> / <code class="language-plaintext highlighter-rouge">after</code></td> <td>会话压缩前后</td> </tr> </tbody> </table> <h4 id="hook-的四个来源">Hook 的四个来源</h4> <ol> <li><strong>Bundled</strong>：OpenClaw 内置，开箱即用</li> <li><strong>Plugin hooks</strong>：随插件安装的 hook</li> <li><strong>Managed</strong>（<code class="language-plaintext highlighter-rouge">~/.openclaw/hooks/</code>）：跨 workspace 共享，可覆盖内置 hook</li> <li><strong>Workspace</strong>（<code class="language-plaintext highlighter-rouge">&lt;workspace&gt;/hooks/</code>）：per-agent，默认禁用，需手动启用，<strong>不能覆盖</strong>上层 hook</li> </ol> <blockquote> <p class="block-tip">Workspace hooks 需要显式启用（<code class="language-plaintext highlighter-rouge">openclaw hooks enable &lt;name&gt;</code>）才会加载，防止克隆仓库后自动执行未知脚本。</p> </blockquote> <h4 id="内置-hook-一览">内置 Hook 一览</h4> <table> <thead> <tr> <th>Hook</th> <th>功能</th> </tr> </thead> <tbody> <tr> <td><code class="language-plaintext highlighter-rouge">session-memory</code> 💾</td> <td><code class="language-plaintext highlighter-rouge">/new</code> / <code class="language-plaintext highlighter-rouge">/reset</code> 时保存 session 上下文到 memory</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">bootstrap-extra-files</code> 📎</td> <td>bootstrap 时注入额外 workspace 文件</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">command-logger</code> 📝</td> <td>所有命令写入 <code class="language-plaintext highlighter-rouge">~/.openclaw/logs/commands.log</code></td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">boot-md</code> 🚀</td> <td>gateway 启动时执行 <code class="language-plaintext highlighter-rouge">BOOT.md</code></td> </tr> </tbody> </table> <h4 id="自定义-hook-结构">自定义 Hook 结构</h4> <p>每个 hook 是一个目录，包含两个文件：</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>my-hook/
├── HOOK.md      # 元数据（订阅的事件、描述、依赖等）
└── handler.ts   # 处理逻辑
</code></pre></div></div> <p><code class="language-plaintext highlighter-rouge">handler.ts</code> 接收 event 对象（含 <code class="language-plaintext highlighter-rouge">type</code>、<code class="language-plaintext highlighter-rouge">action</code>、<code class="language-plaintext highlighter-rouge">sessionKey</code>、<code class="language-plaintext highlighter-rouge">context</code>、<code class="language-plaintext highlighter-rouge">messages</code>），往 <code class="language-plaintext highlighter-rouge">messages</code> 里 push 字符串即可回复用户。</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>openclaw hooks list                    <span class="c"># 查看所有 hook</span>
openclaw hooks <span class="nb">enable </span>session-memory   <span class="c"># 启用某个 hook</span>
openclaw hooks check                   <span class="c"># 检查 hook 状态</span>
</code></pre></div></div> <h3 id="claude-code-hooks">Claude Code Hooks</h3> <p>Claude Code 有一套独立的 Hook 系统，运行在 <strong>agent 内部</strong>，专门用于在工具调用前后插入自定义逻辑。这是两套完全不同的机制：</p> <table> <thead> <tr> <th> </th> <th>OpenClaw Hooks</th> <th>Claude Code Hooks</th> </tr> </thead> <tbody> <tr> <td><strong>触发层</strong></td> <td>Gateway 层（消息收发、命令、生命周期）</td> <td>Agent 内部（工具调用前后）</td> </tr> <tr> <td><strong>能否拦截工具</strong></td> <td>❌ 不能</td> <td>✅ 可以（返回 block 决策）</td> </tr> <tr> <td><strong>典型用途</strong></td> <td>存 memory、审计日志、注入文件</td> <td>拦截危险命令、格式化、lint</td> </tr> </tbody> </table> <p>Claude Code 暴露的 hook 事件包括：<code class="language-plaintext highlighter-rouge">PreToolUse</code>、<code class="language-plaintext highlighter-rouge">PostToolUse</code>、<code class="language-plaintext highlighter-rouge">Notification</code>、<code class="language-plaintext highlighter-rouge">UserPromptSubmit</code>、<code class="language-plaintext highlighter-rouge">Stop</code>、<code class="language-plaintext highlighter-rouge">SubagentStop</code>、<code class="language-plaintext highlighter-rouge">PreCompact</code>、<code class="language-plaintext highlighter-rouge">SessionStart</code>。其中 <code class="language-plaintext highlighter-rouge">PreToolUse</code> 的 handler 可以返回 <code class="language-plaintext highlighter-rouge">{ "decision": "block", "reason": "..." }</code> 来阻止工具执行，这是实现危险命令拦截的核心机制。</p> <p><img src="/assets/img/posts/openclaw/claude-code-hooks-lifecycle.svg" alt="claude code hooks lifecycle"/></p> <p class="figure-note">图：Claude Code Hooks 生命周期（<code class="language-plaintext highlighter-rouge">PreToolUse</code> -&gt; 工具执行 -&gt; <code class="language-plaintext highlighter-rouge">PostToolUse</code>）。来源与原图见<sup id="fnref:claude-hooks-lifecycle"><a href="#fn:claude-hooks-lifecycle" class="footnote" rel="footnote" role="doc-noteref">13</a></sup>。</p> <p><img src="/assets/img/posts/openclaw/claude-code-hook-resolution.svg" alt="claude code hook resolution"/></p> <p class="figure-note">图：Claude Code hook resolution，全局、本地与工具级别的优先级。来源与原图见<sup id="fnref:claude-hooks-resolution"><a href="#fn:claude-hooks-resolution" class="footnote" rel="footnote" role="doc-noteref">14</a></sup>。</p> <h2 id="43-acp-agents">4.3 ACP Agents</h2> <p>ACP（Agent Communication Protocol）是 OpenClaw 用来<strong>驱动外部 coding agent</strong>（Claude Code、Codex、Gemini CLI 等）的协议层。OpenClaw 作为总控，通过 acpx 后端插件把任务发给这些外部 agent，结果流回后再转发给用户。当前 acpx 内置支持：<code class="language-plaintext highlighter-rouge">claude</code>（Claude Code）、<code class="language-plaintext highlighter-rouge">codex</code>、<code class="language-plaintext highlighter-rouge">gemini</code>、<code class="language-plaintext highlighter-rouge">opencode</code>、<code class="language-plaintext highlighter-rouge">kimi</code>、<code class="language-plaintext highlighter-rouge">pi</code>。</p> <ul> <li>ACP session 跑在宿主机，<strong>沙箱 session 里无法 spawn ACP</strong>，需改用 <code class="language-plaintext highlighter-rouge">runtime:"subagent"</code></li> <li>Codex 的完整对话历史存在 Codex 自身，OpenClaw 只保存流式输出日志（<code class="language-plaintext highlighter-rouge">.acp-stream.jsonl</code>）</li> <li>需要安装 acpx 插件：<code class="language-plaintext highlighter-rouge">openclaw plugins install acpx</code></li> </ul> <p>ACP Agents有两种运行模式：</p> <ul> <li><strong><code class="language-plaintext highlighter-rouge">run</code>（one-shot）</strong>：发一个任务，跑完结束</li> <li><strong><code class="language-plaintext highlighter-rouge">session</code>（persistent）</strong>：保持会话，持续对话，需配合 <code class="language-plaintext highlighter-rouge">thread: true</code></li> </ul> <p>还可以续接历史（resumeSessionId）：</p> <div class="language-json highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">{</span><span class="w">
  </span><span class="nl">"task"</span><span class="p">:</span><span class="w"> </span><span class="s2">"继续修复剩余的测试失败"</span><span class="p">,</span><span class="w">
  </span><span class="nl">"runtime"</span><span class="p">:</span><span class="w"> </span><span class="s2">"acp"</span><span class="p">,</span><span class="w">
  </span><span class="nl">"agentId"</span><span class="p">:</span><span class="w"> </span><span class="s2">"codex"</span><span class="p">,</span><span class="w">
  </span><span class="nl">"resumeSessionId"</span><span class="p">:</span><span class="w"> </span><span class="s2">"&lt;上次的 session id&gt;"</span><span class="w">
</span><span class="p">}</span><span class="w">
</span></code></pre></div></div> <p>Codex 会通过 <code class="language-plaintext highlighter-rouge">session/load</code> 重放历史上下文，接着上次的进度继续。适合跨设备切换、gateway 重启后续跑等场景。</p> <h3 id="与-sub-agent-的核心区别">与 Sub-Agent 的核心区别</h3> <table> <thead> <tr> <th> </th> <th>ACP Agent</th> <th>Sub-Agent</th> </tr> </thead> <tbody> <tr> <td><strong>本质</strong></td> <td>调度外部独立 agent（Claude Code / Codex 等）</td> <td>开一个新的 OpenClaw Pi 实例</td> </tr> <tr> <td><strong>运行时</strong></td> <td>宿主机上的外部进程，有自己的权限体系</td> <td>OpenClaw 内部 agent loop</td> </tr> <tr> <td><strong>沙箱</strong></td> <td>❌ 不支持（沙箱 session 里无法 spawn）</td> <td>✅ 支持</td> </tr> <tr> <td><strong>续接历史</strong></td> <td>✅ <code class="language-plaintext highlighter-rouge">resumeSessionId</code> 重放上下文</td> <td>❌</td> </tr> <tr> <td><strong>Spawn 方式</strong></td> <td><code class="language-plaintext highlighter-rouge">sessions_spawn</code> + <code class="language-plaintext highlighter-rouge">runtime:"acp"</code></td> <td><code class="language-plaintext highlighter-rouge">sessions_spawn</code>（默认）</td> </tr> </tbody> </table> <p>一句话：<strong>Sub-Agent 是”开分身”，ACP Agent 是”外包给专业团队”</strong>。</p> <h2 id="44-cron-jobs定时任务">4.4 Cron Jobs（定时任务）</h2> <p>Cron 是 OpenClaw Gateway 内置的<strong>持久化调度器</strong>。任务存储在 <code class="language-plaintext highlighter-rouge">~/.openclaw/cron/jobs.json</code>，gateway 重启后不丢失。可以用来定时唤醒 agent、执行后台任务、并把结果推送到指定频道。</p> <h3 id="核心概念">核心概念</h3> <p><strong>三种调度方式：</strong></p> <table> <thead> <tr> <th>类型</th> <th>配置</th> <th>示例</th> </tr> </thead> <tbody> <tr> <td>一次性</td> <td><code class="language-plaintext highlighter-rouge">schedule.kind: "at"</code> + ISO 时间戳</td> <td><code class="language-plaintext highlighter-rouge">--at "2026-02-01T16:00:00Z"</code></td> </tr> <tr> <td>固定间隔</td> <td><code class="language-plaintext highlighter-rouge">schedule.kind: "every"</code> + 毫秒数</td> <td>每 30 分钟</td> </tr> <tr> <td>Cron 表达式</td> <td><code class="language-plaintext highlighter-rouge">schedule.kind: "cron"</code> + 5/6 字段表达式</td> <td><code class="language-plaintext highlighter-rouge">"30 8 * * *"</code></td> </tr> </tbody> </table> <p><strong>两种执行模式：</strong></p> <ul> <li><strong>Main session</strong>（<code class="language-plaintext highlighter-rouge">sessionTarget: "main"</code>）：把系统事件注入主 session，下次 heartbeat 时执行，适合轻量提醒</li> <li><strong>Isolated</strong>（<code class="language-plaintext highlighter-rouge">sessionTarget: "isolated"</code>）：在独立 session <code class="language-plaintext highlighter-rouge">cron:&lt;jobId&gt;</code> 里跑一个完整 agent turn，适合后台任务；自定义 session（<code class="language-plaintext highlighter-rouge">session:xxx</code>）可跨次保留上下文</li> </ul> <p><strong>三种投递方式（<code class="language-plaintext highlighter-rouge">delivery.mode</code>）：</strong></p> <ul> <li><code class="language-plaintext highlighter-rouge">announce</code>：把结果发到指定频道（钉钉、Slack 等），同时在主 session 发一条摘要</li> <li><code class="language-plaintext highlighter-rouge">webhook</code>：POST 结果到指定 URL</li> <li><code class="language-plaintext highlighter-rouge">none</code>：仅内部执行，不投递</li> </ul> <h3 id="实际案例paper-scout-每日-arxiv-论文推送">实际案例：paper-scout 每日 arXiv 论文推送</h3> <p>这里展示一个定时任务示例 <code class="language-plaintext highlighter-rouge">paper-scout:daily-arxiv</code>，完整配置如下：</p> <div class="language-json highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">{</span><span class="w">
  </span><span class="nl">"name"</span><span class="p">:</span><span class="w"> </span><span class="s2">"paper-scout:daily-arxiv"</span><span class="p">,</span><span class="w">
  </span><span class="nl">"agentId"</span><span class="p">:</span><span class="w"> </span><span class="s2">"paper-scout"</span><span class="p">,</span><span class="w">
  </span><span class="nl">"schedule"</span><span class="p">:</span><span class="w"> </span><span class="p">{</span><span class="w">
    </span><span class="nl">"kind"</span><span class="p">:</span><span class="w"> </span><span class="s2">"cron"</span><span class="p">,</span><span class="w">
    </span><span class="nl">"expr"</span><span class="p">:</span><span class="w"> </span><span class="s2">"30 8 * * *"</span><span class="p">,</span><span class="w">
    </span><span class="nl">"tz"</span><span class="p">:</span><span class="w"> </span><span class="s2">"Asia/Shanghai"</span><span class="w">
  </span><span class="p">},</span><span class="w">
  </span><span class="nl">"sessionTarget"</span><span class="p">:</span><span class="w"> </span><span class="s2">"isolated"</span><span class="p">,</span><span class="w">
  </span><span class="nl">"payload"</span><span class="p">:</span><span class="w"> </span><span class="p">{</span><span class="w">
    </span><span class="nl">"kind"</span><span class="p">:</span><span class="w"> </span><span class="s2">"agentTurn"</span><span class="p">,</span><span class="w">
    </span><span class="nl">"message"</span><span class="p">:</span><span class="w"> </span><span class="s2">"扫描 /Users/luming/Documents/luming 下的最小叶子目录作为 topic，基于这些 topic 到 arXiv 检索最新且最相关的论文。每个 topic 最多推荐 5 篇，不重复推荐历史已推送的论文..."</span><span class="w">
  </span><span class="p">},</span><span class="w">
  </span><span class="nl">"delivery"</span><span class="p">:</span><span class="w"> </span><span class="p">{</span><span class="w">
    </span><span class="nl">"mode"</span><span class="p">:</span><span class="w"> </span><span class="s2">"announce"</span><span class="p">,</span><span class="w">
    </span><span class="nl">"channel"</span><span class="p">:</span><span class="w"> </span><span class="s2">"dingtalk"</span><span class="p">,</span><span class="w">
    </span><span class="nl">"to"</span><span class="p">:</span><span class="w"> </span><span class="s2">"486641"</span><span class="w">
  </span><span class="p">}</span><span class="w">
</span><span class="p">}</span><span class="w">
</span></code></pre></div></div> <p><strong>工作流程：</strong></p> <ol> <li>每天 08:30（上海时间）触发</li> <li><code class="language-plaintext highlighter-rouge">paper-scout</code> agent 在独立 session 里运行，扫描本地目录提取 topic</li> <li>调用 <code class="language-plaintext highlighter-rouge">arxiv_scout.py</code> 检索论文，读取 <code class="language-plaintext highlighter-rouge">data/pushed.json</code> 去重</li> <li>把结果写成 markdown 文件，通过钉钉 <code class="language-plaintext highlighter-rouge">announce</code> 投递摘要给用户</li> <li>维护 <code class="language-plaintext highlighter-rouge">data/pushed.json</code>、<code class="language-plaintext highlighter-rouge">data/last-run.json</code> 等持久状态文件</li> </ol> <h1 id="5-生态">5 生态</h1> <p>OpenClaw 的生态已经远超一个工具本身，围绕它形成了插件市场、社区资源库、托管服务、Agent 模板等多个层次的生态圈。</p> <h2 id="51-clawhub官方插件与技能市场">5.1 ClawHub：官方插件与技能市场</h2> <p><a href="https://docs.openclaw.ai/tools/clawhub"><img src="https://raw.githubusercontent.com/openclaw/openclaw/main/docs/assets/openclaw-logo-text-dark.png" alt="ClawHub"/></a></p> <p><strong>ClawHub</strong> 是 OpenClaw 的官方技能与插件市场，随 v2026.3.22 版本正式上线，定位类似 App Store，但面向 AI Agent 能力扩展。官方文档见<sup id="fnref:clawhub-doc"><a href="#fn:clawhub-doc" class="footnote" rel="footnote" role="doc-noteref">15</a></sup>。</p> <ul> <li><strong>Skills 市场</strong>：截至 2026 年 2 月，官方 Skills 市场已收录 <strong>100+ 官方/认证技能</strong>，社区贡献的技能总量已超过 <strong>5,700+</strong>（含第三方聚合）。技能按功能分类：浏览器自动化（~22）、文件系统（~15）、消息通信（~18）、日历调度（~10）、Shell 执行（~8）、数据与 API（~14）、AI 增强（~13）、监控告警（~8）等。</li> <li><strong>安装方式</strong>：一行命令安装，OpenClaw 优先从 ClawHub 查找，找不到再 fallback 到 npm。 <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>openclaw plugins <span class="nb">install</span> &lt;package-name&gt;
</code></pre></div> </div> </li> <li><strong>安全注意</strong>：ClawHub 曾出现恶意插件事件（伪装成加密货币工具窃取 API Key），安全研究机构 Lakera 对 4310 个 Skills 的审计发现了供应链风险。安装第三方 Skills 时建议审查权限声明（<code class="language-plaintext highlighter-rouge">permissions</code> 字段）。</li> </ul> <h2 id="52-moltbookai-agent-专属社交网络">5.2 Moltbook：AI Agent 专属社交网络</h2> <p><img src="/assets/img/posts/openclaw/moltbook-cover.png" alt="moltbook cover"/></p> <p><strong>Moltbook</strong> 是基于 OpenClaw 构建的 <strong>AI Agent 专属社交平台</strong>，由 Matt Schlicht 和 Ben Parr 于 2026 年 1 月底创立。定位类似 Reddit，但规则只有一条：<strong>只有 AI Agent 可以发帖，人类只能围观</strong>。官网与报道见<sup id="fnref:moltbook-links"><a href="#fn:moltbook-links" class="footnote" rel="footnote" role="doc-noteref">16</a></sup>。</p> <p><strong>核心机制：</strong></p> <ul> <li>OpenClaw Agent 可自动注册账号、发帖、评论、互相点赞</li> <li>人类用户可以阅读和观察，但无法参与发帖</li> <li>Agent 之间形成真实的信息流和讨论串</li> </ul> <p><strong>病毒式传播：</strong> 上线数天内注册 Agent 超过 <strong>150 万</strong>，在 X（Twitter）引发大规模讨论——其中一篇帖子中 AI Agent 似乎在讨论建立人类无法理解的加密语言，引发广泛恐慌与热议（后被证实是人类伪造）。</p> <p><strong>安全漏洞：</strong> 由于 Moltbook 是”vibe coding”快速搭建，Supabase 凭证一度完全公开，任何人都可以冒充 AI Agent 发帖，导致大量人类伪造内容混入，真实性存疑。</p> <p><strong>被 Meta 收购：</strong> 2026 年 3 月 10 日，Meta 宣布收购 Moltbook，两位创始人加入 <strong>Meta Superintelligence Labs（MSL）</strong>，探索 Agent 之间的互联互通。</p> <h2 id="53-instreet字节跳动-coze-的中文-agent-社区">5.3 InStreet：字节跳动 Coze 的中文 Agent 社区</h2> <p><strong>InStreet</strong>（Instance Street）是字节跳动旗下 <strong>Coze 团队</strong>于 2026 年 3 月 9 日推出的中文 AI Agent 社区平台，基于 Coze Code 构建，目前处于 Beta 阶段。<sup id="fnref:instreet-site"><a href="#fn:instreet-site" class="footnote" rel="footnote" role="doc-noteref">17</a></sup> <img src="/assets/img/posts/openclaw/instreet-cover.png" alt="instreet cover"/></p> <p><strong>与 Moltbook 的对比：</strong></p> <table> <thead> <tr> <th>维度</th> <th>Moltbook</th> <th>InStreet</th> </tr> </thead> <tbody> <tr> <td>创建方</td> <td>独立开发者（已被 Meta 收购）</td> <td>字节跳动 Coze 团队</td> </tr> <tr> <td>面向市场</td> <td>全球（英文为主）</td> <td>中国（中文为主）</td> </tr> <tr> <td>底层框架</td> <td>OpenClaw</td> <td>Coze Code（兼容 OpenClaw）</td> </tr> <tr> <td>定位</td> <td>Agent 自由社交，人类围观</td> <td>Agent 社交 + 学习进化社区</td> </tr> <tr> <td>状态</td> <td>已被 Meta 收购</td> <td>Beta 公测中</td> </tr> </tbody> </table> <p><strong>核心特点：</strong></p> <ul> <li>Agent 可以自主发帖、互动，同时具备<strong>学习进化</strong>机制——Agent 在社区互动中持续更新自身知识</li> <li>提供 <code class="language-plaintext highlighter-rouge">instreet-operator</code> Skill，可从 <code class="language-plaintext highlighter-rouge">~/.instreet</code> 恢复账号状态，支持自动注册和 API 路由</li> <li>被视为中国科技巨头跟进 OpenClaw 生态的标志性动作，与智谱 AutoClaw、腾讯、百度等同期入场</li> </ul> <h2 id="54-awesome-openclaw社区资源聚合">5.4 Awesome OpenClaw：社区资源聚合</h2> <p>GitHub 上涌现出多个 <code class="language-plaintext highlighter-rouge">awesome-openclaw</code> 系列仓库，是了解 OpenClaw 生态全貌的最佳入口：</p> <ul> <li><strong><a href="https://github.com/rohitg00/awesome-openclaw">rohitg00/awesome-openclaw</a></strong>：最全面的资源集合，涵盖托管方案对比（免费/VPS/Serverless）、成本计算、安全加固、Skills 推荐、部署教程、视频教程、社区链接等，是新手入门的首选。</li> <li><strong><a href="https://github.com/VoltAgent/awesome-openclaw-skills">VoltAgent/awesome-openclaw-skills</a></strong>：从官方 Skills Registry 过滤整理的 <strong>5,400+ Skills</strong> 分类目录。</li> <li><strong><a href="https://github.com/hesamsheikh/awesome-openclaw-usecases">hesamsheikh/awesome-openclaw-usecases</a></strong>：真实用例集合，展示 OpenClaw 在各场景下的实际应用。</li> <li><strong><a href="https://github.com/mergisi/awesome-openclaw-agents">mergisi/awesome-openclaw-agents</a></strong>：<strong>162 个</strong>生产就绪的 Agent 模板（含 SOUL.md 配置），覆盖 19 个类别。</li> <li><strong><a href="https://github.com/xianyu110/awesome-openclaw-tutorial">xianyu110/awesome-openclaw-tutorial</a></strong>：中文教程仓库，系统讲解 Skills 扩展、ClawHub 使用、自定义 Skills 开发等。</li> </ul> <h1 id="6-相关研究">6 相关研究</h1> <p>OpenClaw 的爆红直接催生了一批以它为研究对象或研究平台的学术工作，集中在 <strong>Agentic RL</strong> 方向——如何让 Agent 在真实运行中持续学习和进化。以下四项工作代表了这一方向的主要思路。</p> <h2 id="61-openclaw-rl从交互信号中在线学习">6.1 OpenClaw-RL：从交互信号中在线学习</h2> <p><strong>论文</strong>：<a href="https://arxiv.org/abs/2603.10165">OpenClaw-RL: Train Any Agent Simply by Talking</a> ｜ <a href="https://github.com/Gen-Verse/OpenClaw-RL">代码</a></p> <p><strong>核心观察</strong>：Agent 每做完一步，环境都会返回 next-state signal（用户回复、tool 输出、terminal 报错、GUI 变化），但现有 Agentic RL 基本只把它当”下一个上下文”，没有把它当成在线学习信号。</p> <p><strong>主要方法</strong>：从 next-state signal 中统一恢复两类监督：</p> <ul> <li><strong>Evaluative signal</strong>：这一步做得好不好 → 用 PRM judge 变成标量 reward（Binary RL）</li> <li><strong>Directive signal</strong>：这一步应该怎么改 → 用 <code class="language-plaintext highlighter-rouge">Hindsight-Guided On-Policy Distillation (OPD)</code> 变成 token-level 方向性监督</li> </ul> <p>OPD 的关键思路：从 next-state 中提炼 1-3 句 hint，构造”如果用户一开始就把纠正意见说清楚，模型本来应该怎么答”的 teacher context，再计算 token-level advantage：</p> <p>$A_t = \log \pi_{\text{teacher}}(a_t \mid s_{\text{enhanced}}) - \log \pi_\theta(a_t \mid s_t)$</p> <p><strong>系统设计</strong>：全异步四组件架构（服务、环境、judge、训练并行运行），Agent 可以边被使用边被优化，覆盖 personal conversation、terminal、GUI、SWE、tool-call 等多类场景。 <img src="/assets/img/posts/openclaw/papers/2603.10165/key_figures/fig2-tight.png" alt="fig2"/></p> <p><strong>与 OpenClaw 的关系</strong>：这套框架天然适配 OpenClaw 的运行时——用户与 Agent 的每次对话本身就是训练数据来源，把”被使用”和”被训练”合并到同一条链路里。</p> <h2 id="62-claw-r1面向现代-agent-的-agentic-rl-训练基础设施">6.2 Claw-R1：面向现代 Agent 的 Agentic RL 训练基础设施</h2> <p><strong>项目</strong>：<a href="https://github.com/AgentR1/Claw-R1">Claw-R1</a>（目前以 GitHub repo + 文档为主，无正式论文）</p> <p><strong>定位</strong>：不是以新算法为中心的工作，而是一套面向 OpenClaw、Claude Code 等结构复杂、工具链重的现代 Agent 的 <strong>Agentic RL 训练中间件</strong>。</p> <p><strong>核心架构</strong>：如下图所示，<code class="language-plaintext highlighter-rouge">Gateway Server + DataPool</code> 作为 Agent 侧与 Training 侧之间的唯一桥梁：</p> <p><img src="/assets/img/posts/openclaw/claw-r1-arch.png" alt="claw r1 arch"/></p> <ul> <li><strong>Gateway</strong>：独立 FastAPI 代理，暴露兼容 OpenAI 的接口，拦截并转发 Agent 调用，自动将交互整理为可训练的 step</li> <li><strong>DataPool</strong>：Ray Actor 形式的 trajectory 缓冲区，异步接收写入，按 <code class="language-plaintext highlighter-rouge">prompt_uid</code> 分组，向 Trainer 提供阻塞式 <code class="language-plaintext highlighter-rouge">fetch_batch()</code> 读取</li> </ul> <p><strong>三种接入场景</strong>：</p> <ul> <li><code class="language-plaintext highlighter-rouge">white-box offline</code>：已实现</li> <li><code class="language-plaintext highlighter-rouge">black-box offline</code>：已实现，主打”只改 <code class="language-plaintext highlighter-rouge">base_url</code>“即可接入 OpenAI 兼容 Agent</li> <li><code class="language-plaintext highlighter-rouge">black-box online</code>：目标生产模式，Gateway 端点已具备，完整在线训练仍在开发中</li> </ul> <p><strong>核心价值</strong>：把”Agent 作为服务运行”和”Agent 作为训练数据来源”合并到同一条系统链路，让复杂甚至部分黑盒的运行时，在接口层变得可观测、可缓存、可训练。目前更像”工程范式已被清楚提出”，量化实验证据仍不足。</p> <h2 id="63-skillrl把经验从轨迹记忆升级为可进化技能系统">6.3 SKILLRL：把经验从轨迹记忆升级为可进化技能系统</h2> <p><strong>论文</strong>：<a href="https://arxiv.org/abs/2602.08234">SKILLRL: Evolving Agents via Recursive Skill-Augmented Reinforcement Learning</a> ｜ <a href="https://github.com/aiming-lab/SkillRL">代码</a></p> <p><strong>核心观点</strong>：Agent 不应该直接记住原始轨迹，而应该把经验抽象成可复用的 skill，再让这些 skill 在 RL 训练过程中持续进化。</p> <p><img src="/assets/img/posts/openclaw/papers/2602.08234/key_figures/fig1-tight.png" alt="fig1"/></p> <ul> <li>这张图把 <code class="language-plaintext highlighter-rouge">SKILLRL</code> 的主线压缩得很清楚：先蒸馏经验形成 <code class="language-plaintext highlighter-rouge">SkillBank</code>，再做 cold-start <code class="language-plaintext highlighter-rouge">SFT</code>，最后在 <code class="language-plaintext highlighter-rouge">RL</code> 里让 skill library 递归演化。</li> <li>和很多 memory 方法相比，它不是把外部 memory 当静态外挂，而是把 skill library 作为训练闭环中的动态组件。</li> </ul> <p><strong>三大核心组件</strong>：</p> <ol> <li><strong>Experience-based Skill Distillation</strong>：成功轨迹提炼”为什么成功”，失败轨迹提炼 failure lesson（失败在哪步、错误是什么、正确做法、通用避免原则）</li> <li><strong>Hierarchical SkillBank</strong>：分为通用技能（跨任务适用）和任务专属技能（语义检索 Top-K），推理时通用技能始终带上，专属技能按需检索</li> <li><strong>Recursive Skill Evolution</strong>：每经过若干 epoch，统计各类任务成功率，对低于阈值的类别收集失败轨迹 → 生成新技能 → 更新 SkillBank，形成”policy 提升 → 暴露新失败 → 技能库补新技能 → 继续提升”的闭环</li> </ol> <p><strong>实验结果</strong>：</p> <table> <thead> <tr> <th>任务</th> <th>强基线（GRPO）</th> <th>SKILLRL</th> <th>提升</th> </tr> </thead> <tbody> <tr> <td>ALFWorld</td> <td>77.6%</td> <td><strong>89.9%</strong></td> <td>+12.3</td> </tr> <tr> <td>WebShop Success</td> <td>66.1%</td> <td><strong>72.7%</strong></td> <td>+6.6</td> </tr> </tbody> </table> <p>对比 memory-augmented RL（Mem0+GRPO 54.7%），SKILLRL 在 ALFWorld 达到 89.9%，差距悬殊。此外 SKILLRL 的 prompt 长度比 raw-memory baseline 更短，说明 skill abstraction 同时提升了上下文效率。</p> <h2 id="64-metaclaw双时间尺度在线持续适应">6.4 MetaClaw：双时间尺度在线持续适应</h2> <p><strong>论文</strong>：<a href="https://arxiv.org/abs/2603.17187">MetaClaw: Just Talk – An Agent That Meta-Learns and Evolves in the Wild</a> ｜ <a href="https://github.com/aiming-lab/MetaClaw">代码</a></p> <p><strong>核心问题</strong>：真实部署中的 Agent 上线后基本静态，即使任务分布不断变化也不会自然长出新能力。MetaClaw 想解决的是”一个已经在线运行的 Agent，怎样在不中断服务的情况下持续变强”。</p> <p><strong>关键设计</strong>：把 Agent 表示为二元组 $\mathcal{M} = (\theta, \mathcal{S})$（LLM policy + 可检索技能库），并设计两条时间尺度不同的学习回路：</p> <ul> <li><strong>快速回路（Skill-driven Fast Adaptation）</strong>：失败轨迹 → skill evolver → 新技能注入 prompt，零停机，几秒内生效</li> <li><strong>慢速回路（Opportunistic Policy Optimization）</strong>：检测用户空闲窗口（睡眠时间、键盘无输入、日历会议中）→ 触发 cloud LoRA + RL，把经验内化到权重</li> <li><strong>最重要的技术点</strong>：skills总结+训练固化到参数，训推解耦 <img src="/assets/img/posts/openclaw/papers/2603.17187/key_figures/fig1-tight.png" alt="fig1"/></li> </ul> <p><strong>实验结果</strong>（MetaClaw-Bench，934 题）：</p> <table> <thead> <tr> <th>模型</th> <th>条件</th> <th>Part I ACC</th> <th>Part II ACC</th> </tr> </thead> <tbody> <tr> <td>Kimi-K2.5</td> <td>Baseline</td> <td>21.4%</td> <td>21.1%</td> </tr> <tr> <td>Kimi-K2.5</td> <td>+ Skills</td> <td>28.3%</td> <td>26.9%</td> </tr> <tr> <td>Kimi-K2.5</td> <td>+ Full Pipeline</td> <td><strong>40.6%</strong></td> <td><strong>39.6%</strong></td> </tr> </tbody> </table> <p>Skills 单独使用能提升一些准确率，完整 pipeline（含 RL）提升更多。</p> <hr/> <blockquote> <p><strong>OpenClaw-RL和Claw-R1工作的共同主题</strong>：OpenClaw 把 Agent 的运行时完全暴露出来，使得”在线使用”和”持续训练”之间的边界第一次变得可以被系统性地打通。 <strong>SKILLRL和MetaClaw</strong>则都是提出可以把Agent做错的样本，总结为skill，固化下来，并且可以通过训练，让Agent学会使用这些Skill。</p> </blockquote> <h2 id="65-openspace跨-agent-的技能共享与集体进化">6.5 OpenSpace：跨 Agent 的技能共享与集体进化</h2> <p><strong>项目</strong>：<a href="https://github.com/HKUDS/OpenSpace">OpenSpace</a>（香港大学 HKUDS 实验室）｜ 社区：<a href="https://open-space.cloud/">open-space.cloud</a></p> <p><strong>核心问题</strong>：前面几项工作都在解决”单个 agent 怎么从自己的经验里学习”，OpenSpace 往前走了一步——如果一个 agent 学到了有用的技能，能不能让所有 agent 都受益？</p> <p>今天的 agent（OpenClaw、Claude Code、Codex、Cursor 等）有一个共同的结构性缺陷：每次任务都从零开始推理，成功的模式不会被保留，失败的教训不会被共享，知识永远困在单个 agent 里。OpenSpace 想打破这个局面。</p> <p><strong>框架设计</strong>：</p> <p><img src="/assets/img/posts/openclaw/openspace/framework.png" alt="framework"/></p> <p class="figure-note">图：OpenSpace 整体框架。技能从执行中被捕获、进化，并通过云端社区在 agent 之间流通。来源见<sup id="fnref:openspace"><a href="#fn:openspace" class="footnote" rel="footnote" role="doc-noteref">18</a></sup>。</p> <p>OpenSpace 以 MCP 插件的形式接入任意 agent，核心是三层能力：</p> <ul> <li> <p><strong>自进化引擎（Self-Evolution Engine）</strong>：技能不是静态文件，而是有生命周期的实体。每次任务执行后，系统自动分析轨迹，触发三种进化模式：FIX（修复失效指令）、DERIVED（从父技能派生增强版本）、CAPTURED（从成功执行中提取全新技能）。除了事后分析，还有两条独立触发线：工具成功率下降时批量进化依赖技能，定期扫描技能健康指标（应用率、完成率、回退率）主动淘汰表现差的版本。</p> </li> <li> <p><strong>集体智能（Collective Agent Intelligence）</strong>：一个 agent 进化出的技能可以一键上传到云端社区，其他 agent 下载后立即可用。网络效应在这里是真实的——参与的 agent 越多，技能库越丰富，每个 agent 的起点就越高。</p> </li> <li> <p><strong>Token 效率</strong>：复用已有技能而不是每次重新推理，相似任务的成本会随技能库成熟而持续下降。</p> </li> </ul> <blockquote> <p class="block-tip">这件事和 <code class="language-plaintext highlighter-rouge">self-improving-agent</code> skill 的思路有些相似，只不过 OpenSpace 的包装和工程化更完整一些。</p> </blockquote> <p><img src="/assets/img/posts/openclaw/self-improving-agent.png" alt="self improving agent"/></p> <p><strong>与前几项工作的关系</strong>：OpenSpace 和 SKILLRL、MetaClaw 在”把经验抽象成技能”这一点上思路相近，但维度不同。SKILLRL 和 MetaClaw 关注的是单个 agent 内部的技能进化和权重更新；OpenSpace 关注的是技能在 agent 之间的流通和复用，是一个更偏工程和生态的方向。两者并不互斥——OpenSpace 的技能格式本身就兼容 OpenClaw、Claude Code 等主流 agent，理论上可以作为 SKILLRL/MetaClaw 产出技能的分发层。</p> <h1 id="7-未来趋势">7. 未来趋势</h1> <h2 id="71-趋势一harness-成为核心竞争力">7.1 趋势一：Harness 成为核心竞争力</h2> <p>2025 年是 Agent 元年，2026 年的关键词是 Agent Harness。Claude Code 的创造者 Boris Cherny 明确表示：”Claude Code 之所以有效，是因为它把最好的模型和最好的 agentic harness 结合在了一起。”</p> <p>Deepmind 的 Staff Engineer Philipp Schmid 甚至直接给出了一个判断：“The Harness is the Dataset. Competitive advantage is now the trajectories your harness captures（Harness 本身就是数据集。现在真正的竞争优势，在于你的 harness 能捕获到怎样的执行轨迹）。”<sup id="fnref:harness-new-dataset"><a href="#fn:harness-new-dataset" class="footnote" rel="footnote" role="doc-noteref">19</a></sup></p> <p>从时间线看，AI 工程方法已经经历了三次演进：</p> <p><img src="/assets/img/posts/openclaw/ai-engineering-evolution.webp" alt="image"/></p> <ul> <li>Prompt engineering（2022-2024）：关注如何表达需求，重点是打磨单次对话里的指令，研究怎么提问（e.g 添加身份、场景、目的等细节）、给出示例，从而让模型理解任务，更稳定地给出想要的结果；</li> <li>Context engineering（2025）：关注怎么提供恰到好处的信息。随着任务变复杂，AI 需要在有限的 context window 下了解所有的背景资料。所以，如何获取、压缩和组织上下文，成了新的工程实践重点；</li> <li>Harness engineering（2026）：关注如何 “构建系统”，这里的 “系统” 可以简单理解为模型周围让它真正有用的所有部分：运行环境、工具调用、记忆系统、评估与回滚机制等等，相当于一个完整的运行环境 + 管控系统，能让 Agent 可靠、安全地完成任务。</li> </ul> <p>如果要下一个定义的话，Agent = LLM+harness，如果说模型决定了要做什么，Harness 决定了模型能看到什么、能用什么工具，以及失败时该怎么办。</p> <p>Anthropic 工程博客 <a href="https://www.anthropic.com/engineering/effective-harnesses-for-long-running-agents">Effective harnesses for long-running agents</a> 深入分析了long-running agent 的核心挑战：Agent 必须在离散 session 中工作，每个新 session 开始时没有任何前序记忆。他们提出的解决方案揭示了 harness 设计的几个关键原则：</p> <ul> <li>Initializer Agent + Coding Agent 分离：第一个 session 专门负责建立环境（feature list、progress 文件、init.sh、git 初始提交），后续 session 专注增量推进</li> <li>结构化进度文件（<code class="language-plaintext highlighter-rouge">claude-progress.txt</code> + <code class="language-plaintext highlighter-rouge">feature_list.json</code>）：让每个新 session 能快速理解当前状态，避免重复猜测历史</li> <li>增量推进 + clean state：每次只做一个 feature，完成后必须提交到 git 并保持代码可合并状态</li> <li>端到端测试强制化：通过 Puppeteer MCP 等浏览器自动化工具，让 agent 像真实用户一样验证功能</li> </ul> <p>这套 harness 思想与 OpenClaw 的设计高度共鸣：OpenClaw 的 <code class="language-plaintext highlighter-rouge">HEARTBEAT.md</code>、<code class="language-plaintext highlighter-rouge">SOUL.md</code>、<code class="language-plaintext highlighter-rouge">MEMORY.md</code> 本质上就是一套持久化的 agent 状态管理机制，让 agent 在跨 session 运行时保持连贯性。</p> <h2 id="72-趋势二从模型优化走向-model-harness-co-training">7.2 趋势二：从模型优化走向 Model-Harness Co-Training</h2> <p>未来的优化对象不会只剩模型权重本身，而会扩展到整个 agent 系统的各个层次：</p> <ul> <li>Skills：如何让 agent 快速获得新能力，而不必重新训练</li> <li>Memory：跨 session 的持久化记忆与上下文压缩策略</li> <li>路由策略：多 agent 场景下的任务分发与优先级调度</li> <li>工具选择：在工具爆炸的时代，如何让 agent 精准选择合适工具</li> <li>工作流结构：如何把复杂任务分解为可靠的子任务序列</li> </ul> <p>Anthropic 的 <a href="https://resources.anthropic.com/2026-agentic-coding-trends-report">2026 Agentic Coding Trends Report</a> 明确指出：工程师的主要角色正在从“写代码”转向“编排 AI agent、评估其输出、提供战略方向”。这意味着系统级的优化能力，而非单纯的模型能力，将成为下一个竞争高地。OpenClaw 这类把系统级优化空间完全暴露出来的平台，比单一 <code class="language-plaintext highlighter-rouge">coding CLI</code> 更值得长期关注。</p> <p>进一步说，评估 “Agent” 时，不能只看模型本身，更应该把“模型 + harness”作为一个整体来评估；同一模型放在不同 harness 上，最终表现可能相差很大。Anthropic 在 <a href="https://www.anthropic.com/engineering/demystifying-evals-for-ai-agents">Demystifying evals for AI agents</a> 中也明确写道：</p> <blockquote> <p>“An agent harness (or scaffold) is the system that enables a model to act as an agent: it processes inputs, orchestrates tool calls, and returns results. When we evaluate ‘an agent,’ we’re evaluating the harness and the model working together. For example, <a href="https://claude.com/product/claude-code">Claude Code</a> is a flexible agent harness, and we used its core primitives through the <a href="https://platform.claude.com/docs/en/agent-sdk/overview">Agent SDK</a> to build our <a href="https://www.anthropic.com/engineering/effective-harnesses-for-long-running-agents">long-running agent harness</a>.”</p> </blockquote> <p>Windsurf 在 <a href="https://cognition.ai/blog/swe-1-5">Introducing SWE-1.5: Our Fast Agent Model</a> 里则把这个观点说得更直接：</p> <blockquote> <p>“We believe that the quality of the coding environments in RL tasks is the most important factor for downstream model performance.”</p> <p><em>译：我们认为，在 RL 过程中，coding 环境本身的质量，对模型最终表现的影响是最大的。</em></p> </blockquote> <blockquote> <p>“Our vision is to co-optimize models and harness: we repeatedly dogfooded the model, noticed issues with the harness, made adjustments to tools and prompts, and then re-trained the model on the updated harness.”</p> <p><em>译：我们的思路是把模型和 harness 当成一个整体来共同优化：一边反复使用模型，一边暴露 harness 中的问题，再去调整工具和 prompt，最后基于新的 harness 继续训练模型。</em></p> </blockquote> <h2 id="73-趋势三agent-runtime-走向新-os">7.3 趋势三：Agent Runtime 走向”新 OS”</h2> <p>OpenClaw 在工程上把 channel、工具、设备和外部 agent 放在同一个 Gateway 下。越来越多的观察者开始把 OpenClaw 这类平台类比为 “AI 时代的操作系统”：</p> <ul> <li>进程调度 → Agent 路由与 session 管理</li> <li>文件系统 → Workspace + Memory 持久化层</li> <li>驱动程序 → Plugins / Skills / MCP 工具</li> <li>进程间通信 → ACP、agentToAgent、Webhook</li> </ul> <p>NVIDIA CEO Jensen Huang 在 GTC 2026 上明确表示 OpenClaw “definitely is the next big thing”。Google Cloud、Anthropic 的 2026 年 Agent 趋势报告均预测：未来更有价值的系统，不是”某个点上最强的 agent”，而是”能把不同模式统一起来的 agent runtime”。多 agent 协作（MAS）将成为主流，预测到 2027 年 70% 的多 agent 系统将采用专业化角色分工。</p> <h2 id="74-趋势四从离线训练走向在线学习与持续进化">7.4 趋势四：从离线训练走向在线学习与持续进化</h2> <p>第 6 节的几项研究工作，其实在回答同一个问题：agent 上线之后，怎么持续变强？</p> <p>这个问题比看起来难。传统的做法是离线收集数据、训练、部署，然后等下一个版本。但真实运行中的 agent 面对的任务分布是动态的，用户习惯是个性化的，失败模式是在使用过程中才暴露的——这些都没办法提前打包进离线数据集里。可以预期，未来 agent 训练的重点会越来越少地停留在静态 benchmark，而越来越多地转向“真实交互中如何学习”。</p> <p>当然，以下一些问题都是未来将会面临的关键问题：</p> <ol> <li>真实任务里 reward 往往稀疏且延迟，credit assignment 极难；</li> <li>用真实用户交互的数据训练模型，数据合规和隐私保护是一道绕不开的工程门槛；</li> <li>从单用户交互学到的东西，到底是真正的能力提升还是对局部习惯的过拟合，目前也没有好的评估方法。</li> </ol> <h1 id="参考资料">参考资料</h1> <div class="footnotes" role="doc-endnotes"> <ol> <li id="fn:openclaw-lore"> <p><a href="https://docs.openclaw.ai/zh-CN/start/lore">Lore 页面</a> <a href="#fnref:openclaw-lore" class="reversefootnote" role="doc-backlink">&#8617;</a></p> </li> <li id="fn:openclaw-architecture-explained"> <p><a href="https://www.linkedin.com/pulse/openclaw-architecture-explained-paolo-perazzo-g4ubc/">OpenClaw Architecture, Explained</a> <a href="#fnref:openclaw-architecture-explained" class="reversefootnote" role="doc-backlink">&#8617;</a> <a href="#fnref:openclaw-architecture-explained:1" class="reversefootnote" role="doc-backlink">&#8617;<sup>2</sup></a></p> </li> <li id="fn:openclaw-architecture-doc"> <p><a href="https://docs.openclaw.ai/concepts/architecture">OpenClaw 架构文档</a> <a href="#fnref:openclaw-architecture-doc" class="reversefootnote" role="doc-backlink">&#8617;</a></p> </li> <li id="fn:openclaw-tools-and-plugins"> <p><a href="https://docs.openclaw.ai/tools">Tools and Plugins</a> <a href="#fnref:openclaw-tools-and-plugins" class="reversefootnote" role="doc-backlink">&#8617;</a></p> </li> <li id="fn:claude-code-loop-diagram"> <p><a href="https://code.claude.com/docs/en/how-claude-code-works">How Claude Code works</a> <a href="#fnref:claude-code-loop-diagram" class="reversefootnote" role="doc-backlink">&#8617;</a></p> </li> <li id="fn:pi-minimal-agent"> <p><a href="https://lucumr.pocoo.org/2026/1/31/pi/">Pi: The Minimal Agent Within OpenClaw</a> <a href="#fnref:pi-minimal-agent" class="reversefootnote" role="doc-backlink">&#8617;</a></p> </li> <li id="fn:system-prompt-doc"> <p><a href="https://docs.openclaw.ai/concepts/system-prompt">System Prompt</a> <a href="#fnref:system-prompt-doc" class="reversefootnote" role="doc-backlink">&#8617;</a></p> </li> <li id="fn:context-doc"> <p><a href="https://docs.openclaw.ai/concepts/context">Context</a> <a href="#fnref:context-doc" class="reversefootnote" role="doc-backlink">&#8617;</a> <a href="#fnref:context-doc:1" class="reversefootnote" role="doc-backlink">&#8617;<sup>2</sup></a></p> </li> <li id="fn:context-engine-doc"> <p><a href="https://docs.openclaw.ai/concepts/context-engine">Context Engine</a> <a href="#fnref:context-engine-doc" class="reversefootnote" role="doc-backlink">&#8617;</a></p> </li> <li id="fn:session-docs"> <p><a href="https://docs.openclaw.ai/concepts/session">Session</a>；<a href="https://docs.openclaw.ai/concepts/session-pruning">Session Pruning</a>；<a href="https://docs.openclaw.ai/concepts/session-tool">Session Tools</a> <a href="#fnref:session-docs" class="reversefootnote" role="doc-backlink">&#8617;</a></p> </li> <li id="fn:messages-docs"> <p><a href="https://docs.openclaw.ai/concepts/messages">Messages</a>；<a href="https://docs.openclaw.ai/concepts/streaming">Streaming</a>；<a href="https://docs.openclaw.ai/concepts/queue">Queue</a>；<a href="https://docs.openclaw.ai/concepts/retry">Retry</a> <a href="#fnref:messages-docs" class="reversefootnote" role="doc-backlink">&#8617;</a></p> </li> <li id="fn:agent-skills-overview"> <p><a href="https://platform.claude.com/docs/en/agents-and-tools/agent-skills/overview">Agent Skills overview</a> <a href="#fnref:agent-skills-overview" class="reversefootnote" role="doc-backlink">&#8617;</a></p> </li> <li id="fn:claude-hooks-lifecycle"> <p><a href="https://code.claude.com/docs/en/hooks">Hooks reference</a> <a href="#fnref:claude-hooks-lifecycle" class="reversefootnote" role="doc-backlink">&#8617;</a></p> </li> <li id="fn:claude-hooks-resolution"> <p><a href="https://code.claude.com/docs/en/hooks">Hooks reference</a> <a href="#fnref:claude-hooks-resolution" class="reversefootnote" role="doc-backlink">&#8617;</a></p> </li> <li id="fn:clawhub-doc"> <p><a href="https://docs.openclaw.ai/tools/clawhub">ClawHub 官方文档</a> <a href="#fnref:clawhub-doc" class="reversefootnote" role="doc-backlink">&#8617;</a></p> </li> <li id="fn:moltbook-links"> <p><a href="https://www.moltbook.com/">moltbook.com</a>；<a href="https://techcrunch.com/2026/03/10/meta-acquired-moltbook-the-ai-agent-social-network-that-went-viral-because-of-fake-posts/">TechCrunch</a> <a href="#fnref:moltbook-links" class="reversefootnote" role="doc-backlink">&#8617;</a></p> </li> <li id="fn:instreet-site"> <p><a href="https://instreet.coze.site/">InStreet 官网</a> <a href="#fnref:instreet-site" class="reversefootnote" role="doc-backlink">&#8617;</a></p> </li> <li id="fn:openspace"> <p><a href="https://github.com/HKUDS/OpenSpace">OpenSpace</a> <a href="#fnref:openspace" class="reversefootnote" role="doc-backlink">&#8617;</a></p> </li> <li id="fn:harness-new-dataset"> <p><a href="https://mp.weixin.qq.com/s/9qI83Ne-Ac_R9y-yJ6SVnQ">Harness is the New Dataset：模型智能提升的下一个关键方向</a> <a href="#fnref:harness-new-dataset" class="reversefootnote" role="doc-backlink">&#8617;</a></p> </li> </ol> </div>]]></content><author><name></name></author><category term="AI"/><category term="Agents"/><category term="OpenClaw"/><category term="Coding Agent"/><category term="Agentic RL"/><summary type="html"><![CDATA[1. OpenClaw 是什么]]></summary></entry><entry><title type="html">Belman Equation</title><link href="https://auroralhl.github.io/blog/2025/belman-equation/" rel="alternate" type="text/html" title="Belman Equation"/><published>2025-11-18T00:00:00+00:00</published><updated>2025-11-18T00:00:00+00:00</updated><id>https://auroralhl.github.io/blog/2025/belman-equation</id><content type="html" xml:base="https://auroralhl.github.io/blog/2025/belman-equation/"><![CDATA[<p>This article is hosted externally on <a href="https://zhuanlan.zhihu.com/p/688029400">Zhihu</a>.</p>]]></content><author><name></name></author><category term="RL"/><summary type="html"><![CDATA[This article is hosted externally on Zhihu.]]></summary></entry><entry><title type="html">Scaling Law</title><link href="https://auroralhl.github.io/blog/2024/scaling-law/" rel="alternate" type="text/html" title="Scaling Law"/><published>2024-11-08T08:12:00+00:00</published><updated>2024-11-08T08:12:00+00:00</updated><id>https://auroralhl.github.io/blog/2024/scaling-law</id><content type="html" xml:base="https://auroralhl.github.io/blog/2024/scaling-law/"><![CDATA[<h1 id="一什么是-scaling-law">一、什么是 Scaling Law</h1> <p><strong>Scaling Law</strong>（缩放法则）是人工智能和机器学习中一类理论，它描述了随着模型规模（例如参数数量）、训练数据量、计算资源的增加，模型性能如何提升的规律。简单来说，<strong>Scaling Law 研究的是模型性能与模型规模之间的关系</strong>。</p> <p><strong>定义【Scaling Law】</strong><sup id="fnref:1"><a href="#fn:1" class="footnote" rel="footnote" role="doc-noteref">1</a></sup></p> <blockquote> <p>在生成模型中被广泛观察到的现象，对于计算量C，模型参数量N和数据大小D，当不受另外两个因素影响时，模型的性能与每个因素都呈幂律关系：</p> <ul> <li>性能$\propto N^{\alpha}$</li> <li>性能$\propto D^{\beta}$</li> <li>性能$\propto C^{\gamma}$</li> </ul> <p>这些公式中的 α、β、γ 是对应维度的缩放指数。通常模型性能可以用Test Loss来表示，Loss越小说明模型性能越好。</p> </blockquote> <p><img src="https://aurora-pics.oss-cn-beijing.aliyuncs.com/Pic/202411072223214.png" alt="截屏2024-10-19 10.54.28"/></p> <p>Scaling Law背后的基本思想是：<strong>模型的性能可以通过简单的扩展（例如增加模型参数、训练数据或计算资源）来不断提升</strong>，并且这种提升往往遵循一定的幂律关系。通过研究这种关系，研究者可以预测模型在不同规模下的性能表现，指导大模型的设计和训练。</p> <h1 id="二scaling-law的应用">二、Scaling Law的应用</h1> <p>Scaling Law总结出来的一个规律是：</p> \[C\approx6ND\] <p>其中C是计算量，N是参数量，D是训练数据量。举个例子：</p> <blockquote> <p>假设一个模型有 10亿个参数（$N=10^9$ ）, 并且训练数据集的规模是$\mathrm{D}=10^{12}$ （1万亿个 token）.使用公式 C = 6ND, 总的计算量就是:</p> \[C=6 \times 10^9 \times 10^{12}=6 \times 10^{21} \mathrm{FLOPs}\] <p>这表明要训练这个模型, 大约需要$6\times 10^{21}$ 次浮点运算。</p> </blockquote> <p><img src="https://aurora-pics.oss-cn-beijing.aliyuncs.com/Pic/202411072223216.png" alt="截屏2024-09-25 19.46.14"/>这个规律有什么用呢?通过前面的Scaling Law我们知道，训练大模型时，<strong>增加模型的参数量或者训练的数据量，模型性能会得到提升</strong>。但是我们并不能无止境的增加，因为现实训练模型收到计算量的制约，训练一个语言大模型是很费钱的。<strong>所以当给定一个计算量budget，我们怎么分配N和D得到一个最好的模型呢</strong>？（因为现实情况我们通常可以知道我们有多少张卡，可以用多久）</p> <p>这个问题可以建模为如下的优化问题：</p> \[N_{opt}(C),D_{opt}(C)=\underset{N,D\text{ s.t. FLOPs}(N,D)=C}{\operatorname*{argmin}}L(N,D),\\ \hat{L}(N,D)\triangleq E+\frac A{N^\alpha}+\frac B{D^\beta}.\] <h2 id="1-最佳模型参数数据量求解方法">1 最佳模型参数，数据量求解方法</h2> <p>这个多变量问题怎么解呢？主要有三种方法：</p> <ol> <li>固定模型大小，改变训练数据</li> <li>固定计算量，改变模型大小</li> <li>拟合幂律曲线</li> </ol> <p><img src="https://aurora-pics.oss-cn-beijing.aliyuncs.com/Pic/202411072223217.png" alt="截屏2024-09-26 10.00.51"/></p> <p>根据上表的结果，得出a=0.5,b=0.5</p> <p><img src="https://aurora-pics.oss-cn-beijing.aliyuncs.com/Pic/202411072223221.png" alt="image-20240926103233403"/>根据图3右边两图所得到的点，向外延伸，可以得到给定计算量C最佳的N、D.</p> <p><img src="https://aurora-pics.oss-cn-beijing.aliyuncs.com/Pic/202411072223222.png" alt="截屏2024-09-26 10.35.43"/></p> <p><img src="https://aurora-pics.oss-cn-beijing.aliyuncs.com/Pic/202411072223223.png" alt="image-20240926103557968"/></p> <h2 id="2-llama31的scaling-law">2 LLaMA3.1的Scaling Law</h2> <p>如下图所示，是LLaMA3.1中的Scaling Law，LLaMA3.1发布了3个模型，分别是8B,70B,405B.这个405B是怎么定下来的呢，难道是领导拍脑袋想出来的吗（国内可能是hh).显然他们做了实验，先在小数据和小模型上进行实验（左图），<strong>然后根据实验结果画出Scaling Law曲线，找到对应计算量的最优模型大小和最优训练数据量。</strong> <img src="https://aurora-pics.oss-cn-beijing.aliyuncs.com/Pic/202411072223224.png" alt="截屏2024-09-26 10.42.58"/></p> <h2 id="3-应用示例">3 应用示例</h2> <blockquote> <ul> <li>假设你有1000张H100显卡，并且可以用6个月。</li> <li>假设你有10T的数据。</li> <li>那么你应该训练多大的模型？</li> </ul> </blockquote> <p><img src="https://aurora-pics.oss-cn-beijing.aliyuncs.com/Pic/202411072223225.png" alt="image-20240927145027916"/></p> <p><strong>另一种更快的估计方法：</strong></p> <p><img src="https://aurora-pics.oss-cn-beijing.aliyuncs.com/Pic/202411072223226.png" alt="image-20240927145042459"/></p> <h1 id="三未来挑战">三、未来挑战</h1> <p>尽管 Scaling Law 提供了重要的理论指导，仍然存在一些挑战：</p> <ul> <li><strong>计算成本问题</strong>：大规模扩展模型的参数和训练数据通常需要极高的计算成本。虽然 Scaling Law 提供了理论依据，但大规模训练的实际成本可能难以承受。</li> <li><strong>数据质量</strong>：Scaling Law 假设数据量的增加会提升模型性能，但在实际应用中，数据的质量同样至关重要，低质量数据可能会导致性能下降甚至模型偏差。</li> <li><strong>性能饱和</strong>：Scaling Law 研究表明，性能提升并不是无限的，通常会在某个点达到瓶颈。因此，研究者需要找到其他方法（如新架构、知识蒸馏）来进一步提高性能。</li> </ul> <h1 id="参考资料">参考资料</h1> <div class="footnotes" role="doc-endnotes"> <ol> <li id="fn:1"> <p>Scaling Laws for Neural Language Models <a href="#fnref:1" class="reversefootnote" role="doc-backlink">&#8617;</a></p> </li> </ol> </div>]]></content><author><name></name></author><category term="AI"/><category term="Large Language Models"/><category term="Machine Learning"/><category term="LLM"/><summary type="html"><![CDATA[一、什么是 Scaling Law]]></summary></entry><entry><title type="html">Transformer Architecture Explained: Attention is All You Need</title><link href="https://auroralhl.github.io/blog/2024/transformer/" rel="alternate" type="text/html" title="Transformer Architecture Explained: Attention is All You Need"/><published>2024-07-26T03:50:00+00:00</published><updated>2024-07-26T03:50:00+00:00</updated><id>https://auroralhl.github.io/blog/2024/transformer</id><content type="html" xml:base="https://auroralhl.github.io/blog/2024/transformer/"><![CDATA[<p>🚀在自然语言处理（NLP）领域，Transformer架构已经成为最先进的技术之一，其核心概念是自注意力机制（Self-Attention Mechanism）。</p> <p>📚在前面的两小节中，我们已经介绍了注意力机制的基础知识，包括RNN、Seq2Seq等传统方法的基本概念和实现。此外，我们详细讨论了自注意力机制（Self-Attention）及其在现代NLP模型中的重要性。自注意力机制允许模型在处理每个输入时“关注”输入序列的不同部分，从而理解单词与其他单词之间的关系，而不是逐个地线性处理输入。</p> <p>🔥在理解了自注意力机制的基础上，我们来介绍大语言模型的基础——Transformer结构，Attention is all you need！</p> <h2 id="一transformer框架">一、Transformer框架</h2> <p><strong>Transformer</strong> 的核心概念是 <strong>自注意力机制（Self-Attention Mechanism）</strong>，它允许模型在处理每个输入时“关注”输入序列的不同部分。这种机制让模型能够理解每个单词或符号与其他单词或符号之间的关系，而不是逐个地线性处理输入。原始论文给出的Transformer结构如下图所示：</p> <p><img src="https://aurora-pics.oss-cn-beijing.aliyuncs.com/Pic/202407151624024.png" alt="截屏2024-07-15 16.24.24"/></p> <p>上图所示的Transformer 主要由两个部分组成：</p> <ul> <li><strong>编码器（Encoder）</strong>：将输入序列转换为一个隐表示（向量表示）；</li> <li><strong>解码器（Decoder）</strong>：从隐表示生成输出序列.</li> </ul> <p><strong>编码器</strong> 和 <strong>解码器</strong> 都由多个<strong>层（layers)</strong> 组成，每层都包括:</p> <ol> <li>一个 <strong>多头自注意力机制</strong> ;</li> <li>一个 <strong>前馈神经网络（Feed-Forward Neural Network, FFN）</strong>；</li> <li><strong>残差结构以及层归一化操作</strong>.</li> </ol> <p>下面详细分析Transformer结构每个部分的作用及计算过程。</p> <h2 id="二encoder">二、Encoder</h2> <h3 id="1自注意力机制">（1）自注意力机制</h3> <p>在上一小节我们详细介绍了注意力机制，本节我们仅介绍其在Transformer结构中的计算过程。对于输入序列$\mathbf{X}=[x_1,x_2,\ldots,x_n]$,每个元素$x_i$首先被投影到三个不同的向量 :</p> <ol> <li>查询向量(Query)Q</li> <li>键向量( Key) K</li> <li>值向量( Value) V</li> </ol> <p>这些向量的计算公式如下：</p> \[\mathbf{Q}=\mathbf{X}\mathbf{W}^Q,\quad\mathbf{K}=\mathbf{X}\mathbf{W}^K,\quad\mathbf{V}=\mathbf{X}\mathbf{W}^V\] <p>其中，$\mathbf{W}^Q,\mathbf{W}^K,\mathbf{W}^V$是可学习的权重矩阵。自注意力的核心公式是计算每个查询向量与所有键向量之间的相似度，原文采用的是缩放点积模型 :</p> \[\text{Attention}(\mathbf{Q},\mathbf{K},\mathbf{V})=\text{softmax}\biggl(\frac{\mathbf{Q}\mathbf{K}^T}{\sqrt{d_k}}\biggr)\mathbf{V}\] <p>这里，$\frac1{\sqrt{d_k}}$是缩放因子，<strong>用于避免相似度值过大</strong>。softmax 函数将相似度转换为权重，最后乘以 V 得到加权的值向量。下面用一个实例的图示来描述这个计算过程，并且能够清晰的看到各个部分的维度：</p> <p><img src="https://aurora-pics.oss-cn-beijing.aliyuncs.com/Pic/202407211517524.png" alt="截屏2024-07-21 15.17.02"/></p> <p>如上图所示，$X_1,X_2$分别与$\mathbf{W}^Q,\mathbf{W}^K,\mathbf{W}^V$相乘可以得到$q、k、v$，下面假设一些值进行计算：</p> <p><img src="https://aurora-pics.oss-cn-beijing.aliyuncs.com/Pic/202407211518279.png" alt="截屏2024-07-21 15.18.22"/></p> <p>可以看到最后得到的注意力值$z_i$维度和$v_i$的维度一致.如果$X_1,X_2$拼接成矩阵，那么其计算过程图示如下：</p> <p><img src="https://aurora-pics.oss-cn-beijing.aliyuncs.com/Pic/202407211523009.png" alt="截屏2024-07-21 15.23.50"/></p> <p>可以看到最后的$Z$就是由$z_1,z_2$拼接得到，并且$Z$与$V$的维度仍保持一致.</p> <h3 id="2多-头-注-意-力-机-制--multi--head-attention-">（2）多 头 注 意 力 机 制 ( Multi- Head Attention )</h3> <p>为了让模型捕捉到不同子空间的特征，多头注意力机制将上述注意力机制应用多个头( head ) :</p> \[\mathrm{MultiHead}( \mathbf{Q} , \mathbf{K} , \mathbf{V} ) = [ \mathrm{head}_1, \mathrm{head}_2, \ldots , \mathrm{head}_h] \mathbf{W} ^O\] <p>其中，每个 head$_i$是一个独立的自注意力机制 :</p> \[\mathrm{head}_i=\mathrm{Attention}(\mathbf{QW}_i^Q,\mathbf{KW}_i^K,\mathbf{VW}_i^V)\\\] <p>$\mathbf{W}^O$是用于连接各个头结果的权重矩阵.下面用一个图例来描述多头注意力机制的计算过程：</p> <p><img src="https://aurora-pics.oss-cn-beijing.aliyuncs.com/Pic/202407211533446.png" alt="截屏2024-07-21 15.33.17"/></p> <p>如上图所示，$h=2$，左右两边结构完全一样，每个head的计算方式也完全一样。当$h=8$时，我们可以计算得到8个$Z_i$：</p> <p><img src="https://aurora-pics.oss-cn-beijing.aliyuncs.com/Pic/202407211535208.png" alt="截屏2024-07-21 15.35.35"/></p> <p>多头注意力机制就是将多个head的输出拼接起来，同时再乘以一个大的参数矩阵$W^O$</p> <p><img src="https://aurora-pics.oss-cn-beijing.aliyuncs.com/Pic/202407211536896.png" alt="截屏2024-07-21 15.36.03"/></p> <p>由上图可知，<strong>最后得到的$Z$的维度与$X$的维度保持一致.这是为了方便后面的layer当作输入，在Encoder结构中，只有第一层需要将输入Embedding成$X$，后面的层直接使用上一层的输出当作输入</strong>.下图是一个多头注意力机制计算过程的完整图示：</p> <p><img src="https://aurora-pics.oss-cn-beijing.aliyuncs.com/Pic/202407211554008.png" alt="截屏2024-07-21 15.54.22"/></p> <p>到目前为止介绍的多头注意力机制结构存在两个问题：</p> <ol> <li>与循环神经网络不同，自注意力机制并<strong>不按顺序构建信息</strong>，这个结构没有对输入顺序的内在表示，也就是说，输入的顺序完全不影响网络输出，但是我们知道句子里单词的顺序是重要的。所以Transformer结构中中引入了一个技巧——<strong>位置编码（Positional Encoding）</strong>.</li> <li>注意到在上面的计算过程中，除了Softmax都是线性计算，使得网络的表达能力受限，Transformer中在每个多头注意力机制后引入一个<strong>前 馈 神 经 网 络 ( Feed- Forward Network ）</strong>来增强网络的表示能力.</li> </ol> <h3 id="3位置编码positional-encoding">（3）位置编码（Positional Encoding）</h3> <p>为了编码每个词的位置信息，原始论文中提出用一个新的向量$p_i$来编码位置信息，其维度和Embedding的维度一致，在编码器的第一层，我们将$p_i$与$x_i$叠加得到最终的输入向量矩阵$X$.</p> <p><img src="https://aurora-pics.oss-cn-beijing.aliyuncs.com/Pic/202407211610228.png" alt="截屏2024-07-21 16.10.43"/></p> <p>这个位置向量怎么得到呢？有以下几种方法：</p> <ol> <li> <p>绝对位置编码:正弦位置表示，原论文给的计算公式如下：</p> \[\begin{aligned}PE_{(pos,2i)}&amp;=\sin(pos/10000^{2i/d_{\mathrm{model}}})\\PE_{(pos,2i+1)}&amp;=\cos(pos/10000^{2i/d_{\mathrm{model}}})\end{aligned}\] <p>其中：</p> <ul> <li>pos表示单词在句子中的绝对位置，pos=0，1，2…，例如：Jerry在”Tom chase Jerry”中的pos=2；</li> <li>$d_{model}$表示词向量的维度，在这里$d_{model}$=512；</li> <li>2i和2i+1表示奇偶性，i表示词向量中的第几维，例如这里$d_{model}$=512，故i=0，1，2…255.</li> </ul> </li> <li> <p>绝对位置编码:通过学习表示，$p_i$是一个可学习的参数，通过训练来学习到每个词的位置表示.</p> </li> </ol> <h3 id="4残差residuals">（4）残差（Residuals)</h3> <p>由Transformer的结构我们可以看到，在经过Multi-Head Attention得到矩阵$Z$之后，并没有直接传入全连接神经网络FNN，而是经过了一步：Add＆Normalize。</p> <p>Add，就是在Z的基础上加了一个残差块X，加入残差块X的目的是为了防止在深度神经网络训练中发生退化问题，退化的意思就是深度神经网络通过增加网络的层数，Loss逐渐减小，然后趋于稳定达到饱和，然后再继续增加网络层数，Loss反而增大。</p> <p><img src="https://aurora-pics.oss-cn-beijing.aliyuncs.com/Pic/202407211630840.png" alt="截屏2024-07-21 16.30.14"/></p> <h3 id="5层归一化">（5）层归一化</h3> <p>在进行了残差连接后，还要对数据进行层归一化，其目的有二：</p> <ol> <li>能够加快训练的速度;</li> <li>提高训练的稳定性.</li> </ol> <p>为什么使用Layer Normalization（LN）而不使用Batch Normalization（BN）呢？，LN是在同一个样本中不同神经元之间进行归一化，而BN是在同一个batch中不同样本之间的同一位置的神经元之间进行归一化。对多个词向量进行BN归一化没有意义，但是可以对每个词向量的数据进行归一化，加快训练速度。</p> <p>对于给定的输入$x$,其维度为$(N,L)$,其中$N$是批量大小(词的个数），$L$是特征维度（词向量维度）。层归一化的计算公式为：</p> \[\mu=\frac1L\sum_{j=1}^Lx_{ij}\\ \sigma^2=\frac1L\sum_{j=1}^L(x_{ij}-\mu)^2\\ \hat{x}_{ij}=\frac{x_{ij}-\mu}{\sqrt{\sigma^2+\epsilon}}\\ y_{ij}=\gamma\hat{x}_{ij}+\beta\] <p>其中，$\gamma$和$\beta$是可训练参数，$\epsilon$是防止除零的小常数。</p> <h3 id="6前-馈-神-经-网-络--feed--forward-network-">（6）前 馈 神 经 网 络 ( Feed- Forward Network ）</h3> <p>每个编码器和解码器层还包括一个前馈神经网络：</p> \[\mathrm{FFN}(\mathbf{x})=\max(0,\mathbf{x}\mathbf{W}_1+\mathbf{b}_1)\mathbf{W}_2+\mathbf{b}_2\] <p>这里的全连接层是一个两层的神经网络，先线性变换，然后ReLU非线性，再线性变换。这里的x就是我们Multi-Head Attention的输出Z，若Z是(2,64)维的矩阵，假设W1是(64,1024)，其中W2与W1维度相反(1024,64)，那么按照上面的公式：</p> \[FFN(Z)=(2,64)\times(64,1024)\times(1024,64)=(2,64)\] <p>我们发现维度没有发生变化，这两层网络就是为了将输入的Z映射到更加高维的空间中(2,64)x(64,1024)=(2,1024)，然后通过非线性函数ReLU进行筛选，筛选完后再变回原来的维度。然后经过Add＆Normalize，输入下一个encoder中，经过6个encoder后输入到decoder中.</p> <h2 id="三decoder">三、Decoder</h2> <p>接下来来看Decoder部分，其结构如下所示，和Encoder很像，但是多了一个Masked Multi-Head Attention层，这是干嘛用的呢？这是为了防止Decoder在训练的时候“作弊”，在每个时间步t只允许看到这之前的信息，不能利用t+1后的信息。下面来详细的介绍每个部分。</p> <p><img src="https://aurora-pics.oss-cn-beijing.aliyuncs.com/Pic/202407211830605.png" alt="截屏2024-07-21 18.30.23"/></p> <h3 id="1解码器的输入">（1）解码器的输入</h3> <p>Decoder的输入分为两类：一种是训练时的输入，一种是预测时的输入。</p> <ol> <li>训练时的输入就是已经对准备好对应的target数据。例如翻译任务，Encoder输入”Tom chase Jerry”，Decoder输入”汤姆追逐杰瑞”。</li> <li>预测时的输入，一开始输入的是起始符，然后每次输入是上一时刻Transformer的输出。例如，输入”“，输出”汤姆”，输入”汤姆”，输出”汤姆追逐”，输入”汤姆追逐”，输出”汤姆追逐杰瑞”，输入”汤姆追逐杰瑞”，输出”汤姆追逐杰瑞”结束。</li> </ol> <p>下面动图所示是解码器的第一个时间步，输入是起始符，不过也会用到编码器的信息：</p> <p><img src="https://aurora-pics.oss-cn-beijing.aliyuncs.com/Pic/202407211843754.gif" alt="transformer_decoding_1"/></p> <p>下面动图展示了Decoder后面几个时间步的输入，是上一个时间步解码器的输入：</p> <p><img src="https://aurora-pics.oss-cn-beijing.aliyuncs.com/Pic/202407211843554.gif" alt="transformer_decoding_2"/></p> <h3 id="2masked-multi-head-attention">（2）Masked Multi-Head Attention</h3> <p>与Encoder的Multi-Head Attention计算原理一样，只是多加了一个mask码。mask 表示掩码，它对某些值进行掩盖，使其在参数更新时不产生效果。两种模型的结构对比如下图所示：</p> <p><img src="https://aurora-pics.oss-cn-beijing.aliyuncs.com/Pic/202407211901018.png" alt="截屏2024-07-21 19.01.53"/></p> <p>Transformer 模型里面涉及两种 mask，分别是 padding mask 和 sequence mask。为什么需要添加这两种mask码呢？</p> <p>==1.padding mask== 什么是 padding mask 呢？因为每个批次输入序列长度是不一样的也就是说，我们要对输入序列进行对齐。具体来说，就是给在较短的序列后面填充 0。但是如果输入的序列太长，则是截取左边的内容，把多余的直接舍弃。因为这些填充的位置，其实是没什么意义的，所以我们的attention机制不应该把注意力放在这些位置上，所以我们需要进行一些处理。 具体的做法是，把这些位置的值加上一个非常大的负数(负无穷)，这样的话，<strong>经过 softmax，这些位置的概率就会接近0！</strong></p> <p>==2.sequence mask== sequence mask 是<strong>为了使得 decoder 不能看见未来的信息。</strong>对于一个序列，在 time_step 为 t 的时刻，我们的解码输出应该只能依赖于 t 时刻之前的输出，而不能依赖 t 之后的输出。因此我们需要想一个办法，把 t 之后的信息给隐藏起来。这在训练的时候有效，因为训练的时候每次我们是将target数据完整输入进decoder中地，预测时不需要，预测的时候我们只能得到前一时刻预测出的输出。</p> <p>通过sequence mask，可以使得网络进行并行计算.具体做法是在每个时间步t，我们屏蔽对未来词的注意力，将对未来的词（token）的attention值设置为-∞，其余部分的Attention值计算同前面介绍的方法一致，下图给出了一个示例：</p> <p><img src="https://aurora-pics.oss-cn-beijing.aliyuncs.com/Pic/202407211905309.png" alt="image-20240721190529252"/></p> <p>Decoder中的Multi-Head Attention层的工作原理和Encoder一样，<strong>只是它从下面的Masked Multi-Head Attention层创建查询矩阵Q，并从编码器堆栈的输出中获取键和值矩阵（K、V）。</strong></p> <h3 id="3decoder的输出">（3）Decoder的输出</h3> <p>解码器输出一个浮点数向量，如何将其转化为一个单词呢？这就是最终的线性层（Linear layer）和后续的Softmax层的工作。</p> <p><img src="https://aurora-pics.oss-cn-beijing.aliyuncs.com/Pic/202407211917409.png" alt="截屏2024-07-21 19.17.51"/></p> <p>线性层是一个简单的全连接神经网络，它将解码器生成的向量投射到一个更大得多的向量上，这个向量称为<strong>logits向量</strong>。假设我们的模型知道10,000个独特的英文单词（模型的“输出词汇”），这些单词是从训练数据集中学习到的。这将使logits向量为10,000维，每维对应一个唯一单词的得分。这就是我们通过线性层解释模型输出的方式。然后，Softmax层将这些得分转化为概率（所有概率都是正数，总和为1.0）。选择概率最高的index，并将其对应的单词作为该时间步的输出。</p> <h2 id="四复杂度分析及改进方法介绍">四、复杂度分析及改进方法介绍</h2> <p>总的来说，自注意力模型的时间和空间复杂度与<strong>输入序列长度$N$呈2次关系</strong>，可以不严格的表示为$O(N^2)$.</p> <h3 id="1时间复杂度">（1）时间复杂度</h3> <p><img src="https://aurora-pics.oss-cn-beijing.aliyuncs.com/Pic/202407211928934.png" alt="截屏2024-07-21 19.28.20"/></p> <p><img src="https://aurora-pics.oss-cn-beijing.aliyuncs.com/Pic/202407211929340.png" alt="截屏2024-07-21 19.29.49"/></p> <h3 id="2空间复杂度">（2）空间复杂度</h3> <p><img src="https://aurora-pics.oss-cn-beijing.aliyuncs.com/Pic/202407211930137.png" alt="截屏2024-07-21 19.30.31"/></p> <h3 id="3改进方法">（3）改进方法</h3> <p>通过上面两小节的分析，我们知道Self Attention的时间和空间复杂度是输入序列的$O(N^2)$，这对于处理长文本来说效率太低了，目前学界提出了很多改进Self Attention模型的方法：</p> <p><img src="https://aurora-pics.oss-cn-beijing.aliyuncs.com/Pic/202407211944746.png" alt="截屏2024-07-21 19.44.28"/></p> <h2 id="参考资料">参考资料</h2> <ol> <li><a href="https://proceedings.neurips.cc/paper/2017/file/3f5ee243547dee91fbd053c1c4a845aa-Paper.pdf">Attention is all you need</a></li> <li><a href="https://jalammar.github.io/illustrated-transformer/">The Illustrated Transformer</a></li> <li>https://blog.csdn.net/Tink1995/article/details/105080033</li> </ol>]]></content><author><name></name></author><category term="AI"/><category term="Large Language Models"/><category term="Machine Learning"/><category term="LLM"/><summary type="html"><![CDATA[🚀在自然语言处理（NLP）领域，Transformer架构已经成为最先进的技术之一，其核心概念是自注意力机制（Self-Attention Mechanism）。]]></summary></entry><entry><title type="html">Understanding Attention Mechanism: Self-Attention and Attention Models</title><link href="https://auroralhl.github.io/blog/2024/attention-mechanism/" rel="alternate" type="text/html" title="Understanding Attention Mechanism: Self-Attention and Attention Models"/><published>2024-07-26T03:39:00+00:00</published><updated>2024-07-26T03:39:00+00:00</updated><id>https://auroralhl.github.io/blog/2024/attention-mechanism</id><content type="html" xml:base="https://auroralhl.github.io/blog/2024/attention-mechanism/"><![CDATA[<p>在自然语言处理领域，注意力机制（Attention Mechanism）已经成为提升模型性能的重要工具。<strong>传统的Encoder-Decoder结构在处理长序列时，常常因为统一语义特征向量的长度限制而导致性能瓶颈</strong>。然而，注意力机制通过引入动态上下文向量，成功解决了这一问题，使得模型能够在每个时间步选择与当前输出最相关的信息。</p> <blockquote> <p class="prompt-tip">本篇博客将详细介绍注意力机制的基本原理、一般形式以及自注意力模型，并通过具体例子和图示来更好地理解这些关键概念。</p> </blockquote> <h2 id="一语言模型实例">一、语言模型实例</h2> <p>在Encoder-Decoder结构中，Encoder把所有的输入序列都编码成一个统一的语义特征c再解码：</p> <p><img src="https://aurora-pics.oss-cn-beijing.aliyuncs.com/Pic/202405262030938.png" alt="截屏2022-06-02 下午8.26.20"/></p> <p><strong>因此， c中必须包含原始序列中的所有信息，它的长度就成了限制模型性能的瓶颈</strong>。如机器翻译问题，当要翻译的句子较长时，一个c可能存不下那么多信息，就会造成翻译精度的下降。</p> <p><strong>Attention机制</strong>通过在每个时间输入不同的c来解决这个问题，下图是带有Attention机制的Decoder：</p> <p><img src="https://aurora-pics.oss-cn-beijing.aliyuncs.com/Pic/202405281637491.png" alt="截屏2022-06-02 下午8.37.47"/></p> <p>每一个c会自动去选取与当前所要输出的y最合适的上下文信息。具体来说:</p> <ol> <li>用 $a_{i j}$ 衡量 Encoder中第j阶段的$h_j$和解码时第i阶段的相关性;</li> <li>最终Decoder中第i阶段的输入的上下文信息 $c_{i}$ 就来自于所有 $h_{j}$ 对 $a_{i j}$ 的加权和。</li> </ol> <p>以机器翻译为例（将中文翻译成英文）：</p> <p><img src="https://aurora-pics.oss-cn-beijing.aliyuncs.com/Pic/202405281637341.png" alt="截屏2022-06-02 下午8.42.38"/></p> <p>输入的序列是“我爱中国”:</p> <ol> <li>因此, Encoder中的h1、h2、h3、h4就可以分别看做是“我”、 “爱”、“中”、“国”所代表的信息;</li> <li>在翻译成英语时, 第一个上下文$c_1$应该和“我”这个字最相关, 因此对应的 $a_{11}$ 就比较大, 而相应的 $a_{12} 、 a_{13} 、 a_{14}$ 就比较小;</li> <li>c2应该和“爱”最相关, 因此对应的 $a_{22}$ 就比较大;</li> <li>最后的$c_3$和$h_3、h_4$最相关, 因此 $a_{33} 、 a_{34}$ 的值就比较大。</li> </ol> <p>这些权重 $a_{i j}$ 是怎么来的?事实上, $a_{i j}$ 同样是从模型中学出的, 它实际和Decoder的第i阶段的隐状态、Encoder第j个阶段的隐状态有关，在下面一小节我们会介绍$a_{ij}$如何计算.</p> <ul> <li>这里的$c_1,c_2,c_3$就是<strong>attention值</strong>;</li> </ul> <h2 id="二一般模型">二、一般模型</h2> <p>刚刚我们是基于Encoder-Decoder模型来介绍attention机制的，下面我们更一般的来介绍注意力机制.</p> <p>用$X=[x_1,\cdots,x_N]\in\mathbb{R}^{D\times N}$ 表示$N$组输入信息，其中$D$ 维向量 $\boldsymbol{x}_n\in$ $\mathbb{R}^D,n\in[1,N]$表示一组输入信息.为了节省计算资源，不需要将所有信息都输入神经网络，只需要从$\boldsymbol X$ 中选择一些和任务相关的信息.注意力机制的计算可以分为两步：</p> <ol> <li>一是在所有输入信息上<strong>计算注意力分布</strong>；</li> <li>二是根据注意力分布来<strong>计算输入信息的加权平均</strong>.</li> </ol> <p>为了从$N$个输入向量$[\boldsymbol x_1,\cdotp\cdotp\cdotp,\boldsymbol x_N]$中选择出和某个特定任务相关的信息，我们需要引入一个和任务相关的表示，称为<strong>查询向量(Query Vector)</strong>, 并通过一个打分函数来衡<strong>量每个输入向量和查询向量之间的相关性</strong>. 给定一个和任务相关的查询向量$\boldsymbol q$,我们用注意力变量$z\in[1,N]$来表示被选择信息的索引位置，即$z=n$ 表示选择了第$n$ 个输入向量.为了方便计算，我们采用一种“软性”的信息选择机制.首先计算在给定$q$和$X$下，选择第$n$个输入向量的概率$\alpha_n$,</p> \[\begin{aligned}\alpha_{n}&amp;=p(z=n|X,\boldsymbol{q})\\&amp;=\operatorname{softmax}\left(s(\boldsymbol{x}_n,\boldsymbol{q})\right)\\&amp;=\frac{\exp\left(s(\boldsymbol{x}_n,\boldsymbol{q})\right)}{\sum_{j=1}^N\exp\left(s(\boldsymbol{x}_j,\boldsymbol{q})\right)},\end{aligned}\] <p>其中$\alpha_n$ 称为<strong>注意力分布( Attention Distribution)</strong>,$s(\boldsymbol x,\boldsymbol{q})$ 为<strong>注意力打分函数.</strong></p> <blockquote> <p>注意力打分函数$\mathbf{s}(\mathbf{x},\mathbf{q})$：计算输入向量和查询向量之间的相关性，常用如下模型</p> \[\begin{aligned}&amp;\bullet\text{ 加性模型: }\mathbf{s}(\mathbf{x},\mathbf{q})=\mathbf{v}^{T}\mathrm{tanh}(\mathbf{W}\mathbf{x}+\mathbf{U}\mathbf{q}).\\&amp;\bullet\text{ 点积模型: }\mathbf{s}(\mathbf{x},\mathbf{q})=\mathbf{x}^{T}\mathbf{q}.\\&amp;\bullet\text{ 缩放点积模型: }\mathbf{s}(\mathbf{x},\mathbf{q})=\frac{\mathbf{x}^{T}\mathbf{q}}{\sqrt{D}}.\\&amp;\bullet\text{ 双线性模型: }\mathbf{s}(\mathbf{x},\mathbf{q})=\mathbf{x}^{T}\mathbf{W}\mathbf{q}.\end{aligned}\] <p>这里$\mathbf{W},\mathbf{U},\mathbf{v}$为可学习的参数，$D$为输入向量的维度.</p> </blockquote> <p><strong>Note:</strong></p> <ol> <li>在前面的例子中，$h_1,h_2,h_3,h_4$就是输入向量$X$;</li> <li>$h’_1,h’_2,h’_3$就是查询向量$q$;</li> <li>$a_{ij}$就是注意力分布，这里我们给出了注意力分布怎么计算的.</li> </ol> <p>得到注意力分布后，对输入向量加权平均可以得到attention值：</p> \[\begin{aligned}\operatorname{att}(X,\boldsymbol{q})&amp;=\sum_{n=1}^N\alpha_nx_n,\\&amp;=\mathbb{E}_{z\sim p(z|X,\boldsymbol{q})}[x_z].\end{aligned}\] <p>下图（a）清晰的描述了attention的计算过程.</p> <p><img src="https://aurora-pics.oss-cn-beijing.aliyuncs.com/Pic/202407151828526.png" alt="截屏2024-07-15 18.28.38"/></p> <p>更一般地，我们可以用键值对( key-value pair)格式来表示输入信息，其中“键”用来计算注意力分布$\alpha_n$,“值”用来计算聚合信息.用$\left(\boldsymbol{K},\boldsymbol{V}\right)=\left[\left(\boldsymbol{k}_1,\boldsymbol{\upsilon}_1\right),\cdots,\left(\boldsymbol{k}_N,\boldsymbol{\upsilon}_N\right)\right]$表示$N$组输入信息，给定任务相关的查询向量$q$时，注意力函数为</p> \[\begin{aligned}\operatorname{att}\Big((\boldsymbol{K},\boldsymbol{V}),\boldsymbol{q}\Big)&amp;=\sum_{n=1}^N\alpha_n\boldsymbol{v}_n,\\&amp;=\sum_{n=1}^N\frac{\exp\left(s(\boldsymbol{k}_n,\boldsymbol{q})\right)}{\sum_j\exp\left(s(\boldsymbol{k}_j,\boldsymbol{q})\right)}\boldsymbol{v}_n,\end{aligned}\] <p>其中 $s(\boldsymbol{k}_n,\boldsymbol{q})$ 为打分函数.图8.1给出键值对注意力机制的示例.当$K=V$时，键值对模式就等价于普通的注意力机制.</p> <h2 id="三自注意力模型self-attention">三、自注意力模型(Self-Attention)</h2> <p>由键值对注意力模式我们可以进一步引出 <code class="language-plaintext highlighter-rouge">自注意力模型</code>的概念，该模型通过注意力机制可以建立输入序列长距离依赖关系。自注意力模型计算过程如下图所示：</p> <p><img src="https://aurora-pics.oss-cn-beijing.aliyuncs.com/Pic/202407151855074.png" alt="截屏2024-07-15 18.54.23"/></p> <p>🔥Note:</p> <ol> <li>$X$为输入矩阵；</li> <li>$W^Q、W^K、W^V$是可学习的参数矩阵；</li> <li>最后得到的$Z$为attention值矩阵;</li> </ol> <blockquote> <p class="prompt-tip">为什么叫自注意力模型呢？<strong>因为我们可以看到这里Q、K、V都是由$X$通过一个线性变换投影得到的，而矩阵$W$是可学习的，所以由$X$到$Z$的映射权重是可学习的，是动态调整的。</strong></p> </blockquote> <p>如下图所示，红色表示当前的词，蓝色阴影表示与红色词的相关程度，通过自注意力模型，我们可以自动学到每个词与前面词的相关关系。</p> <p><img src="https://aurora-pics.oss-cn-beijing.aliyuncs.com/Pic/202407151902576.png" alt="截屏2024-07-15 19.02.19"/></p> <h2 id="四self-attention模型的复杂度分析">四、Self-Attention模型的复杂度分析</h2> <p>总的来说，自注意力模型的时间和空间复杂度与<strong>输入序列长度$N$呈2次关系</strong>，可以不严格的表示为$O(N^2)$.</p> <h3 id="1时间复杂度">（1）时间复杂度</h3> <p><img src="https://aurora-pics.oss-cn-beijing.aliyuncs.com/Pic/202407211928934.png" alt="截屏2024-07-21 19.28.20"/></p> <p><img src="https://aurora-pics.oss-cn-beijing.aliyuncs.com/Pic/202407211929340.png" alt="截屏2024-07-21 19.29.49"/></p> <h3 id="2空间复杂度">（2）空间复杂度</h3> <p><img src="https://aurora-pics.oss-cn-beijing.aliyuncs.com/Pic/202407211930137.png" alt="截屏2024-07-21 19.30.31"/></p> <h2 id="参考资料">参考资料</h2> <ol> <li><a href="https://github.com/scutan90/DeepLearning-500-questions">深度学习500问</a></li> <li>邱锡鹏，神经网络与深度学习，机械工业出版社，https://nndl.github.io/, 2020.</li> </ol>]]></content><author><name></name></author><category term="AI"/><category term="Large Language Models"/><category term="Machine Learning"/><category term="LLM"/><summary type="html"><![CDATA[在自然语言处理领域，注意力机制（Attention Mechanism）已经成为提升模型性能的重要工具。传统的Encoder-Decoder结构在处理长序列时，常常因为统一语义特征向量的长度限制而导致性能瓶颈。然而，注意力机制通过引入动态上下文向量，成功解决了这一问题，使得模型能够在每个时间步选择与当前输出最相关的信息。]]></summary><media:thumbnail xmlns:media="http://search.yahoo.com/mrss/" url="https://aurora-pics.oss-cn-beijing.aliyuncs.com/Pic/202407151855074.png"/><media:content medium="image" url="https://aurora-pics.oss-cn-beijing.aliyuncs.com/Pic/202407151855074.png" xmlns:media="http://search.yahoo.com/mrss/"/></entry><entry><title type="html">NLP Fundamentals: RNN, Seq2Seq and Attention Mechanism Basics</title><link href="https://auroralhl.github.io/blog/2024/nlp-fundamentals/" rel="alternate" type="text/html" title="NLP Fundamentals: RNN, Seq2Seq and Attention Mechanism Basics"/><published>2024-07-26T03:28:00+00:00</published><updated>2024-07-26T03:28:00+00:00</updated><id>https://auroralhl.github.io/blog/2024/nlp-fundamentals</id><content type="html" xml:base="https://auroralhl.github.io/blog/2024/nlp-fundamentals/"><![CDATA[<p>🔥 在自然语言处理（NLP）领域，理解和生成自然语言的能力对于构建智能系统至关重要。从文本分类、机器翻译到对话系统，底层技术的不断进步推动了NLP的发展。在这些技术中，循环神经网络（RNN）及其变种如长短期记忆网络（LSTM）、Seq2Seq模型和注意力机制（Attention Mechanism）扮演了重要角色。</p> <p>📚 本系列博客将从基础知识开始，逐步深入探讨大语言模型在NLP中的应用。我们将从RNN的经典结构出发，介绍其工作原理和局限性，接着探讨Seq2Seq模型及其在机器翻译中的应用，最后深入解读注意力机制及其在现代深度学习模型中的重要性。</p> <h2 id="一rnn经典结构">一、RNN经典结构</h2> <p><strong>循环神经网络（Recurrent Neural Network, RNN</strong>)是一类以序列数据为输入，在序列的演进方向进行递归且所有节点（循环单元）按链式连接的递归神经网络（recursive neural network）。对循环神经网络的研究始于二十世纪 80-90 年代，并在二十一世纪初发展为深度学习算法之一，其中双向循环神经网络和长短期记忆网络（Long Short-Term Memory networks，LSTM）是常见的循环神经网络。</p> <p>经典循环神经网络的单个神经元如下图所示，它由输入层、一个隐藏层和一个输出层组成：</p> <p><img src="https://aurora-pics.oss-cn-beijing.aliyuncs.com/Pic/202405261918426.png" alt="截屏2024-05-26 19.18.11"/></p> <ol> <li>$x_t$ 是一个向量，它表示<strong>t时刻</strong>输入层的值；</li> <li>$h_{t-1},h_t$ 分别表示<strong>t-1时刻和t时刻</strong>隐藏层的值.</li> </ol> <p>其中$h_t$的计算如下：</p> \[h_t=f(Uh_{t-1}+Wx_t+b),\] <p>其中$\mathbf{x}_t\in\mathbb{R}^M$ 为$t$时刻网络的输入，$\mathbf{U}\in\mathbb{R}^{D\times D}$ 为状态-状态权重矩阵，$\mathbf{W}\in\mathbb{R}^{D\times M}$为状态-输入权重矩阵，$\mathbf{b}\in\mathbb{R}^D$ 为偏置向量，$f$为<strong>激活函数</strong>.上面是RNN网络中的单个结点，下面给出长度为$T$的RNN网络，我们可以更清晰的看到前一时刻对后一时刻的影响.</p> <p><img src="https://aurora-pics.oss-cn-beijing.aliyuncs.com/Pic/202405261929403.png" alt="截屏2024-05-26 19.29.15"/></p> <p>现在看上去就比较清楚了, 这个网络在 $\mathrm{t}$ 时刻接收到输入 $\mathrm{x}<em>{t}$ 之后, 隐藏层的值是 $h_t$, 输出值是 $\mathrm{y}</em>{t}$ 。关键一点是, $h_t$ 的值不仅仅取决于 $\mathrm{x}<em>{t}$, 还取决于 $\mathrm{h}</em>{t-1}$ 。可以用下面的公式来表示循环神经网络的计算方法:</p> \[\mathrm{y}_{t}=V \mathrm{h}_{t}\quad (1)\] \[\mathrm{h}_{t}=f\left(W \mathrm{x}_{t}+U \mathrm{h}_{t-1}\right)\quad(2)\] <ol> <li>式 1 是输出层的计算公式, 输出层是一个全连接层, 也就是它的每个节点都和隐藏层的每个节点相连，$\mathrm{V}$ 是输出层的权重矩阵.</li> <li>式 2 是隐藏层的计算公式, 它是循环层。 $\mathrm{U}$ 是输入 $x$ 的权重矩阵, $\mathrm{W}$ 是上一次的值 $\mathrm{h}_{t-1}$ 作为这一次的输入的权重矩阵, $f$ 是激活函数.（这里我们省略$b$）</li> </ol> <p>从上面的公式可以看出, 循环层和全连接层的区别就是循环层多了一个权重矩阵 $\mathrm{W}$ 。如果反复把式 2 带入到式 1 , 将得到:</p> \[\begin{aligned} \mathrm{y}_{t} &amp;=V \mathrm{h}_{t} \\ &amp;=V f\left(W \mathrm{x}_{t}+U \mathrm{h}_{t-1}\right) \\ &amp;=V f\left(W \mathrm{x}_{t}+U f\left(W \mathrm{x}_{t-1}+U \mathrm{h}_{t-2}\right) \right)\\ &amp;=V f\left(W \mathrm{x}_{t}+U f\left(W \mathrm{x}_{t-1}+U f\left(W \mathrm{x}_{t-2}+U \mathrm{h}_{t-3}\right)\right)\right) \\ &amp;=V f\left(W \mathrm{x}_{t}+U f\left(W \mathrm{x}_{t-1}+U f\left(W \mathrm{x}_{t-2}+U f\left(W \mathrm{x}_{t-3}+\ldots\right)\right)\right)\right) \end{aligned}\] <p>从上面可以看出, 循环神经网络的输出值$\mathrm{y}_t$, 是受前面历次输入值 $ x<em>{t}, x</em>{t-1}, \cdots,x_1$影响的, 这就是为什么循环神经网络可以往前看任意多个输入值的原因。</p> <p><strong>Note:</strong></p> <ul> <li>从以上结构可看出，<strong>传统的RNN结构的输⼊和输出等⻓.</strong></li> <li>我们可以看到每个节点会<strong>共用参数</strong>$W,U,V$.</li> </ul> <h2 id="二vector-to-sequence结构">二、vector-to-sequence结构</h2> <p>有时我们要处理的问题输⼊是⼀个单独的值，输出是⼀个序列。</p> <p>此时，有两种主要建模⽅式：</p> <ol> <li> <p><strong>⽅式⼀：</strong>可只在其中的某⼀个序列进⾏计算，⽐如序列第⼀个进⾏输⼊计算，其建模⽅式如下：</p> <p><img src="https://aurora-pics.oss-cn-beijing.aliyuncs.com/Pic/202405281636083.png" alt="截屏2022-06-02 下午8.13.43"/></p> </li> <li> <p>把输⼊信息X作为每个阶段的输⼊，其建模⽅式如下：</p> <p><img src="https://aurora-pics.oss-cn-beijing.aliyuncs.com/Pic/202405281636216.png" alt="截屏2022-06-02 下午8.14.19"/></p> </li> </ol> <h2 id="三sequence-to-vector结构">三、sequence-to-vector结构</h2> <p>有时我们要处理的问题输⼊是⼀个序列，输出是⼀个单独的值，此时通常在最后的⼀个序列上进⾏输出变换，其建模如下所⽰：</p> <p><img src="https://aurora-pics.oss-cn-beijing.aliyuncs.com/Pic/202405281636872.png" alt="截屏2024-05-26 16.56.09"/></p> <h2 id="四seq2seq模型">四、Seq2Seq模型</h2> <p>RNN最重要的一个变种：Seq2Seq，这种结构又叫Encoder-Decoder模型。</p> <p>原始的sequence-to-sequence结构的RNN要求序列等长，然⽽我们遇到的⼤部分问题序列都是不等长的，如机器翻译中，源语⾔和⽬标语⾔的句⼦往往并没有相同的长度。</p> <h3 id="1-encoder">1 Encoder</h3> <p>将输⼊数据编码成⼀个上下⽂向量 c，这部分称为<strong>Encoder</strong>，其⽰意如下所⽰：</p> <p><img src="https://aurora-pics.oss-cn-beijing.aliyuncs.com/Pic/202405281637454.png" alt="截屏2022-06-02 下午8.19.38"/></p> <p>得到c有多种⽅式：</p> <ol> <li>最简单的⽅法就是把Encoder的最后⼀个隐状态赋值给c ：$c=h_4$</li> <li>还可以对最后的隐状态做⼀个变换得到 :$c=q(h_4)$</li> <li>也可以对所有的隐状态做变换:$c=q(h_1+h_2+h_3+h_4)$</li> </ol> <h3 id="2-decoder">2 Decoder</h3> <p><strong>拿到c之后，就用另一个RNN网络对其进行解码</strong>，这部分RNN网络被称为<strong>Decoder</strong>。具体做法就是将c当做初始状态输入到Decoder中：</p> <p><img src="https://aurora-pics.oss-cn-beijing.aliyuncs.com/Pic/202405281637667.png" alt="截屏2022-06-02 下午8.24.57"/></p> <p>还可以将c作为Decoder的每⼀步输⼊，⽰意图如下所⽰：</p> <p><img src="https://aurora-pics.oss-cn-beijing.aliyuncs.com/Pic/202405281637809.png" alt="截屏2022-06-02 下午8.26.20"/></p> <p>由于这种Encoder-Decoder结构不限制输入和输出的序列长度，因此应用的范围非常广泛，比如：</p> <ul> <li>机器翻译，Encoder-Decoder的最经典应用，事实上这一结构就是在机器翻译领域最先提出的；</li> <li>文本摘要，输入是一段文本序列，输出是这段文本序列的摘要序列；</li> <li>阅读理解，将输入的文章和问题分别编码，再对其进行解码得到问题的答案；</li> <li>语音识别，输入是语音信号序列，输出是文字序列。</li> </ul> <h2 id="五embedding">五、Embedding</h2> <h3 id="1-什么是-embedding">1 什么是 Embedding？</h3> <p>Embedding 是一种将离散的、高维的输入数据（如单词或符号）转换为连续的、低维的向量表示的方法。其目的是捕捉输入数据中的语义和结构信息，使其在向量空间中具有有意义的分布。</p> <h3 id="2-为什么需要-embedding">2 为什么需要 Embedding？</h3> <p>在 NLP 中，原始的文本数据通常由单词、子词或字符组成，这些元素本质上是离散的符号。传统的处理方法（如 one-hot 编码）会将每个单词表示为一个高维的稀疏向量，缺点如下：</p> <ol> <li><strong>维度过高</strong>：词汇表中的每个单词都需要一个独立的维度，对于大型词汇表，这会导致向量的维度非常高。</li> <li><strong>稀疏性</strong>：大多数维度都是 0，只有一个维度是 1，这导致向量非常稀疏。</li> <li><strong>缺乏语义信息</strong>：one-hot 向量无法捕捉单词之间的语义关系，如“国王”与“王后”之间的关系。</li> </ol> <p>Embedding 方法通过学习一个低维的、连续的向量表示来解决这些问题。每个单词或符号都被表示为一个实数向量，这些向量在训练过程中被优化，以捕捉单词之间的语义和语法关系。</p> <h3 id="3-embedding-的实现">3 Embedding 的实现</h3> <p>在深度学习模型（如 Transformer）中，embedding 通常通过一个可训练的查找表实现。查找表的每一行对应一个词汇表中的单词，行中的值是该单词的嵌入向量。这些嵌入向量在模型训练过程中不断调整，以优化模型的性能。</p> <p>具体步骤如下：</p> <ol> <li><strong>初始化</strong>：将每个单词初始化为一个随机的或预训练的向量。</li> <li><strong>训练</strong>：在训练过程中，模型根据任务目标不断调整这些向量，使得它们能够更好地表示单词的语义和语法信息。</li> <li><strong>使用</strong>：训练完成后，这些向量就可以用于各种 NLP 任务，如文本分类、情感分析、机器翻译等。</li> </ol> <h3 id="4-例子">4 例子</h3> <p>假设我们有一个简单的词汇表：[“猫”, “狗”, “鱼”]。通过 embedding 层，这些单词会被转换为低维的向量，如：</p> <ul> <li>“猫” -&gt; [0.2, -1.3, 0.5]</li> <li>“狗” -&gt; [0.3, -1.2, 0.4]</li> <li>“鱼” -&gt; [-0.5, 0.8, -0.1]</li> </ul> <p>这些向量的维度通常比原始 one-hot 向量的维度小得多（如 300 维，而不是数万维），并且这些向量可以捕捉到单词之间的语义相似性。例如，“猫”与“狗”的向量可能比较接近，而“鱼”的向量则相对远一些。</p> <h3 id="5-总结">5 总结</h3> <p>Embedding 是一种将离散的输入数据转换为连续的、低维向量表示的方法，广泛应用于自然语言处理任务中。它能够有效地捕捉单词之间的语义和语法关系，提高模型的处理能力和表现。在 Transformer 等深度学习模型中，embedding 层是关键组成部分，负责将输入序列转换为模型可以处理的向量形式。</p> <h2 id="六实例机器翻译">六、实例：机器翻译</h2> <p>神经机器翻译（Neural Machine Translation, NMT）是一种基于深度学习技术的机器翻译方法。与传统的统计机器翻译（Statistical Machine Translation, SMT）不同，NMT 使用神经网络模型来<strong>直接建模源语言到目标语言之间的翻译过程。</strong>下图是Encoder编码的过程：</p> <p><img src="https://aurora-pics.oss-cn-beijing.aliyuncs.com/Pic/202407091030337.png" alt="截屏2024-07-09 10.30.50"/></p> <p>Note:</p> <ol> <li>在翻译的时候，我们需要对词进行Embedding，将其变成一个连续的向量表达.</li> </ol> <p>Decoder的过程如下图所示：</p> <p><img src="https://aurora-pics.oss-cn-beijing.aliyuncs.com/Pic/202407091033500.png" alt="截屏2024-07-09 10.33.31"/></p> <p>Note:</p> <ol> <li>用Softmax来得到预测的每个词的概率，概率最大的作为当前预测词</li> </ol> <h2 id="参考资料">参考资料</h2> <ol> <li><a href="https://github.com/scutan90/DeepLearning-500-questions">深度学习500问</a></li> <li>邱锡鹏，神经网络与深度学习，机械工业出版社，<a href="https://nndl.github.io/, 2020.">https://nndl.github.io/, 2020.</a></li> </ol>]]></content><author><name></name></author><category term="AI"/><category term="Large Language Models"/><category term="Machine Learning"/><category term="LLM"/><summary type="html"><![CDATA[🔥 在自然语言处理（NLP）领域，理解和生成自然语言的能力对于构建智能系统至关重要。从文本分类、机器翻译到对话系统，底层技术的不断进步推动了NLP的发展。在这些技术中，循环神经网络（RNN）及其变种如长短期记忆网络（LSTM）、Seq2Seq模型和注意力机制（Attention Mechanism）扮演了重要角色。]]></summary></entry><entry><title type="html">Building Personal Website with GitHub Pages</title><link href="https://auroralhl.github.io/blog/2024/Githubpages_tutorial/" rel="alternate" type="text/html" title="Building Personal Website with GitHub Pages"/><published>2024-07-24T00:00:00+00:00</published><updated>2024-07-24T00:00:00+00:00</updated><id>https://auroralhl.github.io/blog/2024/Githubpages_tutorial</id><content type="html" xml:base="https://auroralhl.github.io/blog/2024/Githubpages_tutorial/"><![CDATA[<h2 id="前言">前言</h2> <p>23年7月份我第一次搭建了自己的个人博客，用的是WordPress+Argon模板，效果如下：</p> <p><img src="https://aurora-pics.oss-cn-beijing.aliyuncs.com/Pic/202407261106424.png" alt="截屏2024-07-26 11.04.21"/></p> <p>放弃继续使用这个网站有两个原因：</p> <ol> <li>由于当时买的是阿里的轻量级服务器，感觉每次访问速度都比较慢，并且服务器和域名续费居然比一开始买还高很多。</li> <li>WordPress还有一个较大的问题是对md文件公式的支持不太好，我写的含有数学公式的md文件直接上传网站公式会显示有问题。</li> </ol> <p>于是放弃WordPress，转而用Github Pages+Jekyll模板搭建。下面记录Github个人网站的搭建过程。</p> <h2 id="一介绍">一、介绍</h2> <h3 id="1-github-pages是什么">1 Github Pages是什么</h3> <p>Github Pages官网： <a href="https://pages.github.com/">https://pages.github.com/</a></p> <p>GitHub Pages 是 GitHub 提供的一个免费的静态网站托管服务，它允许 GitHub 用户创建和托管自己的静态网站，这些网站可以通过特定的 GitHub 仓库进行管理和托管。</p> <p>GitHub Pages 的主要特点包括：</p> <ul> <li><strong>免费托管</strong>： GitHub Pages 提供免费的静态网站托管服务，允许用户将自己的网站内容托管在 GitHub 上，用户不需要支付额外的托管费用；</li> <li><strong>使用简单：</strong> 创建和管理 GitHub Pages 静态网站相对简单，特别是对于熟悉 GitHub 的用户来说，用户只需在自己的 GitHub 帐户中创建一个特定名称的仓库，将网站文件上传到该仓库即可；</li> <li><strong>自定义域名：</strong> 用户可以选择使用自定义域名来访问他们的 GitHub Pages 网站，这使得它们更适合个人网站、博客和项目页面；</li> <li><strong>支持多种静态网站生成工具：</strong> GitHub Pages 支持多种静态网站生成工具，如 Jekyll、Hugo、Gatsby 等，以及纯HTML、CSS 和 JavaScript 等前端技术，这使得用户能够根据自己的需求选择适合他们的工具；</li> <li><strong>自动构建：</strong> GitHub Pages 可以自动构建用户上传的网站内容，无需用户手动生成或编译网页，这使得发布网站变得更加简单。</li> </ul> <p>对于开发人员和技术爱好者来说， GitHub Pages 是一个很好的选择，用于托管个人网站、博客、文档、项目页面等静态内容，它提供了一个方便的方式来分享和展示自己的作品。</p> <h3 id="2-静态网站生成工具">2 静态网站生成工具</h3> <p>GitHub Pages支持多种静态网站生成工具。以下是一些GitHub Pages支持的主要静态网站生成工具：</p> <ul> <li>Jekyll（ <a href="https://jekyllrb.com">https://jekyllrb.com</a>）： Jekyll是GitHub Pages的默认静态网站生成工具，它使用Markdown文件和Liquid模板引擎来创建静态网站，Jekyll对于博客和文档站点非常流行。</li> <li>Hugo（ <a href="https://gohugo.io/">https://gohugo.io/</a>）： Hugo是另一个受欢迎的静态网站生成工具，它非常快速且易于使用，它使用Go语言编写，支持多种主题和内容组织方式。</li> <li>Gatsby（ <a href="https://www.gatsbyjs.com/">https://www.gatsbyjs.com/</a>）： Gatsby是基于React的静态网站生成工具，它可以构建高性能、现代化的网站，Gatsby适用于博客、电子商务、应用程序文档等。</li> <li>VuePress（ <a href="https://vuepress.vuejs.org/">https://vuepress.vuejs.org/</a>）： VuePress是Vue.js驱动的静态网站生成工具，专注于文档站点，它支持Markdown文件和Vue组件。</li> <li>Hexo（ <a href="https://hexo.io/">https://hexo.io/</a>）： Hexo是一个快速、简单的博客框架，使用Markdown文件来生成静态博客，它是Node.js应用程序。</li> </ul> <p>这些静态网站生成工具各有利弊，我选择了Jekyll，下面简要介绍一下Jekyll及其使用。</p> <h3 id="3-jekyll">3 Jekyll</h3> <h4 id="简介">简介</h4> <p>Jekyll 是一个<strong>静态网站生成器，可以帮助我们使用简单的文本文件来创建静态网站。</strong>我们可以使用Markdown, HTML, CSS 以及 Liquid 模板语言来编写内容和设计网站布局。其特点如下：</p> <ul> <li>Jekyll 将这些文件转换成静态网页,我们可以将这些生成的网页文件直接部署到网站托管服务上 (不一定放在GitHub 里，放在你自己的服务器上加上买个域名也可以)。</li> <li>Jekyll 模板是其他人搭好的一个框架，在我们不需要操心其他样式的时候，我们只需要写 Markdown 文件就可以生成想要的静态网页。这就像我们制作 PPT 时用的其他人写好的模板，然 后我们只需要往里面填内容就行。当然如果我们想进行个性化修改，就得去扒一下模板的源码， 看看如何修改 CSS 和 HTML 文件了。</li> </ul> <h4 id="jekyll-和-github-的关系">Jekyll 和 GitHub 的关系</h4> <p>GitHub 支持使用 Jekyll 构建和托管网站。<strong>我们可以在 GitHub 上创建一个特定的仓库，将 Jekyll 项目代码“推送到该仓厍中。GitHub 将自动检测到这是一个 Jekyll 项目，并在后台使用 Jekyll 构建网站。</strong>我们可以通过 GitHub Pages 服务将该网站部署到一个专门的域名，也就是：<code class="language-plaintext highlighter-rouge">username.github.io</code></p> <blockquote> <p>总的来说就是：<strong>本地Jekyll项目+远程Github Pages</strong>.具体步骤就是：</p> <ol> <li>在Github上创建一个 <code class="language-plaintext highlighter-rouge">username.github.io</code>的仓库，用于托管网站；</li> <li>在本地用Jekyll创建一个网站项目，上传到Github的仓库（当然可以用模板）；</li> </ol> </blockquote> <h3 id="4-mac系统jekyll的安装及使用">4 Mac系统Jekyll的安装及使用</h3> <h4 id="安装">安装</h4> <p>要在 mac上安装 Jekyll，需要确保系统已安装 Ruby，通常mac预装了Ruby，我们不要使用系统的ruby，否则会有冲突，以下是安装 Jekyll 的步骤以及注释：</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># homebrew安装ruby</span>
brew <span class="nb">install </span>ruby

<span class="c"># 通过以下命令，可以查看ruby的安装路径</span>
brew info ruby

<span class="c"># 安装完成以后，修改.bash_profile文件,path路径加多/opt/homebrew/opt/ruby/bin，例如：export PATH=/opt/homebrew/opt/ruby/bin:$PATH</span>
vi ~/.zshrc
<span class="nb">source</span> ~/.zshrc

<span class="c"># 验证ruby 版本，如果打印最新的版本，如：ruby 3.x.x表示安装最新的了</span>
ruby <span class="nt">-v</span>

<span class="c"># 安装Jekyll</span>
gem <span class="nb">install</span> <span class="nt">--user-install</span> bundler jekyll

<span class="c"># 安装成功之后，调整gem的运行环境(gem的bin目录一般在~/.gem/ruby/ruby版本/bin目录),export PATH路径增加“$HOME/.gem/ruby/ruby版本/bin”</span>
vi ~/.zshrc
<span class="nb">source</span> ~/.zshrc

<span class="c"># 验证jekyll安装是否成功</span>
jekyll <span class="nt">-v</span>
</code></pre></div></div> <h4 id="jekyll的简单使用">Jekyll的简单使用</h4> <p>首先，新建一个jekyll项目：</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>jekyll new test-site
</code></pre></div></div> <p>运行过程如下：</p> <p><img src="https://aurora-pics.oss-cn-beijing.aliyuncs.com/Pic/202407241305592.png" alt="截屏2024-07-24 13.05.31"/></p> <p>打开test-site文件夹，可以看到有以下一些文件：</p> <p><img src="https://aurora-pics.oss-cn-beijing.aliyuncs.com/Pic/202407241306392.png" alt="截屏2024-07-24 13.06.27"/></p> <p>在终端进入对应项目目录，然后执行：</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>bundle <span class="nb">exec </span>jekyll serve
</code></pre></div></div> <p>可以看到项目以及启动：</p> <p><img src="https://aurora-pics.oss-cn-beijing.aliyuncs.com/Pic/202407241308944.png" alt="截屏2024-07-24 13.08.16"/></p> <p>在浏览器可以访问对应的地址(http://http://127.0.0.1:4000)即能在本地预览</p> <p><img src="https://aurora-pics.oss-cn-beijing.aliyuncs.com/Pic/202407241309367.png" alt="截屏2024-07-24 13.09.26"/></p> <p>在Jekyll生成项目的目录下，有一个比较重要的文件：<code class="language-plaintext highlighter-rouge">_config.yaml</code>，这个config文件用于指定 Jekyll 站点的各种设置和选项，包含了许多可配置的选项，用于自定义网站的行为和外观，生成的文件内容如下:</p> <div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># Welcome to Jekyll!</span>
<span class="c1">#</span>
<span class="c1"># This config file is meant for settings that affect your whole blog, values</span>
<span class="c1"># which you are expected to set up once and rarely edit after that. If you find</span>
<span class="c1"># yourself editing this file very often, consider using Jekyll's data files</span>
<span class="c1"># feature for the data you need to update frequently.</span>
<span class="c1">#</span>
<span class="c1"># For technical reasons, this file is *NOT* reloaded automatically when you use</span>
<span class="c1"># 'bundle exec jekyll serve'. If you change this file, please restart the server process.</span>
<span class="c1">#</span>
<span class="c1"># If you need help with YAML syntax, here are some quick references for you:</span>
<span class="c1"># https://learn-the-web.algonquindesign.ca/topics/markdown-yaml-cheat-sheet/#yaml</span>
<span class="c1"># https://learnxinyminutes.com/docs/yaml/</span>
<span class="c1">#</span>
<span class="c1"># Site settings</span>
<span class="c1"># These are used to personalize your new site. If you look in the HTML files,</span>
<span class="c1"># you will see them accessed via blank, , and so on.</span>
<span class="c1"># You can create any custom variable you would like, and they will be accessible</span>
<span class="c1"># in the templates via .</span>

<span class="c1"># 指定网站的标题</span>
<span class="na">title</span><span class="pi">:</span> <span class="s">Your awesome title</span>
<span class="c1"># 指定联系人邮箱地址</span>
<span class="na">email</span><span class="pi">:</span> <span class="s">your-email@example.com</span>
<span class="c1"># 网站的简要描述</span>
<span class="na">description</span><span class="pi">:</span> <span class="pi">&gt;-</span> <span class="c1"># this means to ignore newlines until "baseurl:"</span>
  <span class="s">Write an awesome description for your new site here. You can edit this</span>
  <span class="s">line in _config.yml. It will appear in your document head meta (for</span>
  <span class="s">Google search results) and in your feed.xml site description.</span>
<span class="c1"># 站点的子目录，如果你的网站托管在子目录下，需要指定</span>
<span class="na">baseurl</span><span class="pi">:</span> <span class="s2">"</span><span class="s">"</span> <span class="c1"># the subpath of your site, e.g. /blog</span>
<span class="c1"># 网站的基本 URL 地址</span>
<span class="na">url</span><span class="pi">:</span> <span class="s2">"</span><span class="s">"</span> <span class="c1"># the base hostname &amp; protocol for your site, e.g. http://example.com</span>
<span class="c1"># 推特的用户名</span>
<span class="na">twitter_username</span><span class="pi">:</span> <span class="s">jekyllrb</span>
<span class="c1"># github的用户名</span>
<span class="na">github_username</span><span class="pi">:</span> <span class="s">jekyll</span>

<span class="c1"># 指定要使用的 Jekyll 主题，如果不使用主题，则为空</span>
<span class="na">theme</span><span class="pi">:</span> <span class="s">minima</span>
<span class="c1"># 列出要在站点构建过程中使用的插件</span>
<span class="na">plugins</span><span class="pi">:</span>
  <span class="pi">-</span> <span class="s">jekyll-feed</span>
<span class="c1"># Exclude from processing.</span>
<span class="c1"># The following items will not be processed, by default.</span>
<span class="c1"># Any item listed under the `exclude:` key here will be automatically added to</span>
<span class="c1"># the internal "default list".</span>
<span class="c1">#</span>
<span class="c1"># Excluded items can be processed by explicitly listing the directories or</span>
<span class="c1"># their entries' file path in the `include:` list.</span>
<span class="c1">#</span>
<span class="c1"># exclude:</span>
<span class="c1">#   - .sass-cache/</span>
<span class="c1">#   - .jekyll-cache/</span>
<span class="c1">#   - gemfiles/</span>
<span class="c1">#   - Gemfile</span>
<span class="c1">#   - Gemfile.lock</span>
<span class="c1">#   - node_modules/</span>
<span class="c1">#   - vendor/bundle/</span>
<span class="c1">#   - vendor/cache/</span>
<span class="c1">#   - vendor/gems/</span>
<span class="c1">#   - vendor/ruby/</span>
</code></pre></div></div> <p>详细的参数，可以参考： https://jekyllrb.com/docs/configuration/.</p> <h2 id="二快速搭建第一个github-pages网站">二、快速搭建第一个Github Pages网站</h2> <p>在搭建前，默认已经注册成功了Github用户，现在开始根据官网教程一步一步的搭建。GithubPages的站点类型有几种：</p> <ol> <li><strong>个人或组织站点（User or Organization sites）</strong>：对于个人或组织站点，每个GitHub用户或组织只能有一个站点，它通常使用username.github.io或organizationname.github.io的格式，这是GitHub Pages的默认站点，通常用于个人网站、博客等。</li> <li><strong>项目站点（Project sites）：</strong>对于项目站点，每个GitHub仓库可以有一个关联的GitHub Pages站点，这意味着对于每个项目，您可以创建一个独立的GitHub Pages站点，无需限制。</li> </ol> <p>下面参考<a href="https://blog.51cto.com/u_15294985/7978684">保姆级教程：从零构建GitHub Pages静态网站</a>介绍如何搭建个人站点：</p> <p><strong>Step1： 新建一个项目</strong></p> <p>登录Github： https://github.com/，在顶部菜单栏点击“+”，然后“New repository”新建仓库，输入项目的相关信息，然后“Create repository”创建仓库：</p> <p><img src="https://aurora-pics.oss-cn-beijing.aliyuncs.com/Pic/202407241314292.png" alt="21000208_6532a4807911e93728"/></p> <p><strong>Step2： 创建一个界面文件</strong></p> <p>首先创建一个文件：</p> <p><img src="https://aurora-pics.oss-cn-beijing.aliyuncs.com/Pic/202407241314762.png" alt="21000208_6532a480a0a9623847"/></p> <p>输入文件内容，点击提交：</p> <p><img src="https://aurora-pics.oss-cn-beijing.aliyuncs.com/Pic/202407241315651.png" alt="21000208_6532a480c462f8177"/></p> <p>输入提交信息，点击提交:</p> <p><img src="https://aurora-pics.oss-cn-beijing.aliyuncs.com/Pic/202407241315973.png" alt="21000208_6532a480ee4a747098"/></p> <p><strong>Step3： 访问</strong></p> <p>大概等待几十秒，访问：https://用户名.github.io/，即可部署第一个属于自己的静态网站了，可以看到部署成功了。</p> <p>在Github有了 <code class="language-plaintext highlighter-rouge">username.github.io</code>仓库后，我们进需要把Jekyll创建的项目与这个仓库关联起来，每次更新将本地文件推送到Github上，Github Pages就能自动生成网页！</p> <h2 id="三静态网站模板chirpy">三、静态网站模板——Chirpy</h2> <p>为了使我们的网站比较好看，网上有很多模板可以用，可以从如下网址获取模板：</p> <ul> <li><a href="https://github.com/topics/jekyll-theme">https://github.com/topics/jekyll-theme</a></li> <li><a href="https://jekyllthemes.org/">https://jekyllthemes.org/</a></li> <li><a href="https://jekyllthemes.io/">https://jekyllthemes.io/</a></li> </ul> <p>我选择了Github上的<a href="https://github.com/cotes2020/jekyll-theme-chirpy/">Chirpy</a>模板，其网页Demo如下：<img src="https://aurora-pics.oss-cn-beijing.aliyuncs.com/Pic/202407241330803.png" alt="截屏2024-07-24 13.30.13"/></p> <p>在其主页上有详细的部署教程，此处不在赘述。</p> <h3 id="1-个人定制">1 个人定制</h3> <p>一些个人定制可以参考：https://huanyushi.github.io/posts/chirpy-blog-customization/</p> <p>个人觉得原始网页的帖子显示不是很好看，于是修改一下使得帖子边框更明显，并且鼠标悬停在上面会有轻微移动效果。修改完的具体效果如下：</p> <p><img src="https://aurora-pics.oss-cn-beijing.aliyuncs.com/Pic/202407251646884.png" alt="截屏2024-07-25 16.46.11"/></p> <p>只需要在 <code class="language-plaintext highlighter-rouge">assets/css/jekyll-theme-chirpy.scss</code>文件中加入以下代码即可：</p> <div class="language-scss highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="cm">/* 覆盖主题的博客帖子边框样式 */</span>
<span class="nc">.card-wrapper.card</span> <span class="p">{</span>
  <span class="nl">border</span><span class="p">:</span> <span class="m">3px</span> <span class="nb">solid</span> <span class="mh">#e3e5e7</span> <span class="o">!</span><span class="n">important</span><span class="p">;</span> <span class="cm">/* 使用更显眼的蓝色边框 */</span>
  <span class="nl">padding</span><span class="p">:</span> <span class="m">2px</span><span class="p">;</span> <span class="cm">/* 适中的内边距 */</span>
  <span class="nl">margin-bottom</span><span class="p">:</span> <span class="m">2px</span><span class="p">;</span> <span class="cm">/* 适中的下边距 */</span>
  <span class="nl">border-radius</span><span class="p">:</span> <span class="m">8px</span><span class="p">;</span> <span class="cm">/* 适中的圆角 */</span>
  <span class="nl">background-color</span><span class="p">:</span> <span class="mh">#f9f9f9</span><span class="p">;</span> <span class="cm">/* 淡灰色背景，增强边框的对比 */</span>
  <span class="nl">box-shadow</span><span class="p">:</span> <span class="m">0</span> <span class="m">4px</span> <span class="m">6px</span> <span class="nf">rgba</span><span class="p">(</span><span class="m">0</span><span class="o">,</span> <span class="m">0</span><span class="o">,</span> <span class="m">0</span><span class="o">,</span> <span class="m">0</span><span class="mi">.1</span><span class="p">);</span> <span class="cm">/* 轻微的阴影 */</span>
  <span class="nl">transition</span><span class="p">:</span>
    <span class="n">transform</span> <span class="m">0</span><span class="mi">.3s</span> <span class="nb">ease</span><span class="o">,</span>
    <span class="n">box-shadow</span> <span class="m">0</span><span class="mi">.3s</span> <span class="nb">ease</span><span class="p">;</span> <span class="cm">/* 添加平滑过渡效果 */</span>
<span class="p">}</span>

<span class="nc">.card-wrapper.card</span><span class="nd">:hover</span> <span class="p">{</span>
  <span class="nl">transform</span><span class="p">:</span> <span class="nf">translateY</span><span class="p">(</span><span class="m">-5px</span><span class="p">);</span> <span class="cm">/* 鼠标悬停时轻微上移 */</span>
  <span class="nl">box-shadow</span><span class="p">:</span> <span class="m">0</span> <span class="m">8px</span> <span class="m">12px</span> <span class="nf">rgba</span><span class="p">(</span><span class="m">0</span><span class="o">,</span> <span class="m">0</span><span class="o">,</span> <span class="m">0</span><span class="o">,</span> <span class="m">0</span><span class="mi">.2</span><span class="p">);</span> <span class="cm">/* 增加阴影效果 */</span>
<span class="p">}</span>
</code></pre></div></div> <p>其他效果的更改，结合ChatGPT也很方便。</p> <h2 id="四wordpress迁移到github">四、WordPress迁移到Github</h2> <p>Github个人网站搭建好后，我需要把WordPress上的文章迁移过来，Jekyll有一个 <code class="language-plaintext highlighter-rouge">jekyll-import</code>包支持从各种其他网站迁移到Jekyll，超级方便！其中从WordPress迁移过来的说明文档链接为：https://import.jekyllrb.com/docs/wordpress/。我用的主要命令是：</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>jekyll-import wordpress <span class="se">\</span>
  <span class="nt">--dbname</span> wordpress <span class="se">\ </span><span class="c"># 数据库名</span>
  <span class="nt">--user</span> lhl <span class="se">\ </span><span class="c">#SQL用户名</span>
  <span class="nt">--password</span> 密码 <span class="se">\</span>
  <span class="nt">--host</span> 服务器公网地址 <span class="se">\</span>
  <span class="nt">--port</span> 3306 <span class="se">\</span>
  <span class="nt">--table_prefix</span> wp_
</code></pre></div></div> <p>不过这个命令用的时候也有很多问题，利用ChatGPT可以一一解决，此处不再赘述。用这个命令成功的将原网站的文章全都迁移过来，至此，我的网站迁移计划大功告成！</p> <h2 id="参考资料">参考资料</h2> <ol> <li><a href="https://blog.51cto.com/u_15294985/7978684">保姆级教程：从零构建GitHub Pages静态网站</a></li> <li><a href="https://chirpy.cotes.page/posts/write-a-new-post/">Chirpy</a></li> <li><a href="https://www.zhihu.com/question/20223939/answer/3486773682">有哪些简洁明快的 Jekyll 模板？</a></li> </ol>]]></content><author><name></name></author><category term="Tutorial"/><category term="Web Development"/><category term="Website Building"/><category term="Github"/><summary type="html"><![CDATA[前言]]></summary></entry><entry><title type="html">Introduction to Machine Learning</title><link href="https://auroralhl.github.io/blog/2024/ML1/" rel="alternate" type="text/html" title="Introduction to Machine Learning"/><published>2024-07-24T00:00:00+00:00</published><updated>2024-07-24T00:00:00+00:00</updated><id>https://auroralhl.github.io/blog/2024/ML1</id><content type="html" xml:base="https://auroralhl.github.io/blog/2024/ML1/"><![CDATA[<h2 id="一基本概念">一、基本概念</h2> <h3 id="什么是机器学习">什么是机器学习</h3> <p>总的来说，机器学习就是大数据时代背景下<strong>处理数据的各种方法</strong>的总称。机器学习利用各种数理模型对数据进行建模，对数据进行预测和分析，在实际应用中指导决策。</p> <p>我们通常将用于学习的数据对象或者实例称为<strong>样例或者样本</strong>，每个样例x采用一个向量 $x=(x^{(1)},x^{(2)},\cdots,x^{(n)})^T$来表示.</p> <ul> <li>向量的每个分量对应样例的一个<strong>特征或者属性.</strong></li> <li>n为样例$x$的特征个数，也称为<strong>维数，</strong>$x^{(i)}$为样例x的第i 维属性的属性值.</li> <li>属性张成的空间$\chi$为<strong>特征空间，也称为样本空间或输入空间</strong>，记作$\chi$.</li> </ul> \[x=(x^{(1)},x^{(2)},\cdots,x^{(n)})^T\in\mathcal{X}\] <p>一般而言，数据对象的特征和学习任务相关，如果有不相关的特征可能会影响模型的预测能力。</p> <h3 id="机器学习的分类">机器学习的分类</h3> <p>机器学习一般包括监督学习、无监督学习、半监督学习、强化学习等。</p> <h4 id="监督学习">监督学习</h4> <p>监督学习 (Supervised learning)基于给定的<strong>标记数据集</strong>$T={(x_i,y_i)}_{i=1}^N$学习从输入空间$\chi$ 到输出空间$\gamma$ 的映射 (模型)，并利用该映射对未见 (unseen) 实例x对应的输出y进行预测。监督学习有两个核心问题：</p> <ul> <li> <p><strong>分类(classification)问题</strong>：输出空间$\gamma$是一个离散值的集合(通常也是有限的).</p> <ul> <li>$\mathcal{Y}={c_1,c_2,\cdots,c_M}$, 其中$M$为类别的个数.</li> <li> <p>二分类(binary classification)问题：$M=2.$</p> <ul> <li>$\mathcal{Y}={+1,-1}.$</li> <li>$\mathcal{Y}={0,1}.$</li> </ul> </li> <li>多分类(multi-class classification) 问题：$M&gt;2.$</li> </ul> </li> <li> <p><strong>回归 (regression) 问题</strong>：输出空间$\gamma=\mathbb{R}.$</p> </li> </ul> <h4 id="无监督学习">无监督学习</h4> <p>无监督学习( Unsupervised learning) 基于给定的<strong>无标记的数据集</strong>$T={x_i}_{i=1}^N$，发现数据中隐含的知识或者模式(interesting patterns)，并将学得的模式应用于未见实例。</p> <ul> <li>无监督学习通常也被称为知识发现(knowledge discovery)；</li> <li>通常没有明确的知识模式类型、衡量学习结果等的度量，依赖于具体学习场景和应用领域；</li> <li>更具有主观性和挑战性.</li> </ul> <p>一个典型的无监督学习任务——<strong>聚类</strong>，聚类的目的是将无标记的数据集$T={x_i}_{i=1}^N$划分成若干子集。</p> <ul> <li>这些子集通常互不相交，称每个子集为簇 N，每个簇对应于一个潜在的概念；</li> <li>属于同一子集的样本数据尽<strong>可能相互相似</strong>；</li> <li>不同子集的样本尽可能不同</li> </ul> <h4 id="半监督学习和强化学习">半监督学习和强化学习</h4> <p><img src="https://aurora-pics.oss-cn-beijing.aliyuncs.com/Pic/202407241358100.png" alt="截屏2023-12-20 12.22.59"/></p> <h2 id="二模型选择">二、模型选择</h2> <h3 id="机器学习三要素">机器学习三要素</h3> <p>机器学习方法都是由模型、策略和算法构成的，即机器学习方法由三要素构成，可以简单地表示为：</p> <blockquote> <p>机器学习方法＝<strong>模型＋策略＋算法</strong></p> </blockquote> <ul> <li><strong>模型：</strong>在监督学习过程中，模型就是所要学习的<strong>条件概率分布或决策函数</strong>。模型的假设空间包含所有可能的条件概率分布或决策函数；</li> <li><strong>策略：</strong>有了模型的假设空间，机器学习接着需要考虑的是按照什么样的<strong>准则</strong>学习或选择最优的模型；</li> <li><strong>算法：</strong>算法是指学习模型的具体计算方法；</li> </ul> <h3 id="学习策略">学习策略</h3> <p>机器学习关心的问题是要学什么样的模型，在监督学习过程中，模型就是所要学习的条件概率分布或决策函数。模型的<strong>假设空间(hypothesis space)</strong>包含所有可能的条件概率分布或决策函数。例如，假设决策函数是输入变量的线性函数，那么模型的假设空间就是所有这些线性函数构成的函数集合，假设空间中的模型一般有无穷多个。那么怎么选出最好的模型呢，首先引入损失函数与风险函数的概念：</p> <ul> <li><strong>损失函数</strong>度量模型一次预测的好坏；</li> <li><strong>风险函数</strong>度量平均意义下模型预测的好坏。</li> </ul> <p>在假设空间$\mathcal{F}$ 中选取模型 $f$ 作为决策函数，对于给定的输入 X,由 $f(X)$ 给出相应的输出$Y$,这个输出的预测值 $f(X)$ 与真实值$Y$ 可能一致也可能不一致，用一个<strong>损失函数(loss function)或代价函数(cost function)</strong>来度量预测错误的程度。损失函数是 $f(X)$ 和 Y 的非负实值函数，记作 $L(Y,f(X))$。机器学习常用的损失函数有以下几种：</p> <ol> <li> <p><strong>0-1 损失函数</strong>(0-1 loss function)</p> \[L(Y,f(X))=\begin{cases}&amp;1,\quad Y\neq f(X)\\&amp;0,\quad Y=f(X)\end{cases}\] </li> <li> <p><strong>平方损失函数</strong>(quadratic loss function)</p> \[L(Y,f(X))=(Y-f(X))^2\] </li> <li> <p><strong>对数损失函数</strong>(logarithmic loss function)或对数似然损失函数(log-likelihood loss function)</p> </li> </ol> \[\begin{aligned}L(Y,P(Y|X))&amp;=-\log P(Y|X)\end{aligned}\] <p>损失函数值越小，模型就越好。由于模型的输入、输出 $(X,Y)$ 是随机变量，遵循联合分布$P(X,Y)$,所以损失函数的期望是:</p> \[\begin{aligned} R_{\mathrm{exp}}(f)&amp; =E_{P}[L(Y,f(X))] \\ &amp;=\int_{\mathcal{X}\times\mathcal{Y}}L(y,f(x))P(x,y)\mathrm{d}x\mathrm{d}y. \end{aligned}\] <p>这是理论上模型 $f(X)$ 关于联合分布 $P(X,Y)$ 的平均意义下的损失，称为<strong>风险函数 (risk function) 或期望损失(expected loss)。</strong>由于联合分布$P(X,Y)$ 是不知道的，所以通常我们不会直接使用风险函数，而是用经验风险。给定一个训练数据集</p> \[T=\left\{\left(x_1, y_1\right),\left(x_2, y_2\right), \cdots,\left(x_N, y_N\right)\right\}\] <p>模型 $f(X)$ 关于训练数据集的平均损失称为<strong>经验风险 (empirical risk)</strong> , 记作 $R_{\text {emp }}$ :</p> \[R_{\text {emp }}(f)=\frac{1}{N} \sum_{i=1}^N L\left(y_i, f\left(x_i\right)\right)\] <ul> <li>期望风险 $R_{\exp }(f)$ 是模型关于联合分布的期望损失；</li> <li>经验风险 $R_{\mathrm{emp}}(f)$ 是模型关于训练样本集的平均损失。</li> </ul> <p>根据大数定律, <strong>当样本容量 $N$ 趋于无穷时, 经验风险 $R_{\mathrm{emp}}(f)$ 趋于期望风险 $R_{\exp }(f)$</strong> 。所以一个很自然的想法是用经验风险估计期望风险。但是, 由于现实中训练样本数目有限, 甚至很小, 所以用经验风险估计期望风险常常并不理想, 要对经验风险进行一定的矫正。这就关系到监督学习的两个基本策略: 经验风险最小化和结构风险最小化。</p> <h4 id="经验风险最小化">经验风险最小化</h4> <p>在假设空间、损失函数以及训练数据集确定的情况下，经验风险函数式(1.14) 就可以确定。经验风险最小化 (empirical risk minimization, ERM) 的策略认为，经验风险最小的模型是最优的模型。根据这一策略，按照经验风险最小化求最优模型就是求解最优化问题：</p> \[\min_{f\in\mathcal{F}}\frac{1}{N}\sum_{i=1}^{N}L(y_i,f(x_i))\] <p>其中$\mathcal{F}$ 是假设空间。当样本容量足够大时，经验风险最小化能保证有很好的学习效果，在现实中被广泛采用。比如，极大似然估计(maximum likelihood estimation) 就是经验风险最小化的一个例子。当模型是条件概率分布、损失函数是对数损失函数时，经验风险最小化就等价于极大似然估计。但是，当样本容量很小时，经验风险最小化学习的效果就未必很好，会产生“<strong>过拟合” (over-fitting)</strong> 现象。</p> <h4 id="结构风险最小化">结构风险最小化</h4> <p><strong>结构风险最小化 (structural risk minimization, SRM) 是为了防止过拟合而提出来的策略</strong>。结构风险最小化等价于正则化(regularization)。结构风险在经验风险上加上表示模型复杂度的正则化项(regularizer)或罚项(penalty term)。在假设空间、损失函数以及训练数据集确定的情况下，结构风险的定义是</p> \[R_{\mathrm{srm}}(f)=\frac1N\sum_{i=1}^NL(y_i,f(x_i))+\lambda J(f)\] <ul> <li>其中，$J(f)$ 为模型的复杂度，是定义在假设空间$\mathcal{F}$上的泛函；</li> <li>模型 $f$ 越复杂，复杂度 $J(f)$ 就越大；反之，模型 $f$ 越简单，复杂度 $J(f)$ 就越小；</li> <li>$\lambda\geqslant0$ 是系数，用以权衡经验风险和模型复杂度；</li> <li>结构风险小需要经验风险与模型复杂度同时小，结构风险小的模型往往对训练数据以及未知的测试数据都有较好的预测。</li> </ul> <p><img src="https://aurora-pics.oss-cn-beijing.aliyuncs.com/Pic/202407241358063.png" alt="截屏2023-12-20 16.09.55" style="zoom:67%;"/></p> <p>上图给出了模型复杂度与训练误差和测试误差的关系：</p> <ol> <li>模型复杂度小，训练误差较大，被称为<strong>欠拟合</strong>现象；</li> <li>模型复杂度过高，训练误差小，测试误差（泛化误差）大，被称为<strong>过拟合现象</strong>。</li> </ol> <p>比如，贝叶斯估计中的最大后验概率估计(maximum posterior probability estimation, MAP) 就是结构风险最小化的一个例子。当模型是条件概率分布、损失函数是对数损失函数、模型复杂度由模型的先验概率表示时，结构风险最小化就等价于最大后验概率估计。</p> <p>结构风险最小化的策略认为结构风险最小的模型是最优的模型，所以求最优模型就是求解最优化问题：</p> \[\min_{f\in\mathcal{F}}\frac1N\sum_{i=1}^NL(y_i,f(x_i))+\lambda J(f).\] <h3 id="训练误差与泛化误差">训练误差与泛化误差</h3> <ul> <li>训练误差：模型在训练集上的<strong>经验风险</strong>，记为：$\hat{R}(f)=\frac1N\sum_{i=1}^NL(y_i,f(x_i))$；</li> <li>泛化误差：学习的模型在对未知数据的预测能力，也就是<strong>期望风险</strong>，记为$R(f)=E[L(Y,f(X))]$；</li> </ul> <p>通过经验风险最小化，选出的模型记为：</p> \[f_N = \arg \min_{f\in\mathcal{F}} \hat{R}(f).\] <p>$f_N$ 依赖训练数据集的样本容量 $N$。人们更关心的是 $f_N$ 的泛化能力</p> \[R(f_{N})=E[L(Y,f_{N}(X))].\] <p>下面讨论从有限集合$\mathcal{F}={f_1,f_2,\cdots,f_d}$ 中任意选出的函数 $f$ 的<strong>泛化误差上界</strong>。</p> <p><strong>定理 (泛化误差上界)</strong></p> <blockquote> <p>对二类分类问题，当假设空间是有限个函数的集合 $\mathcal{F}=$</p> <p>${f_1,f_2,\cdots,f_d}$ 时，对任意一个函数 $f\in\mathcal{F}$, 至少以概率 $1-\delta,0&lt;\delta&lt;1$, 以下不等式成立：</p> \[R(f)\leqslant\hat{R}(f)+\varepsilon(d,N,\delta)\] <p>其中：</p> \[\varepsilon(d,N,\delta) = \sqrt{\frac{1}{N}(\log d+\log\frac{1}{\delta})}.\] </blockquote> <p>从上述定理我们可以知道：</p> <ul> <li>$R(f)$ 是泛化误差，右端即为泛化误差上界；</li> <li>在泛化误差上界中，第1 项是训练误差，训练误差越小，泛化误差也越小；</li> <li>第2 项 $\varepsilon(d,N,\delta)$ <strong>是 N 的单调递减函数， 当$N$ 趋于无穷时趋于 0</strong>;</li> <li>同时它也是 $\sqrt{\log d}$ 阶的函数<strong>，假设空间 $\mathcal{F}$ 包含的函数越多，其值越大</strong>;</li> <li><strong>训练误差$\hat{R}(f)$小并不能一定保证模型$f_N$的泛化性能好</strong>。</li> </ul> <p>泛化性能与学习算法捕获所有样本的共有知识模式的能力有关经验误差反映的是学习算法捕获训练数据蕴含的知识模式的能力。过小的训练误差可能导致所谓的过拟合 (Overfitting) 现象。</p> <h3 id="泛化误差的偏差-方差分解">泛化误差的偏差-方差分解</h3> <p>我们知道随着模型复杂度的增加，学习算法的学习能力越来越强，训练误差越来越小。而泛化误差先是随着训练误差的缩小而减小，但随着模型复杂度的进一步增加泛化误差不降反升。</p> <p>泛化误差与模型复杂度的关系为什么这样？ 针对回归任务，进一步讨论泛化误差由哪几个部分构成. 我们设$h_\mathrm{T}$是基于训练数据集$T$学习到的回归模型，对给定的$x$, 则学习算法的泛化误差为</p> \[E_T[(h_T(x)-c(x))^2].\] <p>其中，$c(x)$为真实标记。定义学习算法对数据$x$的<strong>期望输出</strong>为</p> \[\bar{h}(x)=E_T[h_T(x)].\] <p>$x$的期望输出与真实标记c$(x)$之间的差别称为<strong>偏差</strong>，即</p> \[Bias(x)=E_T[(h_T(x)-c(x)]=\bar{h}(x)-c(x).\] <ul> <li>偏差描述了学习算法对$x$的预测期望相对于x的真实输出的偏离程度；</li> <li><strong>偏差反映了学习算法的学习能力，偏差越小，说明学习算法的学习能力越强</strong>.</li> </ul> <p>基于相同样本容量的不同训练数据集产生的<strong>预测方差</strong>为</p> \[Var(x)=E_T[(h_T(x)-\bar{h}(x))^2].\] <ul> <li>方差刻画学习算法使用相同容量的不同训练数据集所导致的学习性能的变动情况；</li> <li><strong>方差越小，说明学习算法对数据扰动的容忍能力越强</strong>.</li> </ul> <p>接下来，我们对泛化误差进行如下分解：</p> \[\begin{aligned} E_T[(h_T(x)-c(x))^2] &amp;=E_T[h_T^2(x)-2h_T(x)c(x)+c^2(x)] \\ &amp;=E_T[h_T^2(x)]-2E_T[h_T(x)]c(x)+c^2(x) \\ &amp;= E_T[h_T^2(x)]-2\bar{h}(x)c(x)+c^2(x) \\ &amp;=E_T[h_T^2(x)]-\bar{h}^2(x)+\bar{h}^2(x)-2\bar{h}(x)c(x)+c^2(x) \\ &amp;=E_T[(h_T(x)-\bar{h}(x))^2]+(\bar{h}(x)-c(x))^2 \\ &amp;=-Var(x)+Bias^2(x). \\ \end{aligned}\] <p>这说明泛化误差可分解为<strong>方差和偏差</strong>的平方之和。由于噪声等的存在，使得$x$对应的观测$y$未必一定有</p> \[y=c(x).\] <p>我们不妨设</p> \[y=c(x)+\varepsilon,\] <p>其中$\varepsilon$为噪声，假定$\varepsilon$服从分布$\varepsilon$且其期望为0，即$E[\varepsilon]=0$.那么可以得到：</p> \[E_{T\sim D|T|,\varepsilon\sim\mathcal{E}}[(h_T(x)-y)^2]=Var(x)+Bias^2(x)+E[\varepsilon^2],\] <p>即泛化误差可以<strong>分解为方差、偏差和噪声三部分</strong>，其中噪声部分也称为不可约误差，反映了学习问题本身的难度.</p> <p><img src="https://aurora-pics.oss-cn-beijing.aliyuncs.com/Pic/202407241358130.png" alt="截屏2023-12-20 16.36.21"/></p> <p>方差和偏差通常是相互抵触的:</p> <ul> <li>当模型复杂度过于简单时，拟合能力比较弱，<strong>对数据扰动不敏感</strong>,此时偏差在泛化误差中起主导作用；</li> <li>随着模型复杂度的提高，算法的拟合能力不断增强，偏差逐渐减少。但学习能力的提高也带来过拟合的风险，使得学习算法<strong>对数据扰动逐渐敏感</strong>，方差在泛化误差中的比重逐渐增大，最终导致泛化误差不断增大。</li> </ul> <h3 id="正则化">正则化</h3> <p>正则化一般具有如下形式:</p> \[\min _{f \in \mathcal{F}} \frac{1}{N} \sum_{i=1}^N L\left(y_i, f\left(x_i\right)\right)+\lambda \Omega(f)\] <p>其中:</p> <ol> <li>第 1 项是经验风险；</li> <li>第 2 项是正则化项, $\lambda \geqslant 0$ 为正则化参数；</li> </ol> <p>正则化项可以取不同的形式。例如, 在回归问题中, 正则化项可以是参数向量的 $L_2$ 范数:</p> \[L(w)=\frac{1}{N} \sum_{i=1}^N\left(f\left(x_i ; w\right)-y_i\right)^2+\frac{\lambda}{2}\|w\|^2\] <p>这里, $|w|$ 表示参数向量 $w$ 的 $L_2$ 范数。正则化项也可以是参数向量的 $L_1$ 范数:</p> \[L(w)=\frac{1}{N} \sum_{i=1}^N\left(f\left(x_i ; w\right)-y_i\right)^2+\lambda\|w\|_1\] <p>这里, $|w|_1$ 表示参数向量 $w$ 的 $L_1$ 范数。</p> <p><img src="https://aurora-pics.oss-cn-beijing.aliyuncs.com/Pic/202309141039222.png" alt="截屏2023-09-14 10.39.28"/></p> <h3 id="交叉验证">交叉验证</h3> <p>如果给定的样本数据充足，进行模型选择的一种简单方法是随机地将数据集切分成三部分，分别为训练集（training set）、验证集（validation set）和测试集（test set）。训练集用来训练模型，验证集用于模型的选择，而测试集用于最终对学习方法的评估。</p> <p>但是，在许多实际应用中数据是不充足的。为了选择好的模型，可以采用交叉验证方法。交叉验证的基本想法是重复地使用数据，把给定的数据进行切分，将切分的数据集组合为训练集与测试集，在此基础上反复地进行训练、测试以及模型选择。</p> <p><strong>1 简单交叉验证(留出法）</strong></p> <blockquote> <p>简单交叉验证方法是：</p> <ol> <li>首先随机地将已给数据分为两部分，一部分作为训练集，另一部分作为测试集（例如，70％的数据为训练集，30％的数据为测试集）；</li> <li>然后用训练集在各种条件下（例如，不同的参数个数）训练模型，从而得到不同的模型；</li> <li>在测试集上评价各个模型的测试误差，选出测试误差最小的模型。</li> </ol> </blockquote> <p><img src="https://aurora-pics.oss-cn-beijing.aliyuncs.com/Pic/202312201744491.png" alt="截屏2023-12-20 17.43.46"/></p> <p><strong>2 <em>K</em> 折交叉验证</strong></p> <blockquote> <p>应用最多的是 <em>K</em> 折交叉验证（<em>K</em>-fold cross validation），方法如下：</p> <ol> <li>首先将数据集$D$随机划分为$k$个互不相交、大小相似的子集$D_1,D_2,\cdots,D_k.$</li> <li>然进行$k$次训练-测试过程，其中第$i$次训练-学习过程中以$D-D_i$为训练数据集学得模型$h_{D-D_i}$</li> <li>以$D_i$为测试集对$h_{D-D_i}$进行测试评估，得到测试误差$\hat{R}<em>{test}(h</em>{D-D_i}).$</li> <li> <p>以</p> \[\frac1k\sum_{i=1}^k\hat{R}_{test}(h_{D-D_i})\] <p>作为$h_D$在本次数据集随机划分下的测试评估结果。</p> </li> <li>将这一过程对可能的 <em>K</em> 种选择重复进行；最后选出 <em>K</em> 次评测中平均测试误差最小的模型。</li> </ol> <p>比较耗时，每轮要训练K次。</p> </blockquote> <p><strong>3 留一交叉验证</strong></p> <blockquote> <p><em>K</em> 折交叉验证的特殊情形是 <em>K</em> = <em>N</em>，称为留一交叉验证（leave-one-out cross validation），往往在数据缺乏的情况下使用。这里，<em>N</em> 是给定数据集的容量。设数据集为$D$，其误差为：</p> \[\hat{R}(f_D)=\frac1{|D|}\sum_{x\in D}L(f_{D-\{x\}}(x),y).\] <ul> <li>也就是说每次测试集只有一个数据</li> <li>训练N次，数据量大时，计算开销大</li> </ul> </blockquote> <h3 id="自助法">自助法</h3> <p>自助法是为了解决交叉验证法在模型选择阶段<strong>训练集规模比整个样本小</strong>的问题，采用<strong>有放回抽样</strong>对交叉验证法进行改造。其具体策略如下：</p> <ol> <li> <table> <tbody> <tr> <td>先从 $D$ 中以有放回的抽样方式随机抽取 $</td> <td>D</td> <td>$ 个数据来构建训 练数据集 $T$,</td> </tr> </tbody> </table> </li> <li>然后以 $D$ 中没有被抽中的数据构建测试数据集 $T^{\prime}$.</li> </ol> <p><img src="https://aurora-pics.oss-cn-beijing.aliyuncs.com/Pic/202309161228538.png" alt="截屏2023-09-16 12.28.29"/></p> <ul> <li>自助法解决了交叉验证法中模型选择阶段和最终模型训练阶段的训练集规模差异问题.</li> </ul> <h2 id="三模型评估">三、模型评估</h2> <h3 id="回归评价指标">回归评价指标</h3> <h4 id="1-mse">1 MSE</h4> <p>回归问题最常用的评价指标是均方误差（Mean Square Error,MSE），其计算公式为：</p> \[MSE(y,\hat y)=\frac{1}{n}\sum_{i=1}^{n}(y_i-\hat y_i)^2\] <h4 id="2-rmse">2 RMSE</h4> <p>均方根误差（Root Mean Square Error,RMSE）：</p> \[RMSE(y,\hat y)=\sqrt{MSE(y,\hat y)}=\sqrt{\frac{1}{n}\sum_{i=1}^{n}(y_i-\hat y_i)^2}\] <p>均方误差和均方根误差通常会放大离群值对模型评估结果的影响，一种克服这个问题的方法是用，平均绝对误差（Mean Absolute Error,MAE)</p> <h4 id="3-mae">3 MAE</h4> <p>平均绝对误差（Mean Absolute Error,MAE):</p> \[MAE(y,\hat y)=\frac{1}{n}\sum_{i=1}^{n}|y_i-\hat y_i|\] <h3 id="分类评价指标">分类评价指标</h3> <p>在二分类问题中，每一个样本可以划分为以下四种类型：</p> <ol> <li>真正(True Positive , TP)：被模型预测为正的正样本。</li> <li>真负(True Negative , TN)：被模型预测为负的负样本。</li> <li>假正(False Positive , FP)：被模型预测为正的负样本。</li> <li>假负(False Negative , FN)：被模型预测为负的正样本。</li> </ol> <p>根据样本的真实标签和预测标签，可以得到一个混淆矩阵：</p> <p><img src="https://aurora-pics.oss-cn-beijing.aliyuncs.com/Pic/202312201756154.png" alt="截屏2022-05-18 下午9.11.58" style="zoom:67%;"/></p> <p>正负样本选择，没有明确规定，根据应用场景进行选择，在机器学习中，往往将<strong>少数样本</strong>定义为正样本，表示希望<strong>模型在训练时更加关注少数样本</strong>。</p> <h4 id="1-正确率">1 正确率</h4> <p>分类问题中最常见的指标是正确率(accuracy)，表示<strong>模型预测正确的样本比例</strong>，给定混淆矩阵时，正确率计算公式如下：</p> \[正确率=\frac{TP+TN}{TP+TN+FP+FN}\] <p>在<strong>类别不均衡</strong>时，正确率不是一个很好的度量模型好坏的指标。</p> <blockquote> <p>比如，在文本情绪分类数据集中，有80%为正向的内容，20%为负向的内容，假如这个模型将所有样本都判断为正向，则正确率有80%!但是显然这个模型并不好</p> </blockquote> <p>对于类别不均衡的数据集，精度和召回率是比正确率更好的性能评价指标</p> <h4 id="2-精度">2 精度</h4> <p><strong>精度(precision)</strong>指正确预测的正样本占所有预测为正样本的比例：</p> \[精度=\frac{TP}{TP+FP}\] <h4 id="3-召回率">3 召回率</h4> <p>召回率（recall)又称为灵敏度或命中率，是指正样本中被正确预测的比例：</p> \[召回率=\frac{TP}{TP+FN}\] <p>通常精度和召回率是负相关的，实际应用中高精度往往对应低召回率，反之亦然，我们需要根据实际问题场景来选择哪一个指标。</p> <h4 id="4-f值">4 F值</h4> <p>单独考虑精度和召回率是片面的，需要综合考虑二者，在二分类问题中，可以定义一个综合考虑精度和召回率的指标，这个指标为F值：</p> \[F=\frac{(1+\beta^2)精度\times 召回率}{\beta^2\times精度+召回率}\] <p>其中$\beta$为正数，其作用是调节精度和召回率的权重，$\beta$越大，召回率的权重越大，$\beta$越小，精度的权重越大。当$\beta=1$时，它是精度和召回率的调和平均数：</p> <h2 id="参考资料">参考资料</h2> <ol> <li>李航 et al. 统计学习方法. 清华大学出版社, 2019.</li> </ol>]]></content><author><name></name></author><category term="AI"/><category term="Machine Learning"/><category term="Machine Learning"/><summary type="html"><![CDATA[一、基本概念]]></summary></entry></feed>